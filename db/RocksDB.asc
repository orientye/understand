= RocksDB
:toc:
:toclevels: 5
:hardbreaks-option:

<<<

== 概览
- 概要
https://github.com/facebook/rocksdb/wiki/RocksDB-Overview
https://github.com/facebook/rocksdb/wiki/RocksDB-FAQ
https://www.zhihu.com/question/533045476
https://xie.infoq.cn/article/462cb16f70d9a0eba30c0ef89

- vs. LevelDB
https://github.com/facebook/rocksdb/wiki/Features-Not-in-LevelDB

- LSM(Log-Structured Merge-Trees) vs B+
LSM-Tree: 为写入优化。其核心思想是将随机写入转换为顺序写入，通过一种“延迟”和“批处理”的方式在后台进行数据合并。
B+ Tree: 为读取优化。其核心思想是在磁盘上维护一个始终有序的、平衡的树状结构，提供高效的点查和范围查询。
http://www.benstopford.com/2015/02/14/log-structured-merge-trees/
https://tikv.org/docs/deep-dive/key-value-engine/b-tree-vs-lsm/
Through comparing various kinds of amplification between B+ tree and Level-based LSM-tree, we can come to a conclusion that Level-based LSM-tree has a better write performance than B+ tree while its read performance is not as good as B+ tree. The main purpose for TiKV to use LSM-tree instead of B-tree as its underlying storage engine is because using cache technology to promote read performance is much easier than promote write performance.

=== 架构
https://github.com/facebook/rocksdb/wiki/RocksDB-Overview#3-high-level-architecture

=== 结构
- MemTable：内存数据结构，具体实现是 SkipList。读写请求优先查 MemTable；写路径上 WAL 落盘后再写入 MemTable。

- Immutable MemTable：当 MemTable 的大小达到设定的阈值时，会变成 Immutable MemTable，只接受读操作，不再接受写操作，后续由后台线程 Flush 到磁盘上。Flush 完成前可能同时存在多个 Immutable MemTable。

- SST Files：Sorted String Table Files，磁盘数据存储文件。分为 Level0 到 LevelN 多层，每一层包含多个 SST 文件，文件内数据有序。Level0 直接由 Immutable MemTable Flush 得到，其它每一层的数据由上一层进行 Compaction 得到。

- Manifest Files：记录 SST 文件在各 Level 的分布、每个 SST 的最大/最小 key、sequence 等元数据；通过增量更新维护版本。

- Current File：CURRENT 文件指向当前有效的 Manifest 文件名。

- Log Files (WAL)：用于防止 MemTable 丢数据的日志文件。

- Block Cache：缓存 SST 中的 Data Block，减少磁盘 I/O，是 LSM 弥补读性能的主要手段。

- Bloom Filter：每个 SST 文件附带 Bloom Filter，读路径上先过滤不可能包含目标 key 的 SST，避免无效打开。

==== Q&A
Q: RocksDB 会丢失数据吗？

A: 取决于 sync 策略与崩溃时机（与 LevelDB 行为一致）。

    ▪ sync=1（每次写都 fsync WAL）：WAL 落盘后再返回，MemTable 未 Flush 的数据也可从 WAL 恢复，不会丢已确认写入。
    ▪ sync=0（默认）：写操作先写 WAL 缓冲区即返回；进程崩溃时，缓冲区中尚未 fsync 的 WAL 可能丢失，对应 MemTable 中的数据也会丢失。
    ▪ 已 Flush 到 SST 的数据：由 Manifest 记录元信息，正常关闭或崩溃恢复后均可读。
    ▪ 磁盘/文件系统故障：与任何存储引擎一样，超出 WAL + 副本保护范围的数据可能损坏或丢失。

=== 参考
https://rocksdb.org/docs/
https://github.com/facebook/rocksdb/wiki
https://github.com/facebook/rocksdb/

== 写
=== 流程
- 1. 写入 WAL（Write-Ahead Log）。根据 `sync` 选项决定是否 fsync；`sync=0` 时写入 OS 缓冲区即返回，`sync=1` 时 fsync 后才返回。
- 2. 写入 MemTable（SkipList）。
- 3. MemTable 的大小达到设定阈值时，转换成 Immutable MemTable；同时分配新的 MemTable 继续接受写。
- 4. Immutable MemTable 由后台线程异步 Flush 到磁盘，成为 Level0 上的一个 SST 文件；当某 WAL 段内所有记录均已 Flush 且 Manifest 已更新，该 WAL 段可被回收。
- 5. 在某些条件下，触发后台线程对 Level0 ~ LevelN 的文件进行 Compaction。

== 读
=== 流程
- 1. 构造 MergeIterator，按序合并：当前 MemTable → 所有 Immutable MemTable（新→旧）→ Level0 ~ LevelN 的 SST。
- 2. 同一 key 取 sequence number 最大的版本；若最新版本为 Tombstone（删除标记），视为 key 不存在。
- 3. 扫描 SST 时，先用 Bloom Filter 过滤不可能包含目标 key 的文件，再查 Block Cache；Cache 未命中则从磁盘读 Data Block 并回填 Cache。
- 4. 所有层均未命中，返回不存在。

== Compaction
=== 目的
- 合并多层 SST，减少文件数量和 key 冗余版本，降低读放大。
- 清理 Tombstone 与过期版本，回收磁盘空间。
- 维持各 Level 的大小比例，避免 Level0 文件过多导致读性能下降。

=== 触发条件
- Level0 文件数超过 `level0_file_num_compaction_trigger`。
- 某 Level 总大小超过该层上限（由 `max_bytes_for_level_base` 等参数决定）。
- 手动调用 `CompactRange` 等 API。

=== 策略
- Leveled Compaction（默认）：Level i 合并到 Level i+1；每层内 key 范围不重叠（Level0 除外），读放大低，写放大较高。
- Universal Compaction：按大小/时间挑选 SST 合并，写放大较低，读放大较高，适合写多读少。
- FIFO Compaction：最老文件优先淘汰，适合 TTL 类场景。

=== 过程（Leveled 为例）
- 1. 从 Level0 选取一个或多个 SST，与 Level1 中 key 范围重叠的 SST 合并。
- 2. 多路归并排序，同一 key 保留最新版本，丢弃旧版本；在无 Snapshot 阻碍的层间合并中清理 Tombstone。
- 3. 输出新的 SST 写入 Level1；若 Level1 超限，继续向下层 Compaction。
- 4. 更新 Manifest，删除被替换的旧 SST 文件。

=== 代价
- 写放大：同一份数据可能被多次重写（Level0 → L1 → ... → Ln）。
- 读放大：Compaction 期间需读取参与合并的多个 SST。
- 空间放大：新旧 SST 短暂共存，Compaction 完成前占用额外磁盘。
- Write Stall：Compaction 跟不上写入速度时，RocksDB 会暂停或限速写入，避免 Level0 失控。
