# CLAUDE.md

- Plain text docs only — no build system, no tests, no package manager.
- Math formulas use `ifdef::env-github[]` / `ifndef::env-github[]` dual-environment syntax (see `/math-formula` skill).
