= reinforcement-learning
:hardbreaks-option:
:revnumber: 0.0.1
:author: orient
:toc:
:toclevels: 5
:sectnums:
:sectnumlevels: 5
:stem: latexmath

== 概念
在强化学习问题中，智能体(agent)在一系列的时间步骤上与环境交互。在每个特定时间点，智能体从环境接收一些观察(observation)，并且必须选择一个动作(action)，然后通过某种机制(有时称为执行器)将其传输回环境，最后智能体从环境中获得奖励(reward)。此后新一轮循环开始，智能体接收后续观察，并选择后续操作，依此类推。

强化学习的一些特征：
（1）强化学习会试错探索，它通过探索环境来获取对环境的理解。
（2）强化学习智能体会从环境里面获得延迟的奖励。
（3）在强化学习的训练过程中，时间非常重要。因为得到的是有时间关联的数据（sequential data），而不是独立同分布的数据。在机器学习中，如果观测数据有非常强的关联，会使得训练非常不稳定。
（4）智能体的动作会影响它随后得到的数据。如果在训练过程中，智能体不能保持稳定，就会使采集到的数据非常糟糕。通过数据来训练智能体，如果数据有问题，整个训练过程就会失败。因此在强化学习里面一个非常重要的问题就是，怎么让智能体的动作一直稳定地提升。

当环境可被完全观察到时，强化学习问题被称为马尔可夫决策过程(markov decision process)。
当状态不依赖于之前的操作时，称该问题为上下文赌博机(contextual bandit problem)。
当没有状态，只有一组最初未知回报的可用动作时，这个问题就是经典的多臂赌博机(multi-armed bandit problem)。
Q: 对于路径规划问题，A*算法与强化学习有哪些区别？

- Action Space
What can your agent do? (discrete choices, continuous values, etc.) 
在给定的环境中，有效动作的集合经常被称为动作空间（action space）。像围棋这样的环境有离散动作空间（discrete action space），在这个动作空间里，智能体的动作数量是有限的。在其他环境，比如在物理世界中控制一个智能体，在这个环境中就有连续动作空间（continuous action space）。
走迷宫机器人如果只有往东、往南、往西、往北这 4 种移动方式，则其动作空间为离散动作空间；
如果机器人可以向 360 度中的任意角度进行移动，则其动作空间为连续动作空间。

- Observation Space
What can your agent see? (images, numbers, structured data, etc.)

- 稠密奖励(Dense Reward)与稀疏奖励(Sparse Reward)
特性    稠密奖励 (Dense Reward)    稀疏奖励 (Sparse Reward)
反馈频率    每一步都有（走一步给一个反馈）    几乎没有（只有成功或失败才给）
典型代表    CartPole（每活一步 +1）    MountainCar（到山顶前全是 -1）
训练难度    极低。走一步修正一步，方向明确极高。    容易迷失在没有反馈的黑夜里

- vs. 深度学习
** 深度学习通过多层神经网络学习数据的特征表示。
** 强化学习通过与环境交互，基于奖励机制学习最优策略。
** 深度学习适用于数据驱动和需要感知的任务，如图像、语音、文本处理。
** 强化学习适用于需要决策和控制的场景，如游戏、机器人、资源管理。

- 发展历史
** 1970年代-1980年代
    强化学习的基础 Richard Sutton(强化学习奠基人)和Christopher Watkins等人提出了自适应动态规划(Adaptive Dynamic Programming)和Q-learning等基本方法。
** 1990年代-2000年代
    Christopher Watkins提出了基于差分学习的Q-learning算法，是现代强化学习的重要里程碑。
    Ronald J. Williams引入了基于梯度的强化学习算法，即策略梯度方法，为后来的进一步发展奠定了基础。
** 2013年以来 深度强化学习时代
    通过深度神经网络的引入，强化学习得到了重大的推动和突破。
    2013年 Deep Q-Network(DQN)是由DeepMind提出的一种结合深度神经网络和Q-learning的算法，首次实现了在Atari游戏中超越人类水平的表现。
    2015年: AlphaGo

- 挑战
如样本效率、探索与利用的平衡、通用性和可解释性等问题。
Q: 强化学习比深度学习简单吗？如果是复杂，复杂在哪里？

- 分类
* 按“是否理解环境”分类
    ** 无模型强化学习 (Model-free RL)：
        *** 智能体不尝试理解环境运行的物理规律，而是直接通过“试错”获取经验。
        *** 特点：通用性强，但成本高、数据利用率低（需要大量练习）。
    ** 基于模型的强化学习 (Model-based RL)：
        *** 智能体先学习一个环境模型（预测给定状态和动作后，下一个状态和奖励是什么），然后在模型中进行虚拟演练。
        *** 特点：成本低一些，但泛化性能受限，即模型偏差 Model Bias。
    ** 免模型（Model-Free）例子：
    *** OpenAI Five（Dota 2 战队）： AI 每天和自己对弈相当于 180 年的游戏时长。它内部没有任何“物理引擎”去计算每个英雄的弹道轨迹或移动公式，它只是纯粹通过“点击、死掉、复活、再试”的试错循环，最终摸索出了完美的团战拉扯战术。
    *** AlphaGo / AlphaZero（围棋/象棋）： 它不需要去建模人类对手的心理或者精密的数学公式。它通过和自己下几千万盘棋，在海量的棋局样本中直接调整自己的下棋策略。
    *** 经典算法： DQN、PPO、SAC。
    ** 有模型（Model-Based）例子：
    *** MuZero（AlphaGo的进化版）： 与 AlphaZero 不同，MuZero 会在脑子里自己去学习和建立一套游戏规则的模型。在每走一步棋之前，它会在内部模型中进行“想象”和前向预测（“如果我走这里，接下来的棋盘会变成什么样？我会得多少分？”），在内部推演满意后，才在真实棋盘上落子。
    *** 世界模型（World Models / Dreamer系列）： AI 先通过观察游戏画面，在自己的神经网络里构建了一个“虚拟的连连看或赛车游戏”。然后，AI 完全在这个由神经网络“脑补”出来的虚拟梦境（Dream）中训练自己的策略，训练好之后再直接去玩真实游戏。
    *** 算法： World Models、Dreamer V1/V2/V3、MBPO。

* 按“学习目标”分类（Model-free 下的主流划分）
    ** 基于价值 (Value-based)
        *** 不直接学习动作，而是学习“在某个状态下，做某个动作未来能拿多少分”。即回答“在这个状态下，做哪个动作未来收益最高？”。
        *** 核心理念：学习一个动作价值函数 Q(s, a)，“在状态s下采取动作a，未来能获得的累积奖励期望是多少”。执行时，只需选择Q值最大的动作。
        *** 代表算法：
            **** Q-Learning：最经典的基础算法，维护一个 Q 表。
            **** DQN (Deep Q-Network)：用深度神经网络代替 Q 表，开启了深度强化学习时代。
    ** 基于策略 (Policy-based)
        *** 直接学习“在某个状态下，采取各动作的概率分布”。即回答“在这个状态下，应该选择什么动作？”。
        *** 核心理念：直接优化策略函数 π(a|s)，通过奖励的好坏来调整动作出现的概率。
        *** 代表算法：
            **** Policy Gradient (策略梯度)：简单直接。
            **** REINFORCE (蒙特卡洛策略梯度)：最基本的策略梯度算法。
            它完整地执行一个回合（episode），然后根据整个回合的总回报来增加“好”动作的概率，降低“坏”动作的概率。缺点是方差大，学习慢。
    ** 演员-评论家 (Actor-Critic)
        *** 结合了上述两者的优点：Actor 负责出招（策略），Critic 负责给这一招打分（价值评估）。
        *** 核心理念：Critic 提供更准确的反馈，帮助 Actor 更稳健地学习。
        *** 代表算法：
            **** A3C / A2C：异步/同步优势演员-评论家。
            **** PPO (Proximal Policy Optimization)：当前工业界最通用、最稳健的算法（OpenAI 默认算法，也是 RLHF 的核心）。
            **** SAC (Soft Actor-Critic)：兼顾高奖励与动作多样性，在机器人控制领域表现优异。

* 按“经验利用方式”分类在线学习
    ** 在线学习(On-policy)：
        *** 必须用当前策略采集到的数据进行更新。数据用完即弃，学习较慢但较稳定。
        *** 例子：SARSA, PPO。
    ** 离线学习 (Off-policy)：
        *** 可以利用旧策略产生的数据，甚至别人的数据（经验回放）。数据利用率极高。
        *** 例子：Q-Learning, DQN, DDPG。

- 按“对马尔可夫性质的依赖程度”来分类
分类	        依赖程度	      假设条件	              输入信息	         典型算法	        样本效率
MDP RL	       高（强依赖）	   严格满足MDP	           仅当前状态Sₜ        DQN, PPO, Q-learning	最高
POMDP RL	   中（条件依赖）  不满足，但可近似为MDP	最近k步历史序列	     DRQN, RDPG	         中等
非马尔可夫方法	低/无（不依赖）	不作任何假设	         整条轨迹或策略参数	  进化策略, 遗传算法	最低

* https://spinningup.openai.com/en/latest/spinningup/rl_intro2.html#a-taxonomy-of-rl-algorithms

- 策略
策略是智能体的动作模型，它决定了智能体的动作。
它其实是一个函数，用于把输入的状态变成动作。
策略可分为两种：随机性策略（stochastic policy）和确定性策略（deterministic policy）。

- 价值
价值函数的值是对未来奖励的预测，用它来评估状态的好坏。
价值函数里面有一个折扣因子（discount factor），希望在尽可能短的时间里面得到尽可能多的奖励。

- 策略与价值
基于价值的智能体（value-based agent）显式地学习价值函数，隐式地学习它的策略。策略是其从学到的价值函数里面推算出来的。
基于策略的智能体（policy-based agent）直接学习策略，给它一个状态，它就会输出对应动作的概率。基于策略的智能体并没有学习价值函数。
把基于价值的智能体和基于策略的智能体结合起来就有了演员-评论员智能体（actor-critic agent）。

- 探索-利用窘境（Exploration-Exploitation Dilemma）
是强化学习中在尝试未知行动以获取新知识（探索）与坚持已知最佳行动以最大化即时奖励（利用）之间取得平衡的核心挑战。

- RLHF（Reinforcement Learning from Human Feedback，基于人类反馈的强化学习）
RLHF一种将人类偏好作为奖励信号，来训练或微调人工智能模型的方法。它尤其擅长让语言模型的行为更符合人类的期望、价值观和复杂指令。

- DPO（Direct Preference Optimization，直接偏好优化）
DPO和RLHF是目前大模型对齐的两大主流算法。它们的目标一致，但实现路径有本质区别，各有优劣。
维度	DPO (直接偏好优化)	RLHF (基于人类反馈的强化学习)
核心原理	直接学习：通过对比学习，直接根据“答案A比B好”的反馈来更新模型。	间接学习：先训练一个“奖励模型”来模仿人类打分，再用这个模型通过强化学习去训练主模型。
实现复杂度	低。流程简单，去除了奖励模型和RL环节，稳定性高，更易于实现和调试。	高。需要多阶段训练，涉及策略模型、奖励模型等多个组件，调参复杂，易出现“奖励黑客”等不稳定现象。
训练效率	高。显著减少了计算开销和训练时间，据估计可节省50-60% 的成本。	低。训练流程繁琐，显存和算力需求比DPO高30%-50%，周期长2-3倍。
核心优势	简单、高效、稳定。不削弱模型的基础能力（如事实性知识或逻辑推理），甚至在控制回答语气等任务上效果更优。	精准、复杂、可控。能精细化地捕捉和权衡多维度（如帮助性、无害性）的人类偏好，擅长处理非常复杂的对齐目标和场景。
主要局限性	对复杂偏好的建模能力有限。标准DPO假设存在一个单一的偏好目标，难以处理人类偏好中的多维性和冲突。	流程繁琐，资源消耗巨大。极高的计算成本和训练不稳定性是其应用的主要门槛

Q：强化学习中所谓的损失函数与深度学习中的损失函数有什么区别呢？
深度学习：Loss 是目标——要让“预测值”拟合“真实值”。
强化学习：Loss 是手段——设计这个Loss，是为了给策略梯度一个“方向指示器”，以便在不可微的环境中，通过采样近似让“动作分布”向高奖励区域偏移；真正的目标（总奖励期望）在损失函数之外，由环境来评判。

== 马尔可夫决策过程(Markov Decision Process, MDP)
=== 概念
https://en.wikipedia.org/wiki/Markov_decision_process

    State
    Action
    State transition, state transition probability p(s′∣s,a)
    Reward, reward probability p(r∣s,a)
    Trajectory, episode, return, discounted return
    Markov decision process

- 马尔可夫性质(Markov property)
马尔可夫性质也可以描述为给定当前状态时，将来的状态与过去状态是条件独立的。如果某一个过程满足马尔可夫性质，那么未来的转移与过去的是独立的，它只取决于现在。
设随机过程的状态序列为：S₁, S₂, ..., Sₜ, Sₜ₊₁, ...
若该过程满足马尔可夫性质，则对于任意状态 s 和任意时刻 t，有：
P( Sₜ₊₁ = s | S₁, S₂, ..., Sₜ ) = P( Sₜ₊₁ = s | Sₜ )
两者完全相等，说明一旦“现在”被确定，任何“过去”的信息对于预测“未来”都是多余的，这就是条件独立（Conditionally Independent）。
不是无条件独立：过去的历史通过连锁反应，决定了现在处于什么位置，因此过去和未来当然有间接关系。
是条件独立：只有当“给定当前状态”这个条件成立时，过去和未来才被隔断。

- 马尔可夫性质的通俗理解
在跟AI下国际象棋，棋盘上目前的残局就是“当前状态”。这个残局是怎么形成的（是对方第一步走错了，是刚才精妙布局造成的，即“过去”）完全不影响接下来的胜率预测。AI只需要看一眼眼前的棋盘布局（现在），就能做出完美的下一步决策（未来）。因此，马尔可夫性质也被形象地称为“无记忆性”（Memoryless）。
过去的信息已经被“压碎”并全部包含在了“当前状态”里。

- Q: 不满足马尔可夫性质怎么办？
非马尔可夫（原始）：
P(明天健康 | 今天报告) ≠ P(明天健康 | 今天报告 + 过去5年病史)
马尔可夫化（扩充状态）：
定义 S'ₜ = (今天的报告, 过去5年病史摘要)，则
P( S'ₜ₊₁ | S'₁, ..., S'ₜ ) = P( S'ₜ₊₁ | S'ₜ )
原问题：状态不够“全”，不满足马尔可夫性。
解决方案：把历史关键信息编码进当前状态，让状态“足够”描述过去。
结果：一个高维但满足马尔可夫性的状态空间，使得强化学习、隐马尔可夫模型、蒙特卡洛方法等 AI 工具可以正常使用。
状态增强（State Augmentation）：
当系统不满足马尔可夫性质时，通过扩充状态空间，将历史信息编码进当前状态，使得扩充后的状态序列满足马尔可夫性质。

- 马尔可夫过程（MP）
纯随机的状态转移链条。
组成：只有状态（State）和状态转移概率（Transition Probability）

- 马尔可夫奖励过程（MRP）
在 MP 的基础上，给每一次状态转移或进入某个状态时，加上一个即时分数（奖励）。
组成： 状态 + 转移概率 + 奖励（Reward）

- 马尔可夫决策过程（MDP）
在 MRP 的基础上，引入了动作（Action）和策略（Policy）。
组成： 状态 + 转移概率 + 奖励 + 动作（Action）

- Q: 按“对马尔可夫性质的依赖程度”来划分，强化学习如何分类？举例。
象棋和围棋属于标准 MDP。
马里奥、斗地主属于 POMDP RL（部分可观测马尔可夫决策过程）。
为什么马里奥原生属于 POMDP（物理运动的 POMDP）？
单张游戏画面（静态状态）无法满足马里奥的马尔可夫性质：核心问题：单张画面丢失了“速度”和“方向”信息。如果只看马里奥在某一帧的截图，无法知道他是正在向右高速奔跑、还是刚刚起跳、还是正在向下坠落。输入信息：需要最近 k 步历史序列（帧堆叠 Frame Stacking）。为了让游戏满足马尔可夫性，经典的强化学习论文（如 DeepMind 的 DQN 论文）引入了帧堆叠（Frame Stacking）技术。算法不只输入当前帧，而是连续输入最近的 4 帧画面（即 k=4）。通过连续的 4 帧，网络就能计算出马里奥的速度、加速度和移动方向。
斗地主（不完美信息的 POMDP）：
隐藏信息是“对手的手牌”：你不知道底牌（叫牌前）和另外两家手里的具体牌型。历史的局限性：哪怕记录了从开局到现在的所有出牌历史（整条轨迹），由于洗牌和发牌的随机性，在第 10 步时，依然无法百分之百确定对手手里剩下的是什么牌。
斗地主的历史序列只能用来“推算概率”（比如对方有炸弹的概率是 70%），而无法像马里奥那样推导出确定、完整的状态。

- Q: 马尔可夫过程是什么？马尔可夫决策过程又是什么？其中马尔可夫最重要的性质是什么呢？
马尔可夫过程是一个二元组 ⟨𝑆, 𝑃⟩，𝑆 为状态集合，𝑃 为状态转移函数；
马尔可夫决策过程是一个五元组 ⟨𝑆, 𝑃, 𝐴, 𝑅, 𝛾⟩，其中 𝑅 表示从 𝑆 到 𝑆′ 能够获得的奖励期望，𝛾 为折扣因子，𝐴 为动作集合；
马尔可夫最重要的性质是下一个状态只与当前状态有关，与之前的状态无关，也就是𝑝(𝑠ₜ₊₁ | 𝑠ₜ) = 𝑝(𝑠ₜ₊₁ | 𝑠₁, 𝑠₂, … , 𝑠ₜ)

- Q: 一般怎么求解马尔可夫决策过程？
求解马尔可夫决策过程时，可以直接求解贝尔曼方程或动态规划方程：
𝑉(𝑠) = 𝑅(𝑠) + 𝛾 ∑ₛ′∈𝑺 𝑝(𝑠′|𝑠) 𝑉(𝑠′)
特别地，其矩阵形式为 𝑽 = 𝑹 + 𝛾 𝑷 𝑽。
贝尔曼方程很难求解且计算复杂度较高，因此可以使用动态规划、蒙特卡洛以及时序差分等方法求解。

- Q: 如果数据流不具备马尔可夫性质怎么办？
扩充状态（把最近 k 步历史拼进当前状态），或用 LSTM/RNN 记忆历史，或改用 POMDP 框架处理部分可观测性。

- Q: 基于状态价值函数的贝尔曼方程与基于动作价值函数的贝尔曼方程

=== Key elements of MDP
==== Sets
* State: the set of states *S*
* Action: the set of actions *A(s)* is associated for state *s ∈ S*.
* Reward: the set of rewards *R(s, a)*.

==== Probability distribution
* State transition probability: at state *s*, taking action *a*, the probability to transit to state *s'* is *p(s' | s, a)*
* Reward probability: at state *s*, taking action *a*, the probability to get reward *r* is *p(r | s, a)*

==== Policy
At state *s*, the probability to choose action *a* is *π(a | s)*

==== Markov property: memoryless property
ifndef::env-github[]
\[p(s_{t+1} | a_{t+1}, s_t, \ldots, a_1, s_0) = p(s_{t+1} | a_{t+1}, s_t)\]
endif::env-github[]
ifdef::env-github[]
```math
p(s_{t+1} | a_{t+1}, s_t, \ldots, a_1, s_0) = p(s_{t+1} | a_{t+1}, s_t)
```
endif::env-github[]

ifndef::env-github[]
\[p(r_{t+1} | a_{t+1}, s_t, \ldots, a_1, s_0) = p(r_{t+1} | a_{t+1}, s_t)\]
endif::env-github[]
ifdef::env-github[]
```math
p(r_{t+1} | a_{t+1}, s_t, \ldots, a_1, s_0) = p(r_{t+1} | a_{t+1}, s_t)
```
endif::env-github[]

=== 马尔可夫性质
具有马尔可夫性质，即未来状态只依赖于当前状态和动作，与过去状态无关:
ifndef::env-github[]
stem:[P(s_{t+1} | s_t, a_t, s_{t-1}, a_{t-1}, \ldots) = P(s_{t+1} | s_t, a_t)]
endif::[]
ifdef::env-github[]
```math
P(s_{t+1} | s_t, a_t, s_{t-1}, a_{t-1}, \ldots) = P(s_{t+1} | s_t, a_t)
```
endif::[]

=== 马尔科夫奖励过程
马尔可夫奖励过程（Markov reward process, MRP）是马尔可夫链加上奖励函数。在马尔可夫奖励过程中，状态转移矩阵和状态都与马尔可夫链一样，只是多了奖励函数（reward function）。奖励函数 R 是一个期望，表示当到达某一个状态的时候，可以获得多大的奖励。另外定义了折扣因子 γ 。如果状态数是有限的，那么 R 可以是一个向量。

=== 参考
https://en.d2l.ai/chapter_reinforcement-learning/mdp.html

== 贝尔曼公式/贝尔曼方程(Bellman Equation)
=== 概念
贝尔曼方程定义了当前状态与未来状态之间的关系。未来奖励的折扣总和加上即时奖励，就组成了贝尔曼方程。

贝尔曼方程就是当前状态与未来状态的迭代关系，表示当前状态的价值函数可以通过下个状态的价值函数来计算。贝尔曼方程因其提出者、动态规划创始人理查德⋅贝尔曼（Richard Bellman）而得名，也叫作“动态规划方程”。

=== return(回报)
What is return? The (discounted) sum of the rewards obtained along a trajectory.

假设一个确定性环境：状态 A → 状态 B → 终止，A 的奖励 0，B 的奖励 10，γ=0.9。
在 B 点：GB =10
在 A 点：GA=0+0.9×GB=9
用贝尔曼形式看：V(A)=0+0.9V(B)=9
Return 不是把 0 和 10 简单相加（那会是 10），而是把 B 的价值“折现”后传回给 A。贝尔曼方程做的正是——把远处的价值通过折扣一层层传回来。

Q: 为什么有γ：折扣因子？
γ 有三个不可替代的作用：
数学上：防止无穷奖励导致 Return 爆炸，保证数值有限；
直觉上：反映“近期的奖励比远期的更可靠、更重要”的常识；
算法上：保证贝尔曼方程迭代求解时必然收敛。

Q: 如何计算return?
return(回报) Gₜ 是从时刻 t 起沿轨迹累积的（折现）奖励之和。

定义式（求和）：把未来每步奖励乘以折扣因子 γᵏ 后相加，γ ∈ [0, 1]：
ifndef::env-github[]
\[G_t = \sum_{k=0}^{\infty} \gamma^k R_{t+k+1} = R_{t+1} + \gamma R_{t+2} + \gamma^2 R_{t+3} + \cdots\]
endif::env-github[]
ifdef::env-github[]
$$G_t = \sum_{k=0}^{\infty} \gamma^k R_{t+k+1} = R_{t+1} + \gamma R_{t+2} + \gamma^2 R_{t+3} + \cdots$$
endif::env-github[]
若回合在终止时刻 T 结束，求和上限改为 T − t − 1。

递推式（贝尔曼形式，最常用）：当前回报 = 即时奖励 + γ × 下一时刻回报：
ifndef::env-github[]
\[G_t = R_{t+1} + \gamma G_{t+1}\]
endif::env-github[]
ifdef::env-github[]
$$G_t = R_{t+1} + \gamma G_{t+1}$$
endif::env-github[]
这是自举（bootstrap）的核心，也是贝尔曼方程的来源。上面的例子中：G_B = 10，G_A = 0 + 0.9 × G_B = 9，正是用递推式从后往前算的结果。实操中很少按定义式展开无穷求和，而是用递推式反向计算（蒙特卡洛），或用贝尔曼方程迭代逼近。

=== state value
`G_t` 的期望（或称期望值、均值）被定义为*状态价值函数*，或简称为*状态价值*：
ifndef::env-github[]
\[v_\pi(s) = \mathbb{E}[G_t \mid S_t = s]\]
endif::env-github[]
ifdef::env-github[]
$$v_\pi(s) = \mathbb{E}[G_t \mid S_t = s]$$
endif::env-github[]

* 它是 `s` 的函数。它是一个条件期望，条件是状态从 `s` 开始。
* 它基于策略 `π`。对于不同的策略，状态价值可能不同。
* 它表示一个状态的“价值”。比较两个策略不能只看单个状态：若对所有状态 `s` 都有 `v_π₁(s) ≥ v_π₂(s)`（且至少一个状态严格大于），则称 `π₁` 优于 `π₂`；实践中常以初始状态价值 `v_π(s₀)` 或按状态分布加权的均值来衡量。
问：回报（return）与状态价值（state value）之间的关系是什么？
答：状态价值是从一个状态出发所能获得的所有可能回报的均值。
如果所有因素——`π(a|s)`、`p(r|s,a)`、`p(s'|s,a)`——都是确定性的，
那么状态价值与回报相同。

=== 推导(Derivation)

    贝尔曼方程的推导过程如下：
        V(s) = E[ Gₜ | sₜ = s ]
            = E[ rₜ₊₁ + γ·rₜ₊₂ + γ²·rₜ₊₃ + … | sₜ = s ]
            = E[ rₜ₊₁ | sₜ = s ] + γ·E[ rₜ₊₂ + γ·rₜ₊₃ + γ²·rₜ₊₄ + … | sₜ = s ]
            = R(s) + γ·E[ Gₜ₊₁ | sₜ = s ]
            = R(s) + γ·[ V(sₜ₊₁) | sₜ = s ]
            = R(s) + γ·Σ p(s′|s)·V(s′)

== 贝尔曼最优公式(Bellman Optimality Equation)

== 值迭代(Value Iteration)与策略迭代(Policy Iteration)
=== 概念
值迭代(Value Iteration)是强化学习中的一种经典算法，用于求解马尔可夫决策过程(MDP)的最优策略。它通过迭代更新值函数来逼近最优值函数，并最终导出最优策略。

=== 参考
https://en.d2l.ai/chapter_reinforcement-learning/value-iter.html

== 蒙特卡洛方法

== 随机近似与随机梯度下降

== 时序差分方法
=== Q-Learning
==== 概念
- 概念
Q: Quality（质量）
Q(s, a) 就是：在状态 s (State) 下，采取动作 a (Action) 的质量分数。
Q-learning 是一种无模型的强化学习算法，用于在给定的马尔可夫决策过程(MDP)中找到最优的动作选择策略。它是一种离策略(off-policy)算法，意味着它可以独立于智能体的行为来学习最优策略。

- 优点

    无模型:
        不需要环境模型。
    保证收敛:
        在一定条件下，Q-learning 保证收敛到最优策略。

- 局限性

    可扩展性:
        对于具有许多状态和动作的问题，Q 表可能变得非常大。
    收敛速度慢:
        学习可能很慢，特别是在大型或复杂的环境中。

- 扩展与变体

    深度 Q 学习(DQN):
        将 Q-learning 与深度神经网络结合，以处理高维状态空间。
    双 Q 学习:
        通过使用两个 Q 函数减少 Q 值的高估。
    SARSA:
        Q-learning 的一种在策略(on-policy)变体，基于实际采取的动作更新 Q 值。

==== DQN(深度Q网络，Deep Q-Network)
===== 核心思想
用一个神经网络去近似「最优动作价值函数 Q*(s, a)」（即在状态 s 下采取动作 a 后，按最优策略走下去能拿到的累计折扣回报），然后用 Q-learning 的更新规则训练网络。

===== 变种
====== Double DQN
解耦「动作选择」与「Q 值评估」，缓解普通 DQN 中的过估计(Overestimation)偏差。
论文：van Hasselt, Guez & Silver, 2015（Deep Reinforcement Learning with Double Q-learning）。

动机 - 过估计从何而来::
普通 DQN 的 TD target 为：

ifndef::env-github[]
\[y_{\text{DQN}} = r + \gamma \cdot \max_{a'} \hat{Q}(s', a'; \theta^-)\]
endif::env-github[]
ifdef::env-github[]
```math
y_{\text{DQN}} = r + \gamma \cdot \max_{a'} \hat{Q}(s', a'; \theta^-)
```
endif::env-github[]

这里目标网络 θ⁻ 同时干两件事：先在内层用 max 挑出 Q 值最大的动作，再用同一组参数 θ⁻ 给这个动作打分。「选择」与「评估」耦合在同一网络里，估计误差于是自我放大。

- 过估计(Overestimation)
指的是数值上的系统性正偏差。即算法计算出的动作价值（Q值）整体高于其真实能获得的累积奖励。

- 例子
一个动作实际只能得 10 分，但 Q 网络估算出它能得 100 分。

- 为何 max 会引入正偏差
Q 值估计本身带噪声。对带噪估计取 max，倾向于挑中「恰好被高估」的动作；高估的动作被选中后，它（被高估）的值又被原样塞回 target，下一轮继续被强化。数学上即：

ifndef::env-github[]
\[\mathbb{E}\!\left[\max_{a'} \hat{Q}(s',a')\right] \;\geq\; \max_{a'} \mathbb{E}\!\left[\hat{Q}(s',a')\right]\]
endif::env-github[]
ifdef::env-github[]
```math
\mathbb{E}\!\left[\max_{a'} \hat{Q}(s',a')\right] \;\geq\; \max_{a'} \mathbb{E}\!\left[\hat{Q}(s',a')\right]
```
endif::env-github[]

「估计的 max」系统性不低于「真值的 max」，这个差值就是过估计偏差的来源。
换个说法：若 Q̂ 是无偏估计，则 E[Q̂] = Q*，上式右边即真实的 max_{a'} Q*(s',a')，于是 target 的第二项被整体向上抬高了。

解法 - 用两个网络分别负责选择与评估::
关键想法：让「选择」与「评估」使用两套参数不同的网络，使同一处高估难以同时对齐。

① 动作选择(Action Selection)：用在线主网络 θ 在下一状态 s' 中选出 Q 值最大的动作：

ifndef::env-github[]
\[a^* = \arg\max_{a'} Q(s', a'; \theta)\]
endif::env-github[]
ifdef::env-github[]
```math
a^* = \arg\max_{a'} Q(s', a'; \theta)
```
endif::env-github[]

② Q 值评估(Action Evaluation)：再用目标网络 θ⁻ 评估这个被选中的动作 a*，得到 target：

ifndef::env-github[]
\[y_{\text{DDQN}} = r + \gamma \cdot \hat{Q}(s', a^*; \theta^-)\]
endif::env-github[]
ifdef::env-github[]
```math
y_{\text{DDQN}} = r + \gamma \cdot \hat{Q}(s', a^*; \theta^-)
```
endif::env-github[]

把两步合写，即 Double DQN 的 target（选择用 θ、评估用 θ⁻）：

ifndef::env-github[]
\[y_{\text{DDQN}} = r + \gamma \cdot \hat{Q}\!\left(s',\, \arg\max_{a'} Q(s', a'; \theta);\, \theta^-\right)\]
endif::env-github[]
ifdef::env-github[]
```math
y_{\text{DDQN}} = r + \gamma \cdot \hat{Q}\!\left(s',\, \arg\max_{a'} Q(s', a'; \theta);\, \theta^-\right)
```
endif::env-github[]

- DQN vs Double DQN 公式对比

| | 动作选择 | Q 值评估 | target |
|---|---|---|---|
| DQN | θ⁻ | θ⁻（同一网络） | r + γ·max_{a'} Q̂(s',a';θ⁻) |
| Double DQN | θ（主网络） | θ⁻（目标网络） | r + γ·Q̂(s', argmax_{a'}Q(s',a';θ); θ⁻) |

Double DQN 只动了「用谁选动作」这一处：选择改用主网络 θ，评估仍用目标网络 θ⁻。

- 直觉解释
假设动作 a₁ 的真实值是 10，但因为噪声，主网络 θ 把它估成了 100；而目标网络 θ⁻ 对 a₁ 的估计是独立的、可能只有 12。
- DQN：选择和评估都用 θ⁻，若 θ⁻ 恰好也高估了 a₁，target 就被钉在 100 附近，高估被固化。
- Double DQN：用 θ 选出 a₁，但用 θ⁻ 评估，target 落到 12 附近，高估被「拉回」接近真实值。
两套参数相互独立，θ 的高估点与 θ⁻ 的高估点很难恰好对齐，于是系统性正偏差被抵消掉一大部分。

算法步骤（在 DQN 每步循环基础上替换第⑤步）::
初始化: 主网络 Q(s,a;θ)、目标网络 Q̂(s,a;θ⁻) ← θ、回放池 D（均与 DQN 相同）。

每步循环:

① 观察状态 s，用 ε-贪婪策略选动作 a。
② 执行 a，观察奖励 r 与下一状态 s'。
③ 将转移 (s, a, r, s') 存入回放池 D。
④ 从 D 随机采样一批转移 (s, a, r, s')。
⑤ 计算 TD target（与 DQN 的唯一区别在此——选择用 θ，评估用 θ⁻；若 s' 为终止状态则 y = r）:

ifndef::env-github[]
\[y = r + \gamma \cdot \hat{Q}\!\left(s',\, \arg\max_{a'} Q(s', a'; \theta);\, \theta^-\right)\]
endif::env-github[]
ifdef::env-github[]
```math
y = r + \gamma \cdot \hat{Q}\!\left(s',\, \arg\max_{a'} Q(s', a'; \theta);\, \theta^-\right)
```
endif::env-github[]

⑥ 最小化损失函数（与 DQN 完全相同），对主网络做一步梯度下降:

ifndef::env-github[]
\[L(\theta) = \mathbb{E}_{(s,a,r,s') \sim \mathcal{D}}\left[\left(y - Q(s, a; \theta)\right)^2\right]\]
endif::env-github[]
ifdef::env-github[]
```math
L(\theta) = \mathbb{E}_{(s,a,r,s') \sim \mathcal{D}}\left[\left(y - Q(s, a; \theta)\right)^2\right]
```
endif::env-github[]

⑦ 每 C 步同步目标网络一次: θ⁻ ← θ。

- 小结
Double DQN 几乎零成本：不增加网络数量、不改变结构、不动损失函数，只把 target 里「选动作」的网络从 θ⁻ 换成 θ。却能有效降低过估计、提升值函数估计的稳定性与最终策略质量。

https://pytorch.org/tutorials/intermediate/mario_rl_tutorial.html
https://arxiv.org/pdf/1509.06461

====== Dueling DQN
分离状态价值和优势函数

====== Prioritized Replay
优先回放高TD误差的经验，加速学习

===== 核心元素
- 马尔可夫决策过程(MDP)

    状态(S): 表示智能体可能处于的不同情况。
    动作(A): 智能体可以采取的可能行动。
    奖励(R): 采取动作后从环境获得的即时反馈。
    转移概率(P): 在给定动作下从一个状态转移到另一个状态的概率。

- Q值

    Q(s,a) 表示在状态 s 下采取动作 a 并随后遵循最优策略的预期累积奖励。

- 贝尔曼方程

===== 核心技巧
朴素地把神经网络塞进 Q-learning 会发散，DQN 靠三个工程技巧稳定训练：

经验回放 (Experience Replay):: 把每步转移 (s, a, r, s') 存进回放池 D，训练时从中随机采样小批量。一举两得：打破相邻样本的时间相关性（近似 i.i.d.），并重复利用旧数据、提高样本效率。

目标网络 (Target Network):: 另外维护参数为 θ⁻ 的目标网络 Q̂ 用来计算 TD target。主网络 θ 每步更新，目标网络 θ⁻ 每 C 步同步一次（θ⁻ ← θ），使 target 在短期内保持稳定，避免「追着自己跑」的正反馈震荡。

ε-贪婪探索 (ε-greedy):: 以概率 ε 随机探索、以概率 1−ε 选 Q 值最大的动作；ε 随训练逐步衰减，从「多探索」过渡到「多利用」。

===== 算法步骤
初始化: 主网络 Q(s,a;θ)（随机权重）、目标网络 Q̂(s,a;θ⁻) ← θ、回放池 D。

每步循环:

① 观察状态 s，用 ε-贪婪策略选动作 a。
② 执行 a，观察奖励 r 与下一状态 s'。
③ 将转移 (s, a, r, s') 存入回放池 D。
④ 从 D 随机采样一批转移 (s, a, r, s')。
⑤ 计算 TD target（用目标网络 Q̂；若 s' 为终止状态则不加未来项，即 y = r）:

ifndef::env-github[]
\[y = r + \gamma \cdot \max_{a'} \hat{Q}(s', a'; \theta^-)\]
endif::env-github[]
ifdef::env-github[]
$$y = r + \gamma \cdot \max_{a'} \hat{Q}(s', a'; \theta^-)$$
endif::env-github[]

⑥ 最小化损失函数，对主网络做一步梯度下降:

ifndef::env-github[]
\[L(\theta) = \mathbb{E}_{(s,a,r,s') \sim \mathcal{D}}\left[\left(y - Q(s, a; \theta)\right)^2\right]\]
endif::env-github[]
ifdef::env-github[]
$$L(\theta) = \mathbb{E}_{(s,a,r,s') \sim \mathcal{D}}\left[\left(y - Q(s, a; \theta)\right)^2\right]$$
endif::env-github[]

⑦ 每 C 步同步目标网络一次: θ⁻ ← θ。

==== 参考
https://en.d2l.ai/chapter_reinforcement-learning/qlearning.html
https://arxiv.org/pdf/1509.06461

== 值函数近似

== 策略梯度方法

== Actor-Critic方法

== 动态规划法

== project
- env
https://cloud.tencent.com/developer/article/1788317

- mario
https://github.com/vietnh1009/Super-mario-bros-PPO-pytorch
https://pytorch.org/tutorials/intermediate/mario_rl_tutorial.html

- dota2
https://github.com/bilibili/LastOrder-Dota2

- 斗地主
https://github.com/kwai/DouZero

- Pong from Pixels
https://karpathy.github.io/2016/05/31/rl/

- gymnasium
https://gymnasium.farama.org/introduction/basic_usage/#your-first-rl-program
https://gymnasium.farama.org/introduction/train_agent/

== 参考
- 李宏毅
https://datawhalechina.github.io/easy-rl/

- 赵世钰
《强化学习的数学原理》
https://www.icourse163.org/course/XHUN-1470436188?tid=1475125444
https://www.bilibili.com/video/BV1sd4y167NS
https://github.com/MathFoundationRL/Book-Mathematical-Foundation-of-Reinforcement-Learning

- 王树森
https://www.bilibili.com/video/BV1hhbSzjEi1/
《深度强化学习》
https://github.com/wangshusen/DRL

-《动手强化学习》
https://hrl.boyuai.com/chapter/intro

- 《Reinforcement Learning: An Introduction》2nd Edition
- 《Deep Reinforcement Learning Hands-On》

- UC Berkeley
CS285
https://rail.eecs.berkeley.edu/deeprlcourse/

- 斯坦福大学《CS234: 强化学习》

- UCLA
Reinforcement Learning of Large Language Models
