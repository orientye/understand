= reinforcement-learning
:hardbreaks-option:
:revnumber: 0.0.1
:author: orient
:toc:
:toclevels: 5
:sectnums:
:sectnumlevels: 5
:stem: latexmath

== 概念
在强化学习问题中，智能体(agent)在一系列的时间步骤上与环境交互。在每个特定时间点，智能体从环境接收一些观察(observation)，并且必须选择一个动作(action)，然后通过某种机制(有时称为执行器)将其传输回环境，最后智能体从环境中获得奖励(reward)。此后新一轮循环开始，智能体接收后续观察，并选择后续操作，依此类推。

强化学习的一些特征：
（1）强化学习会试错探索，它通过探索环境来获取对环境的理解。
（2）强化学习智能体会从环境里面获得延迟的奖励。
（3）在强化学习的训练过程中，时间非常重要。因为得到的是有时间关联的数据（sequential data），而不是独立同分布的数据。在机器学习中，如果观测数据有非常强的关联，会使得训练非常不稳定。
（4）智能体的动作会影响它随后得到的数据。如果在训练过程中，智能体不能保持稳定，就会使采集到的数据非常糟糕。通过数据来训练智能体，如果数据有问题，整个训练过程就会失败。因此在强化学习里面一个非常重要的问题就是，怎么让智能体的动作一直稳定地提升。

当环境可被完全观察到时，强化学习问题被称为马尔可夫决策过程(markov decision process)。
当状态不依赖于之前的操作时，称该问题为上下文赌博机(contextual bandit problem)。
当没有状态，只有一组最初未知回报的可用动作时，这个问题就是经典的多臂赌博机(multi-armed bandit problem)。
Q: 对于路径规划问题，A*算法与强化学习有哪些区别？

- Action Space
What can your agent do? (discrete choices, continuous values, etc.) 

- Observation Space
What can your agent see? (images, numbers, structured data, etc.)

- 稠密奖励(Dense Reward)与稀疏奖励(Sparse Reward)
特性    稠密奖励 (Dense Reward)    稀疏奖励 (Sparse Reward)
反馈频率    每一步都有（走一步给一个反馈）    几乎没有（只有成功或失败才给）
典型代表    CartPole（每活一步 +1）    MountainCar（到山顶前全是 -1）
训练难度    极低。走一步修正一步，方向明确极高。    容易迷失在没有反馈的黑夜里

- vs. 深度学习
** 深度学习通过多层神经网络学习数据的特征表示。
** 强化学习通过与环境交互，基于奖励机制学习最优策略。
** 深度学习适用于数据驱动和需要感知的任务，如图像、语音、文本处理。
** 强化学习适用于需要决策和控制的场景，如游戏、机器人、资源管理。

- 发展历史
** 1970年代-1980年代
    强化学习的基础 Richard Sutton(强化学习奠基人)和Christopher Watkins等人提出了自适应动态规划(Adaptive Dynamic Programming)和Q-learning等基本方法。
** 1990年代-2000年代
    Christopher Watkins提出了基于差分学习的Q-learning算法，是现代强化学习的重要里程碑。
    Ronald J. Williams引入了基于梯度的强化学习算法，即策略梯度方法，为后来的进一步发展奠定了基础。
** 2013年以来 深度强化学习时代
    通过深度神经网络的引入，强化学习得到了重大的推动和突破。
    2013年 Deep Q-Network(DQN)是由DeepMind提出的一种结合深度神经网络和Q-learning的算法，首次实现了在Atari游戏中超越人类水平的表现。
    2015年: AlphaGo

- 挑战
如样本效率、探索与利用的平衡、通用性和可解释性等问题。
Q: 强化学习比深度学习简单吗？如果是复杂，复杂在哪里？

- 分类
* 按“是否理解环境”分类
    ** 无模型强化学习 (Model-free RL)：
        *** 智能体不尝试理解环境运行的物理规律，而是直接通过“试错”获取经验。
        *** 特点：通用性强，但数据利用率低（需要大量练习）。
    ** 基于模型的强化学习 (Model-based RL)：
        *** 智能体先学习一个环境模型（预测给定状态和动作后，下一个状态和奖励是什么），然后在模型中进行虚拟演练。
        *** 代表：AlphaZero。

* 按“学习目标”分类（Model-free 下的主流划分）
    ** 基于价值 (Value-based)
        *** 不直接学习动作，而是学习“在某个状态下，做某个动作未来能拿多少分”。即回答“在这个状态下，做哪个动作未来收益最高？”。
        *** 核心理念：学习一个动作价值函数 Q(s, a)，“在状态s下采取动作a，未来能获得的累积奖励期望是多少”。执行时，只需选择Q值最大的动作。
        *** 代表算法：
            **** Q-Learning：最经典的基础算法，维护一个 Q 表。
            **** DQN (Deep Q-Network)：用深度神经网络代替 Q 表，开启了深度强化学习时代。
    ** 基于策略 (Policy-based)
        *** 直接学习“在某个状态下，采取各动作的概率分布”。即回答“在这个状态下，应该选择什么动作？”。
        *** 核心理念：直接优化策略函数 π(a|s)，通过奖励的好坏来调整动作出现的概率。
        *** 代表算法：
            **** Policy Gradient (策略梯度)：简单直接。
            **** REINFORCE (蒙特卡洛策略梯度)：最基本的策略梯度算法。
            它完整地执行一个回合（episode），然后根据整个回合的总回报来增加“好”动作的概率，降低“坏”动作的概率。缺点是方差大，学习慢。
    ** 演员-评论家 (Actor-Critic)
        *** 结合了上述两者的优点：Actor 负责出招（策略），Critic 负责给这一招打分（价值评估）。
        *** 核心理念：Critic 提供更准确的反馈，帮助 Actor 更稳健地学习。
        *** 代表算法：
            **** A3C / A2C：异步/同步优势演员-评论家。
            **** PPO (Proximal Policy Optimization)：当前工业界最通用、最稳健的算法（OpenAI 默认算法，也是 RLHF 的核心）。
            **** SAC (Soft Actor-Critic)：兼顾高奖励与动作多样性，在机器人控制领域表现优异。

- 策略
策略是智能体的动作模型，它决定了智能体的动作。
它其实是一个函数，用于把输入的状态变成动作。
策略可分为两种：随机性策略（stochastic policy）和确定性策略（deterministic policy）。

- 价值
价值函数的值是对未来奖励的预测，用它来评估状态的好坏。
价值函数里面有一个折扣因子（discount factor），希望在尽可能短的时间里面得到尽可能多的奖励。

- 策略与价值
基于价值的智能体（value-based agent）显式地学习价值函数，隐式地学习它的策略。策略是其从学到的价值函数里面推算出来的。
基于策略的智能体（policy-based agent）直接学习策略，给它一个状态，它就会输出对应动作的概率。基于策略的智能体并没有学习价值函数。
把基于价值的智能体和基于策略的智能体结合起来就有了演员-评论员智能体（actor-critic agent）。

* 按“经验利用方式”分类在线学习
    ** 在线学习(On-policy)：
        *** 必须用当前策略采集到的数据进行更新。数据用完即弃，学习较慢但较稳定。
        *** 例子：SARSA, PPO。
    ** 离线学习 (Off-policy)：
        *** 可以利用旧策略产生的数据，甚至别人的数据（经验回放）。数据利用率极高。
        *** 例子：Q-Learning, DQN, DDPG。

* https://spinningup.openai.com/en/latest/spinningup/rl_intro2.html#a-taxonomy-of-rl-algorithms

- RLHF（Reinforcement Learning from Human Feedback，基于人类反馈的强化学习）
RLHF一种将人类偏好作为奖励信号，来训练或微调人工智能模型的方法。它尤其擅长让语言模型的行为更符合人类的期望、价值观和复杂指令。

- DPO（Direct Preference Optimization，直接偏好优化）
DPO和RLHF是目前大模型对齐的两大主流算法。它们的目标一致，但实现路径有本质区别，各有优劣。
维度	DPO (直接偏好优化)	RLHF (基于人类反馈的强化学习)
核心原理	直接学习：通过对比学习，直接根据“答案A比B好”的反馈来更新模型。	间接学习：先训练一个“奖励模型”来模仿人类打分，再用这个模型通过强化学习去训练主模型。
实现复杂度	低。流程简单，去除了奖励模型和RL环节，稳定性高，更易于实现和调试。	高。需要多阶段训练，涉及策略模型、奖励模型等多个组件，调参复杂，易出现“奖励黑客”等不稳定现象。
训练效率	高。显著减少了计算开销和训练时间，据估计可节省50-60% 的成本。	低。训练流程繁琐，显存和算力需求比DPO高30%-50%，周期长2-3倍。
核心优势	简单、高效、稳定。不削弱模型的基础能力（如事实性知识或逻辑推理），甚至在控制回答语气等任务上效果更优。	精准、复杂、可控。能精细化地捕捉和权衡多维度（如帮助性、无害性）的人类偏好，擅长处理非常复杂的对齐目标和场景。
主要局限性	对复杂偏好的建模能力有限。标准DPO假设存在一个单一的偏好目标，难以处理人类偏好中的多维性和冲突。	流程繁琐，资源消耗巨大。极高的计算成本和训练不稳定性是其应用的主要门槛

== 马尔可夫决策过程(Markov Decision Process, MDP)
=== 概念
https://en.wikipedia.org/wiki/Markov_decision_process

    State
    Action
    State transition, state transition probability p(s′∣s,a)
    Reward, reward probability p(r∣s,a)
    Trajectory, episode, return, discounted return
    Markov decision process

=== Key elements of MDP
==== Sets
* State: the set of states *S*
* Action: the set of actions *A(s)* is associated for state *s ∈ S*.
* Reward: the set of rewards *R(s, a)*.

==== Probability distribution
* State transition probability: at state *s*, taking action *a*, the probability to transit to state *s'* is *p(s' | s, a)*
* Reward probability: at state *s*, taking action *a*, the probability to get reward *r* is *p(r | s, a)*

==== Policy
At state *s*, the probability to choose action *a* is *π(a | s)*

==== Markov property: memoryless property
ifndef::env-github[]
\[p(s_{t+1} | a_{t+1}, s_t, \ldots, a_1, s_0) = p(s_{t+1} | a_{t+1}, s_t)\]
endif::env-github[]
ifdef::env-github[]
```math
p(s_{t+1} | a_{t+1}, s_t, \ldots, a_1, s_0) = p(s_{t+1} | a_{t+1}, s_t)
```
endif::env-github[]

ifndef::env-github[]
\[p(r_{t+1} | a_{t+1}, s_t, \ldots, a_1, s_0) = p(r_{t+1} | a_{t+1}, s_t)\]
endif::env-github[]
ifdef::env-github[]
```math
p(r_{t+1} | a_{t+1}, s_t, \ldots, a_1, s_0) = p(r_{t+1} | a_{t+1}, s_t)
```
endif::env-github[]

=== 马尔可夫性质
具有马尔可夫性质，即未来状态只依赖于当前状态和动作，与过去状态无关:
ifndef::env-github[]
stem:[P(s_{t+1} | s_t, a_t, s_{t-1}, a_{t-1}, \ldots) = P(s_{t+1} | s_t, a_t)]
endif::[]
ifdef::env-github[]
```math
P(s_{t+1} | s_t, a_t, s_{t-1}, a_{t-1}, \ldots) = P(s_{t+1} | s_t, a_t)
```
endif::[]

=== 参考
https://en.d2l.ai/chapter_reinforcement-learning/mdp.html

== 贝尔曼公式/贝尔曼方程(Bellman Equation)
=== return(回报)
What is return? The (discounted) sum of the rewards obtained along a trajectory.

假设一个确定性环境：状态 A → 状态 B → 终止，A 的奖励 0，B 的奖励 10，γ=0.9。
在 B 点：GB =10
在 A 点：GA=0+0.9×GB=9
用贝尔曼形式看：V(A)=0+0.9V(B)=9
Return 不是把 0 和 10 简单相加（那会是 10），而是把 B 的价值“折现”后传回给 A。贝尔曼方程做的正是——把远处的价值通过折扣一层层传回来。

Q: 为什么有γ：折扣因子？
γ 有三个不可替代的作用：
数学上：防止无穷奖励导致 Return 爆炸，保证数值有限；
直觉上：反映“近期的奖励比远期的更可靠、更重要”的常识；
算法上：保证贝尔曼方程迭代求解时必然收敛。

Q: 如何计算return?
return(回报) Gₜ 是从时刻 t 起沿轨迹累积的（折现）奖励之和。

定义式（求和）：把未来每步奖励乘以折扣因子 γᵏ 后相加，γ ∈ [0, 1]：
ifndef::env-github[]
\[G_t = \sum_{k=0}^{\infty} \gamma^k R_{t+k+1} = R_{t+1} + \gamma R_{t+2} + \gamma^2 R_{t+3} + \cdots\]
endif::env-github[]
ifdef::env-github[]
$$G_t = \sum_{k=0}^{\infty} \gamma^k R_{t+k+1} = R_{t+1} + \gamma R_{t+2} + \gamma^2 R_{t+3} + \cdots$$
endif::env-github[]
若回合在终止时刻 T 结束，求和上限改为 T − t − 1。

递推式（贝尔曼形式，最常用）：当前回报 = 即时奖励 + γ × 下一时刻回报：
ifndef::env-github[]
\[G_t = R_{t+1} + \gamma G_{t+1}\]
endif::env-github[]
ifdef::env-github[]
$$G_t = R_{t+1} + \gamma G_{t+1}$$
endif::env-github[]
这是自举（bootstrap）的核心，也是贝尔曼方程的来源。上面的例子中：G_B = 10，G_A = 0 + 0.9 × G_B = 9，正是用递推式从后往前算的结果。实操中很少按定义式展开无穷求和，而是用递推式反向计算（蒙特卡洛），或用贝尔曼方程迭代逼近。

=== state value
`G_t` 的期望（或称期望值、均值）被定义为*状态价值函数*，或简称为*状态价值*：
ifndef::env-github[]
\[v_\pi(s) = \mathbb{E}[G_t \mid S_t = s]\]
endif::env-github[]
ifdef::env-github[]
$$v_\pi(s) = \mathbb{E}[G_t \mid S_t = s]$$
endif::env-github[]

* 它是 `s` 的函数。它是一个条件期望，条件是状态从 `s` 开始。
* 它基于策略 `π`。对于不同的策略，状态价值可能不同。
* 它表示一个状态的“价值”。比较两个策略不能只看单个状态：若对所有状态 `s` 都有 `v_π₁(s) ≥ v_π₂(s)`（且至少一个状态严格大于），则称 `π₁` 优于 `π₂`；实践中常以初始状态价值 `v_π(s₀)` 或按状态分布加权的均值来衡量。
问：回报（return）与状态价值（state value）之间的关系是什么？
答：状态价值是从一个状态出发所能获得的所有可能回报的均值。
如果所有因素——`π(a|s)`、`p(r|s,a)`、`p(s'|s,a)`——都是确定性的，
那么状态价值与回报相同。

=== 推导(Derivation)

== 贝尔曼最优公式(Bellman Optimality Equation)

== 值迭代(Value Iteration)与策略迭代(Policy Iteration)
=== 概念
值迭代(Value Iteration)是强化学习中的一种经典算法，用于求解马尔可夫决策过程(MDP)的最优策略。它通过迭代更新值函数来逼近最优值函数，并最终导出最优策略。

=== 参考
https://en.d2l.ai/chapter_reinforcement-learning/value-iter.html

== 蒙特卡洛方法

== 随机近似与随机梯度下降

== 时序差分方法
=== Q-Learning
==== 概念
- 概念
Q-learning 是一种无模型的强化学习算法，用于在给定的马尔可夫决策过程(MDP)中找到最优的动作选择策略。它是一种离策略(off-policy)算法，意味着它可以独立于智能体的行为来学习最优策略。

- 优点

    无模型:
        不需要环境模型。
    保证收敛:
        在一定条件下，Q-learning 保证收敛到最优策略。

- 局限性

    可扩展性:
        对于具有许多状态和动作的问题，Q 表可能变得非常大。
    收敛速度慢:
        学习可能很慢，特别是在大型或复杂的环境中。

- 扩展与变体

    深度 Q 学习(DQN):
        将 Q-learning 与深度神经网络结合，以处理高维状态空间。
    双 Q 学习:
        通过使用两个 Q 函数减少 Q 值的高估。
    SARSA:
        Q-learning 的一种在策略(on-policy)变体，基于实际采取的动作更新 Q 值。

==== DQN(深度Q网络，Deep Q-Network)
===== 变种
- Double DQN
解耦动作选择和Q值评估，缓解过估计
https://pytorch.org/tutorials/intermediate/mario_rl_tutorial.html
https://arxiv.org/pdf/1509.06461

- Dueling DQN
分离状态价值和优势函数

- Prioritized Replay
优先回放高TD误差的经验，加速学习

===== 核心元素
- 马尔可夫决策过程(MDP)

    状态(S): 表示智能体可能处于的不同情况。
    动作(A): 智能体可以采取的可能行动。
    奖励(R): 采取动作后从环境获得的即时反馈。
    转移概率(P): 在给定动作下从一个状态转移到另一个状态的概率。

- Q值

    Q(s,a) 表示在状态 s 下采取动作 a 并随后遵循最优策略的预期累积奖励。

- 贝尔曼方程

===== 算法步骤
TODO:

==== 参考
https://en.d2l.ai/chapter_reinforcement-learning/qlearning.html
https://arxiv.org/pdf/1509.06461

== 值函数近似

== 策略梯度方法

== Actor-Critic方法

== 动态规划法

== project
- env
https://cloud.tencent.com/developer/article/1788317

- mario
https://github.com/vietnh1009/Super-mario-bros-PPO-pytorch
https://pytorch.org/tutorials/intermediate/mario_rl_tutorial.html

- dota2
https://github.com/bilibili/LastOrder-Dota2

- 斗地主
https://github.com/kwai/DouZero

- Pong from Pixels
https://karpathy.github.io/2016/05/31/rl/

- gymnasium
https://gymnasium.farama.org/introduction/basic_usage/#your-first-rl-program
https://gymnasium.farama.org/introduction/train_agent/

== 参考
- 李宏毅
https://datawhalechina.github.io/easy-rl/

- 赵世钰
《强化学习的数学原理》
https://www.icourse163.org/course/XHUN-1470436188?tid=1475125444
https://www.bilibili.com/video/BV1sd4y167NS
https://github.com/MathFoundationRL/Book-Mathematical-Foundation-of-Reinforcement-Learning

- 王树森
https://www.bilibili.com/video/BV1hhbSzjEi1/
《深度强化学习》
https://github.com/wangshusen/DRL

-《动手强化学习》
https://hrl.boyuai.com/chapter/intro

- 《Reinforcement Learning: An Introduction》2nd Edition
- 《Deep Reinforcement Learning Hands-On》

- UC Berkeley
CS285
https://rail.eecs.berkeley.edu/deeprlcourse/

- 斯坦福大学《CS234: 强化学习》

- UCLA
Reinforcement Learning of Large Language Models
