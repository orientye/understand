==== 前向传播、反向传播和计算图
===== 前向传播/正向传播(forward propagation或forward pass)
前向传播按依赖顺序计算并存储每个节点的值。对神经网络就是从输入层到输出层；对任意计算图就是按拓扑序求值。要存的中间结果，反向传播会用到。
计算图必须是 DAG（有向无环图）。

为什么要求值？
训练时：算出输出和损失；并把中间值留下，反向传播（链式法则）需要用到前向传播的中间数值（局部导数里要用到）。
推理时：只为得到结果，这时一般不建图、不留中间值给反向，也不调用 backward。

为什么要建图？
建图是为了把复杂函数的梯度计算，分解成一系列简单操作的局部导数组合，让计算机自动执行链式法则：
记录依赖（_prev）：知道梯度往哪传
存储操作（_op）：知道用什么导数公式
排序执行（拓扑排序）：保证计算顺序正确

以 Karpathy 的 micrograd 为例：
每个节点是一个标量 `Value`，记下 `data`（前向值）、`grad`（反向填）、孩子指针、局部 `_backward`。没有矩阵、没有正则，主要是链式法则的应用。
https://github.com/karpathy/micrograd
https://www.youtube.com/watch?v=VMj-3S1tku0

表达式 L = (a×b + c)×f：

```
from micrograd.engine import Value
a = Value(2.0)
b = Value(-3.0)
c = Value(10.0)
e = a * b          # e.data = -6
d = e + c          # d.data = 4
f = Value(-2.0)
L = d * f          # L.data = -8
```

```
a=2 ──┐
      × → e=-6 ──┐
b=-3 ─┘          + → d=4 ──┐
c=10 ────────────┘         × → L=-8
f=-2 ──────────────────────┘
```

前向就是按 Python 从左往右算，顺手把 DAG 搭起来（Define by Run）。每个 `Value.data` 是前向结果；孩子指针是边。反向真正用到的中间值是 d.data=4（L=d×f 要「换另一个因子」）；e 只进加法，加法不用 e.data。叶子 a、b、f 的 data 也会被乘法用到。

`d = e + c` 走的是 `Value.__add__`。前向三件事：适配类型、算出 `out.data`、把 `_backward` 挂到新节点上。

```
def __add__(self, other):
    other = other if isinstance(other, Value) else Value(other)
    out = Value(self.data + other.data, (self, other), '+')

    def _backward():
        self.grad += out.grad      # ∂L/∂self = out.grad × 1
        other.grad += out.grad     # ∂L/∂other = out.grad × 1
    out._backward = _backward
    return out
```

加法 `out = self + other` 的局部导数是常量 1：

| 偏导数 | 值 |
|--------|----|
| ∂out/∂self | 1 |
| ∂out/∂other | 1 |

链式法则：∂L/∂self = (∂L/∂out) × (∂out/∂self) = out.grad × 1。例子里 out 是 d，self/other 是 e 和 c，所以反传是 `e.grad += d.grad`、`c.grad += d.grad`。

`e = a * b`、`L = d * f` 走的是 `Value.__mul__`。同样是求值时挂 `_backward`。

```
def __mul__(self, other):
    other = other if isinstance(other, Value) else Value(other)
    out = Value(self.data * other.data, (self, other), '*')

    def _backward():
        self.grad += other.data * out.grad    # ∂L/∂self = out.grad × other
        other.grad += self.data * out.grad    # ∂L/∂other = out.grad × self
    out._backward = _backward
    return out
```

乘法 `out = self × other` 的局部导数是另一个因子（前向留下的 `.data`，不是 `.grad`）：

| 偏导数 | 值 |
|--------|----|
| ∂out/∂self | other.data |
| ∂out/∂other | self.data |

链式法则：∂L/∂self = (∂L/∂out) × (∂out/∂self) = out.grad × other.data。`out.grad` 是上游已算好的。例子里 L = d × f，所以 `d.grad += f.data * L.grad`。

参考：
https://en.d2l.ai/chapter_multilayer-perceptrons/backprop.html#forward-propagation
https://zh.d2l.ai/chapter_multilayer-perceptrons/backprop.html#id2

===== 计算图(computational graph)
====== 概念
计算图是把计算画成有向无环图(DAG)：节点是张量或算子（micrograd 里就是 `Value`），边是数据依赖。前向按拓扑序执行，反向按逆拓扑序求导。RNN 沿时间展开后也是 DAG。
通过计算图可以直观把握计算过程：节点只做局部计算，局部计算组合成全局计算；反向传播沿图回传，逐节点算局部导数。
https://en.d2l.ai/chapter_multilayer-perceptrons/backprop.html#computational-graph-of-forward-propagation
https://zh.d2l.ai/chapter_multilayer-perceptrons/backprop.html#id3

为什么图必须是 DAG（有向无环图）？
数学基础（链式法则）：链式法则要求梯度从输出端（Loss）沿着前向传播的逆序往回传播。如果存在环（Cycle），就意味着某个变量的梯度依赖于它自身未来的状态，这会形成无穷级数或循环依赖，导致无法求出唯一的偏导数闭式解。
拓扑排序（执行顺序）：反向传播需要拓扑排序。只有 DAG 才能进行拓扑排序（即存在一个线性序列，使得所有依赖项排在被依赖项之前）。如果存在环，排序算法会失败，程序无法决定先计算谁的梯度。
物理意义（时间维度）：在神经网络中，前向传播代表时间或空间的单向流动（从 Input 到 Loss）。即使是 RNN（循环神经网络），在时间轴上展开（Unroll）后，也是一个沿着时间步延伸的 DAG，而不是闭合的环。

====== 局部反向规则
反向传播的每一步都是「上游梯度 × 局部导数」。micrograd 里节点是标量，那个上游梯度就叫 `out.grad`；矩阵写法（D2L）里同一个量叫 g；`+=` 是因为一个节点可能被用多次（多元链式法则；和前文广播那节「一条参数用多次，梯度加多次」是同一条规则）。

| 前向 | `_backward`（上游为 out.grad） |
|------|-------------------------------|
| d = e + c | e.grad += out.grad，c.grad += out.grad（加法：原样分发；out.grad 即 d.grad） |
| e = a × b | a.grad += b.data × out.grad，b.grad += a.data × out.grad（乘法：换另一个因子；out.grad 即 e.grad） |
| o = tanh(n) | n.grad += (1 − o.data²) × out.grad（out.grad 即 o.grad） |

矩阵版是同一套规则的批量写法，框架对每个算子只实现局部 VJP，不拼整张雅可比（见上文 autograd）：

| 前向 | 反向（上游梯度为 g） |
|------|-------------------|
| o = W h | ∂J/∂h = Wᵀ g，∂J/∂W = g hᵀ |
| h = φ(z) | ∂J/∂z = g ⊙ φ′(z) |

====== **Define by Run(动态图)**
Define by Run（运行时定义）指在代码执行时动态构建计算图，而非预先静态定义。micrograd / PyTorch 都是这样：上面那段 Python 跑完，图已经在了。常见于 PyTorch、TensorFlow 2.x(Eager 模式)等框架。

====== **Define-and-Run(静态图)**
图需预先完整定义再执行，典型框架 TensorFlow 1.x。

[cols="1,2,2", options="header"]
|===
| 对比项 | Define by Run (动态图) | Define-and-Run (静态图)
| 图构建 | 运行时动态生成 | 预先完整定义
| 控制流 | Python 原生 `if`/`for` | 需在图里显式表达
| 调试 | 友好，可打印中间值 | 相对困难
| 灵活性 | 高(可变长度输入、条件分支) | 低
| 性能 | 有 Python 开销 | 可整图优化、去 Python 开销
|===

静态/编译图的好处：整图可见，便于算子融合、内存规划、常量折叠；可编译成不依赖 Python 的可部署形式。分布式训练里，流水线并行需要知道 stage 切分点，导出/Serving 也依赖整图；但数据并行(DDP)、完全分片(FSDP)并不依赖预先定义整张静态图。

====== 动态图静态图转换
现代 AI 框架大多不再割裂两者，而是走向动静统一。
开发流程：业界普遍采用动态图调试研发 -> 静态图转换 -> 生产环境部署的标准流程。

PyTorch：
torch.compile（2.0 起）：通过 Dynamo 捕捉动态计算图，再交给 Inductor 等后端编译优化。视模型而定能带来提速，但有 graph break、首次编译开销，不保证加速。
TorchScript：通过 torch.jit.trace 或 torch.jit.script 将模型导出为静态、可独立部署的图表式。

TensorFlow 2.x：
默认动态图，只需加 @tf.function 装饰器就能将 Python 函数自动转成静态图（AutoGraph），兼顾易用与高速。

JAX：通过函数变换和 JIT 编译，将纯函数式的动态代码编译成高效静态执行图。

====== 参考
https://openmlsys.github.io/chapter_computational_graph/index.html

===== 反向传播(backward propagation或backpropagation)
====== 概念
反向传播指的是计算神经网络参数梯度的方法。根据微积分中的链式法则，按相反的顺序从输出层到输入层遍历网络。
`L.backward()` 做两件事：拓扑排序，再倒着调用每个节点自己的 `_backward`。

为什么需要反向传播？
手动计算深层网络的梯度极其复杂，反向传播通过链式法则高效计算梯度。
梯度下降依赖反向传播提供的梯度方向来更新参数。

====== 示例1：L = (a×b + c)×f
接上面 L = (a×b + c)×f。L 是标量目标，种子 L.grad = 1（∂L/∂L）。按逆拓扑序，每步「上游梯度 × 局部导数」（`+=`，因为节点可能被用多次）。
容易混：要填的 `d.grad` 才是目标对 d 的导数 ∂L/∂d；乘在前面的是本步局部的 ∂out/∂d（这一步 out 就是 L，所以 ∂out/∂d = f，不是已经算好的 `d.grad`）。

```
起步
  L.grad = 1                    # ∂L/∂out，out 就是 L

L = d * f                       # 这一步 out = L = d × f
  局部（只看本算子）：∂out/∂d = f，∂out/∂f = d
  用前向值：∂out/∂d = f.data = -2
  上游已填：out.grad = L.grad = 1
  链式：∂L/∂d = (∂L/∂out) × (∂out/∂d) = L.grad × f.data
  d.grad += 1 * (-2) = -2
  同理：∂L/∂f = L.grad × d.data = 1 * 4 = 4
  f.grad += 4

d = e + c                       # 这一步 out = d = e + c
  局部（只看本算子）：∂out/∂e = 1，∂out/∂c = 1
  加法不用前向值
  上游已填：out.grad = d.grad = -2
  链式：∂L/∂e = (∂L/∂out) × (∂out/∂e) = d.grad × 1
  e.grad += (-2) * 1 = -2
  同理：∂L/∂c = d.grad × 1 = -2
  c.grad += -2

e = a * b                       # 这一步 out = e = a × b
  局部（只看本算子）：∂out/∂a = b，∂out/∂b = a
  用前向值：∂out/∂a = b.data = -3
  上游已填：out.grad = e.grad = -2
  链式：∂L/∂a = (∂L/∂out) × (∂out/∂a) = e.grad × b.data
  a.grad += (-2) * (-3) = 6
  同理：∂L/∂b = e.grad × a.data = (-2) * 2 = -4
  b.grad += -4
```

对照闭式 L = (a×b + c)×f：

```
∂L/∂a = b×f = (-3)×(-2) =  6
∂L/∂b = a×f =  2 ×(-2) = -4
∂L/∂c = f     = -2
∂L/∂f = d     =  4
```

逐步回传和闭式一致。全程没有拼雅可比：每步只用该算子需要的 `data`（乘法用另一个因子，加法不用操作数的值）。

====== 示例2：a、b 各用两次（必须 +=）
示例1 里每个叶子只进一条路，`+=` 和 `=` 看不出差别。这里 a、b 各走两条路到 e（a/b 分到 c、d，再汇到 e）；仍是 DAG，不是环。

```
a = Value(2.0)
b = Value(3.0)
c = a * b          # 6
d = a + b          # 5
e = c * d          # 30
```

```
      a=2                b=3
       │\                /│
       │ \              / │
       │  ↘            ↙  │
       ↓                  ↓
    c=a×b=6            d=a+b=5
         \                /
          \              /
           ↘            ↙
              e=c×d=30
```

闭式 e = (a b)(a + b) = a²b + a b²：

```
∂e/∂a = 2 a b + b² = 12 + 9 = 21
∂e/∂b = a² + 2 a b =  4 + 12 = 16
```

a.grad = 21、b.grad = 16 的意思：在 (a,b)=(2,3) 这一点的瞬时变化率（单位是 e 的单位 / 该输入的单位）。a 微增，e 按约 21 倍变；b 微增，按约 16 倍变。不是有限步：a 从 2 加到 3，e 从 30 到 54，变了 24 而不是 21；b 从 3 加到 4，e 变成 48，变了 18 而不是 16。函数不是线性的，21 / 16 是切线斜率；只有 Δ 很小时 Δe ≈ 21 Δa。用很小的 Δ 去数值微分，(e(a+Δ)−e(a))/Δ 会贴近 21。

这一点上 21 > 16，同样加一单位时 a 对 e 的局部影响更大，可以说 a 更敏感。不要推满：换一个点就不一定（a=10, b=1 时 ∂e/∂a=21、∂e/∂b=120，反而 b 更陡）；a、b 量纲或尺度差很多时，不能直接比两个偏导的大小。

反传（和示例1 同一套规则；a、b 各有两条路，所以是 +=）：

```
起步
  e.grad = 1                    # ∂e/∂out，out 就是 e

e = c * d                       # 这一步 out = e = c × d
  局部（只看本算子）：∂out/∂c = d，∂out/∂d = c
  用前向值：∂out/∂c = d.data = 5
  上游已填：out.grad = e.grad = 1
  链式：∂e/∂c = e.grad × d.data = 1 × 5 = 5
  c.grad += 5
  同理：∂e/∂d = e.grad × c.data = 1 × 6 = 6
  d.grad += 6

c = a * b                       # 这一步 out = c = a × b；第一条路
  局部：∂out/∂a = b，∂out/∂b = a
  用前向值：∂out/∂a = b.data = 3
  上游已填：out.grad = c.grad = 5
  链式：∂e/∂a 的这一路 = c.grad × b.data = 5 × 3 = 15
  a.grad += 15                  # 现在 a.grad = 15
  同理：∂e/∂b 的这一路 = c.grad × a.data = 5 × 2 = 10
  b.grad += 10                  # 现在 b.grad = 10

d = a + b                       # 这一步 out = d = a + b；第二条路
  局部：∂out/∂a = 1，∂out/∂b = 1
  上游已填：out.grad = d.grad = 6
  链式：∂e/∂a 的这一路 = d.grad × 1 = 6
  a.grad += 6                   # 15 + 6 = 21
  同理：∂e/∂b 的这一路 = d.grad × 1 = 6
  b.grad += 6                   # 10 + 6 = 16
```

两条路加起来才是全导数（多元链式法则）：

```
∂e/∂a = (∂e/∂c)(∂c/∂a) + (∂e/∂d)(∂d/∂a) = 5×3 + 6×1 = 15 + 6 = 21
∂e/∂b = (∂e/∂c)(∂c/∂b) + (∂e/∂d)(∂d/∂b) = 5×2 + 6×1 = 10 + 6 = 16
```

若 `_backward` 里写 `=` 而不是 `+=`，后走的那条会盖掉先走的：

```
def _backward():
    self.grad = other.data * out.grad    # 错：= 会覆盖
```

先走 c = a * b：a.grad = 15；再走 d = a + b：a.grad = 6（盖掉 15）。最终 a.grad = 6，不是 21。+= 才能累加到 21。

反向必须逆拓扑序（从输出到叶子），保证调某节点的 `_backward` 时，它的上游 `.grad` 已就绪。topo 怎么来：从输出做后序 DFS（先走完孩子再把本节点记下），再把记录倒过来。示例2 从 e 出发，孩子是 c、d。若先探 c：先记下 a、b、c，再记下 d，最后 e，记录 [a, b, c, d, e]，倒过来 [e, d, c, b, a]。若先探 d：倒过来就是下面这种 [e, c, d, a, b]。两种都是逆拓扑序。

```
topo = [e, c, d, a, b]    # 后序遍历后倒过来的一种
e._backward()             # c.grad=5, d.grad=6
c._backward()             # a.grad=15, b.grad=10
d._backward()             # a.grad=21, b.grad=16
```

e 必须先于 c、d。c 与 d 之间谁先都行（都在 e 之后，写的是不同叶子上的 +=）；但不能在 e 之前就调 c 或 d，那时上游还是 0。和闭式对不上 30 / 21 / 16，就是图、局部公式、+= 或顺序断了。

====== 一个神经元
Karpathy 课里那个 tanh 神经元才像网络，仍然全是标量：

```
x1, x2 = Value(2.0), Value(0.0)
w1, w2 = Value(-3.0), Value(1.0)
b = Value(6.8813735870195432)   # 故意让 n = tanh⁻¹(√2/2)
n = x1*w1 + x2*w2 + b           # n.data = 0.881373...
o = n.tanh()                    # o.data = √2/2
o.backward()
```

`tanh` 的局部规则：n.grad += (1 − o.data²) × o.grad。
o.grad = 1，o = √2/2，故 1 − o² = 1/2，n.grad = 0.5。
n 是三项之和，先拆成加法树再按乘法分：

```
p = x1*w1 = -6
q = x2*w2 =  0
t = p + q              # 第一个 +
n = t + b              # 第二个 +
n.grad = 0.5
t.grad += 0.5          # n → t
b.grad += 0.5          # n → b
p.grad += 0.5          # t → p
q.grad += 0.5          # t → q
w1.grad = x1.data × p.grad =  2 × 0.5 =  1.0
x1.grad = w1.data × p.grad = -3 × 0.5 = -1.5
w2.grad = x2.data × q.grad =  0 × 0.5 =  0
x2.grad = w2.data × q.grad =  1 × 0.5 =  0.5
```

和示例1 一样是二元 +：n.grad 先到 t 和 b，再从 t 到 p、q，每条边乘 1。再按乘法换因子。MLP 只是很多个这样的 `Value` 叠起来。

====== 同一套规则的矩阵写法
把上面的神经元收成向量/矩阵。先只看一步 o = W h，把转置和外积从标量乘加推出来；再用平方误差把上游 g 算开；最后才是 D2L 那个单隐层 MLP（不带偏置，可再加 L₂）。

从标量乘加到 matmul
o = W h。W 形状 q×m，h 是 m 维列向量（这里 h 只表示右侧向量，还不是隐层），o 是 q 维。第 i 个输出是一排乘加，和神经元里的 wx 一样：

oᵢ = Σₖ Wᵢₖ hₖ

每个 Wᵢₖ × hₖ 是示例1 的乘法，再按加法把 k 项加起来。令 g = ∂J/∂o（下面用损失算出它）。对某一个 hⱼ：它进了每一个 oᵢ，所以

∂J/∂hⱼ = Σᵢ gᵢ · Wᵢⱼ

这正是 (Wᵀ g)ⱼ。故 ∂J/∂h = Wᵀ g。
为什么是转置：前向用 W 的第 i 行去点 h；反向时 hⱼ 要对 W 的第 j 列负责，把这一列抽出来左乘，就是 Wᵀ。

对某一个 Wᵢⱼ：乘法换另一个因子，另一个因子是 hⱼ，

∂J/∂Wᵢⱼ = gᵢ · hⱼ

收成矩阵：∂J/∂W = g hᵀ（外积）。g 是 q×1，hᵀ 是 1×m，得到 q×m，和 W 同形。不是 W×h 那种矩阵乘向量。

数字（q=2, m=2）：

```
h = [2, 1]
W = [[1, 2],
     [3, 4]]
o = W h = [4, 10]
```

先假定 g = ∂J/∂o = [1, 2]（下面会得到这个 g）：

```
∂J/∂h = Wᵀ g = [[1, 3], [2, 4]] [1, 2] = [7, 10]
标量核对：∂J/∂h₁ = 1×1 + 2×3 = 7，∂J/∂h₂ = 1×2 + 2×4 = 10

∂J/∂W = g hᵀ = [[1], [2]] [2, 1] = [[2, 1], [4, 2]]
标量核对：W₁₁: 1×2=2，W₁₂: 1×1=1，W₂₁: 2×2=4，W₂₂: 2×1=2
```

框架对 matmul 只实现这一对局部 VJP，不会拼 q×m 的雅可比。激活是按元素：∂J/∂z = g ⊙ φ′(z)，和 tanh 的 (1−o²)×out.grad 同一类。

损失把 g 算开
上面的 g 从哪来。取平方误差。½ 只是让导数少一个 2；后文训练循环 `loss = (o − y)**2` 对应 ∂L/∂o = 2(o−y)，2 可并进学习率：

L = ½ Σᵢ (oᵢ − yᵢ)²
∂L/∂oᵢ = oᵢ − yᵢ
即 g = o − y。

接上例：令 y = [3, 8]，则 o − y = [1, 2]，L = ½(1+4) = 2.5，g = [1, 2]，正是上面用的上游。

种子仍是标量 ∂L/∂L = 1；过损失节点之后才变成向量 g。交叉熵只是损失节点的局部导数换成别的式子（softmax+CE 合在一起时常是 o−y）；进 matmul 之后还是 Wᵀ g、g hᵀ。

两层小数（matmul 用两次，中间夹 ⊙）
输入 2 维，2 个隐单元，输出 1 维，φ=ReLU，无偏置、无正则。

```
x  = [2, 0]
W¹ = [[1.0, 0.5],
      [-0.5, 1.0]]          # 2×2
z  = W¹ x = [2, -1]
h  = ReLU(z) = [2, 0]
W² = [1, 3]                 # 1×2
o  = W² h = 2
y  = 1
L  = ½ (o−y)² = 0.5
```

反向：

```
g = ∂L/∂o = o−y = 1

∂L/∂W² = g hᵀ = [2, 0]      # 外积；h₂=0，W² 的第二列这轮没贡献
∂L/∂h  = (W²)ᵀ g = [1, 3]
∂L/∂z  = [1, 3] ⊙ ReLU′(z) = [1, 3] ⊙ [1, 0] = [1, 0]
         # z₂<0，这条隐单元关掉，梯度到此截断
∂L/∂W¹ = (∂L/∂z) xᵀ = [[1], [0]] [2, 0] = [[2, 0], [0, 0]]
```

第二行 W¹ 全 0：不是漏了 +=。示例2 的 += 是同一叶子有多条都有贡献的路径，要累加；这里路径在，只是 ReLU 在 z₂<0 处局部导数是 0，乘完这条就是 0。

一般写法（D2L 单隐层）
输入 x ∈ ℝᵈ，隐藏层宽 m，输出 q 维（向量仍叫 h；仍不带偏置；正则另算）：

ifndef::env-github[]
[stem]
++++
\mathbf{z} = \mathbf{W}^{(1)} \mathbf{x},\quad
\mathbf{h} = \phi(\mathbf{z}),\quad
\mathbf{o} = \mathbf{W}^{(2)} \mathbf{h}
++++

[stem]
++++
L = l(\mathbf{o}, y),\quad
s = \frac{\lambda}{2}\bigl(\|\mathbf{W}^{(1)}\|_F^2 + \|\mathbf{W}^{(2)}\|_F^2\bigr),\quad
J = L + s
++++
endif::[]
ifdef::env-github[]
```math
\mathbf{z} = \mathbf{W}^{(1)} \mathbf{x},\quad
\mathbf{h} = \phi(\mathbf{z}),\quad
\mathbf{o} = \mathbf{W}^{(2)} \mathbf{h}
```

```math
L = l(\mathbf{o}, y),\quad
s = \frac{\lambda}{2}\bigl(\|\mathbf{W}^{(1)}\|_F^2 + \|\mathbf{W}^{(2)}\|_F^2\bigr),\quad
J = L + s
```
endif::[]

前向按 z → h → o → L → J，并把 z、h 留下来（和 micrograd 把 `data` 留在节点上是一回事）。平方误差时 ∂L/∂o = o − y；一般损失就留成 ∂L/∂o。反向按相反顺序，每步仍是刚推的局部 VJP（matmul 用 Wᵀ g 和 g hᵀ，激活用 ⊙）：

ifndef::env-github[]
[stem]
++++
\frac{\partial J}{\partial \mathbf{o}} = \frac{\partial L}{\partial \mathbf{o}}
++++

[stem]
++++
\frac{\partial J}{\partial \mathbf{W}^{(2)}} = \frac{\partial J}{\partial \mathbf{o}}\,\mathbf{h}^\top + \lambda \mathbf{W}^{(2)}
++++

[stem]
++++
\frac{\partial J}{\partial \mathbf{h}} = {\mathbf{W}^{(2)}}^\top \frac{\partial J}{\partial \mathbf{o}},\quad
\frac{\partial J}{\partial \mathbf{z}} = \frac{\partial J}{\partial \mathbf{h}} \odot \phi'(\mathbf{z})
++++

[stem]
++++
\frac{\partial J}{\partial \mathbf{W}^{(1)}} = \frac{\partial J}{\partial \mathbf{z}}\,\mathbf{x}^\top + \lambda \mathbf{W}^{(1)}
++++
endif::[]
ifdef::env-github[]
```math
\frac{\partial J}{\partial \mathbf{o}} = \frac{\partial L}{\partial \mathbf{o}}
```

```math
\frac{\partial J}{\partial \mathbf{W}^{(2)}} = \frac{\partial J}{\partial \mathbf{o}}\,\mathbf{h}^\top + \lambda \mathbf{W}^{(2)}
```

```math
\frac{\partial J}{\partial \mathbf{h}} = {\mathbf{W}^{(2)}}^\top \frac{\partial J}{\partial \mathbf{o}},\quad
\frac{\partial J}{\partial \mathbf{z}} = \frac{\partial J}{\partial \mathbf{h}} \odot \phi'(\mathbf{z})
```

```math
\frac{\partial J}{\partial \mathbf{W}^{(1)}} = \frac{\partial J}{\partial \mathbf{z}}\,\mathbf{x}^\top + \lambda \mathbf{W}^{(1)}
```
endif::[]

两层就是把 matmul VJP 用两次，中间夹一次 ⊙ φ′。`+ λW` 是正则项 s 自己的梯度（W → s → J），不再穿过 h、z。和示例2 一样是两条路加在同一个 W 上（过网络的 L，过正则的 s）。

参考：
https://en.d2l.ai/chapter_multilayer-perceptrons/backprop.html#backpropagation
https://zh.d2l.ai/chapter_multilayer-perceptrons/backprop.html#id4

====== 为什么用反向模式
标量损失、n 个参数时三种求导方式的代价：

| 方式 | 代价(量级) |
|------|-----------|
| 反向模式 AD | 一次前向 + 一次反向，约 2~3 倍前向 |
| 前向模式 AD | 对每个输入播一次切向，约 n 次前向 |
| 数值微分(前向差分) | 每个参数至少一次额外函数求值 |

AD 是 automatic differentiation（自动微分）。
反向模式 AD：从标量损失往参数回传，一次拿到全部梯度。
前向模式 AD：从某个输入往输出推切向，每个输入要播一次，因此大约要 n 次前向。

深度学习训权重是「标量损失 + 海量参数」，反向模式一次就能拿到全部梯度，代价与前向同量级——这就是「一次反传得到全部梯度」的来源，也是上一节无梯度方法在可微损失上通常更贵的对照。前向模式适合输出很多、输入很少(例如 `jax.jacfwd`)，不是训练权重的默认。

====== 局限与争议
反向传播对训练神经网络仍是默认方法，研究层面的争议主要是：

内存: 必须存中间激活值以计算梯度，训练比推理占显存更多。
backward locking: 层间反向有依赖，第 l 层要等 l+1 层的上游梯度，限制了层间并行；数据并行、张量并行不受此影响。
生物合理性: 反传需要前向/反向严格交替，与生物神经系统的信号机制不符。这是 Forward-Forward、predictive coding 等替代方向的出发点，与上述 locking、内存是并列的问题，不是因果关系。
连续学习: 灾难性遗忘是任务依次到来、共享参数被新任务覆盖的现象，与反传按层顺序传梯度无关。

====== 替代方向
见上一节「无梯度算法」。补充几条与反向传播对照的方向：

基于局部损失: 差异目标传播(difference target propagation)、Forward-Forward 算法，避免全局反传，但精度和可扩展性仍受限。
零阶梯度: 用函数值估计 ∇f(有限差分、SPSA)，再走类似梯度下降。
进化策略: 见上一节。

这些方法在准确性、计算效率、可扩展性上尚未撼动反传在主流深度学习中的地位。

===== 训练神经网络
在训练神经网络时，前向传播和反向传播相互依赖：
- 前向用的是上一轮更新后的参数（micrograd 里是各 `Value.data`，矩阵写法里是 W）；
- 反向用前向存下的中间值（示例1 里是 `d.data`；矩阵写法里是 h，以及 φ′ 要用的 z）。

因此必须交替，不能只做反向传播。在初始化模型参数后，交替使用前向传播和反向传播，利用反向传播给出的梯度来更新模型参数。反向传播重复利用前向传播中存储的中间值，以避免重复计算；代价是要保留这些中间值直到反向传播完成，这也是训练比单纯的预测需要更多内存(显存)的原因之一，且大致与层数和批量大小成正比。

接上面那个 tanh 神经元：x1、x2 当数据，只训 w1、w2、b。前面 `o.backward()` 只走了一轮；训练是把同一套前向/反向套进循环（不再拆 `_backward`）：

```
y = Value(1.0)    # 想让 o 靠近 1
lr = 0.1
for k in range(200):            # 固定迭代次数：最简单的停止条件
    n = x1*w1 + x2*w2 + b     # 前向：用当前 Value.data 建图；n、o、loss 会留下
    o = n.tanh()
    loss = (o - y)**2

    w1.grad = w2.grad = b.grad = 0.0   # 对应 zero_grad；不清则 += 叠上轮
    # x1、x2、y 的 .grad 也会累，但不更新它们，不影响下一步
    loss.backward()                    # 反向：用本轮留下的中间值（tanh 用 o.data，**2 用 (o−y).data）

    w1.data += -lr * w1.grad           # 梯度下降：w ← w − lr · ∂loss/∂w
    w2.data += -lr * w2.grad           # .grad 指向 loss 增大的方向，所以乘负号往下走
    b.data  += -lr * b.grad            # 只改叶子的 .data，不建新节点（对应 step）
```

只动参数，不动数据：x1、x2、y 即使有 `.grad` 也不改 `.data`。改的是数本身，不是图；下一轮 `n = x1*w1 + …` 读到的就是新的 `w.data`。不要写成 `w1 = w1 + …`，那会再长出一个 `Value`，叶子就丢了。

`range(200)` 是固定迭代次数——最简单的停止条件。生产代码里更常用早停：监控验证集损失，连续若干轮不降就 `break`。理论上梯度为零才停，但深度学习几乎不等。

输入数据 → [正向传播] → 计算损失 → [反向传播] → 更新参数 → 迭代。PyTorch 里同一条循环是 `zero_grad → 前向 → 目标 → backward → step`，见下一节。

优化:
神经网络分为训练(train)和推理(inference)两个阶段。训练阶段需要求导数；推理阶段只做正向传播，可以把中间结果扔掉，大幅缩减内存。需要进一步省显存时用梯度检查点(gradient checkpointing)：反向时不存中间激活而是重算，用时间换空间，见后文。

参考：
https://karpathy.github.io/2019/04/25/recipe/

===== 实现
训练循环里每次算完损失后：`backward()` 把梯度写入叶子张量的 `.grad`(累积，所以要先 `optimizer.zero_grad()`)；真正改参数是下一步 `optimizer.step()`。即 `zero_grad → 前向 → 目标 → backward → step`，详见上文 autograd 与训练过程。

===== 自动微分(automatic differentiation)
====== 概念
- 计算机求导
主要有 3 种: 数值微分、符号微分、自动微分。

- 符号微分(symbolic differentiation)
** 使用的导数公式求导、输入和输出都是式子(式子可以用树状结构的数据形式表示)
** 被用在Mathematica和MATLAB等软件中
** 符号微分的输出是求导后的表达式，即导函数
** 符号微分的问题是表达式很容易变得臃肿，在不考虑优化的实现中，表达式很快就会变大

- 数值微分(numerical differentiation)
** 核心思想:
    用差分逼近微分，核心公式源自泰勒展开: 忽略高阶项后，可推导出导数近似表达式。
** 数值微分的缺点:
    *** 精度问题:
        步长 h 难以选择(太小→舍入误差；太大→截断误差)。
        前向差分取 h ≈ √ε、中心差分取 h ≈ ε^(1/3)，其中 ε 是机器精度(float64 约 2×10⁻¹⁶，float32 约 1.2×10⁻⁷)。注意 10⁻⁸ 是 float64 前向差分的推荐步长，不是 ε 本身；float32 的 √ε 约 3×10⁻⁴。
        提升精度：用中心差分 / 高阶差分(多一次求值换更小截断误差)、用更高精度浮点、做 Richardson 外推。
    *** 计算成本高:
        每计算一个梯度需 O(n) 次函数调用(n 是参数维度)。
    *** 不适用于高维:
        深度学习模型参数动辄百万级，通过数值微分对这么多的变量求导是不现实的。
        实践中常用来做梯度检查(gradient check)：在小数据、小网络上比对解析梯度和数值梯度，验证 autograd 实现是否正确。

- 自动微分(automatic differentiation)
** 核心思想:
    利用链式法则分解计算图，精确计算导数。精确指的是「程序这次实际执行展开出的那张图」上的链式法则(浮点舍入除外)，不是抽象数学函数；`if` 只走已执行分支、ReLU 在 0 处按实现定义。
** 前向模式(Forward Mode):
    沿计算图正向传播导数，每个输入播一次切向。适合输出多、输入少。
** 反向模式(Reverse Mode):
    反向传播梯度，一次拿到全部输入的梯度。适合标量损失、输入多。
** 自动微分与反向传播:
    自动微分分前向、反向两种模式；反向传播是反向模式自动微分在神经网络训练中的应用。

====== 实现
- API
https://pytorch.org/docs/stable/autograd.html
动态计算图：每次前向传播都创建新图
延迟执行：梯度计算在 backward() 时才进行
梯度是累加的：记得及时清零
叶子节点 vs 中间节点：只有叶子节点直接存储梯度
上下文管理：合理使用 no_grad() 和 inference_mode()
torch.inference_mode() 是 PyTorch 中用于推理的上下文管理器，作用是禁用梯度计算和自动微分机制。它比 no_grad() 关掉更多追踪(version counter、view tracking)，因此更快；代价是出来的张量更难再参与需要梯度的计算。纯推理用 inference_mode()；验证集评估等需要偶尔接回训练图的场景用 no_grad()。

- micrograd
https://github.com/karpathy/micrograd

- Automatic differentiation in PyTorch
https://openreview.net/pdf?id=BJJsrmfCZ

- Autograd mechanics
https://pytorch.org/docs/stable/notes/autograd.html

===== 参考
https://en.d2l.ai/chapter_multilayer-perceptrons/backprop.html
https://zh.d2l.ai/chapter_multilayer-perceptrons/backprop.html

