:toc:
:toclevels: 6
:hardbreaks-option:

== Attention Mechanisms and Transformers
=== 概念
注意力机制(attention mechanisms)的核心思想是:
在处理一个输入(如一段文本、图像等)时，并不是所有信息都同等重要。
通过赋予输入中不同部分不同的权重(即注意力权重)，模型可以专注于更加重要的部分，从而提高效率和效果。
Transformer的核心创新在于自注意力机制。这允许模型在处理一个词时，能够根据其重要性关注输入序列中的任何其他词，无论距离多远。这极大地克服了早期模型(如RNN)难以处理长距离依赖关系的缺陷，是LLM能够理解复杂上下文和生成连贯长文本的关键。

注意力机制的首次提出（2014年）：
《Neural Machine Translation by Jointly Learning to Align and Translate》: https://arxiv.org/abs/1409.0473
它提出了注意力机制这一概念，使得神经网络能够有选择性地关注输入信息的不同部分。
它有效解决了Seq2Seq模型处理长序列的难题。
该机制后来被广泛应用于自然语言处理、计算机视觉等众多领域，并启发了后续像Transformer这样的革命性架构（其核心“自注意力”即源于此思想）。

优势:
长距离依赖建模: 传统RNN/LSTM难以处理长序列依赖，而注意力机制可以直接建模任意距离的关系。
并行计算: 与RNN的时序计算不同，注意力机制可以并行处理所有位置。
可解释性: 注意力权重可以直观显示模型关注了哪些部分（但权重高不一定等于对预测有因果贡献，见 Jain & Wallace 2019《Attention is not Explanation》）。

典型应用:
Transformer模型: 完全基于自注意力机制，取代了传统的RNN/CNN结构（如BERT、GPT）。
机器翻译: 解码器动态关注编码器的不同部分（如原始论文Attention Is All You Need）。
图像分类: 将图像分块后通过注意力处理。
语音识别: 关注音频序列的关键片段。

* 注意力机制分类 (Taxonomy of Attention Types)
    ** 软注意力 (Soft Attention)：
        完全可微，可以通过标准的反向传播进行训练。包括：
        *** 通道注意力 (Channel Attention)：如SENet中的Squeeze-and-Excitation模块。
        *** 空间注意力 (Spatial Attention)：关注特征图中“哪里”是重要的区域。
        *** 自注意力 (Self-Attention)：Transformer的基础，用于捕捉序列或图像内不同元素之间的关系。
        *** 多模态注意力 (Multi-Modal Attention)：处理来自不同模态（如图像和文本）的数据。
    ** 硬注意力 (Hard Attention)：
        涉及随机选择或预测输入中的特定区域，通常是不可微的，需要使用强化学习等技术进行训练。它包括基于强化学习、统计方法等的变体。
    ** 参考：
        *** https://arxiv.org/abs/2204.07756 (2024)
        *** https://zhuanlan.zhihu.com/p/696136220

参考：
https://zhuanlan.zhihu.com/p/104393915
https://www.zhihu.com/question/68482809

=== Queries, Keys, and Values
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/queries-keys-values.html
https://zh.d2l.ai/chapter_attention-mechanisms/attention-cues.html

在 Transformer 模型提出之前，对序列 X 的常规编码方式是通过循环神经网络（RNNs）或卷积神经网络（CNNs）来进行。

循环神经网络（例如 LSTM）的方案很简单，每一个词 x_t 对应的编码结果 y_t都基于这个词前面一个词的表示获得，通过递归地计算得到（也就是基于词的上文信息来计算词嵌入）：
y_t = f(y_{t-1}, x_t)
循环神经网络的序列建模方式虽然与人类阅读类似，但是递归的结构导致其无法并行计算，在处理长序列时还可能会出现梯度爆炸或梯度消失问题，而且循环神经网络的隐状态只携带有限的历史信息（马尔可夫性质），难以学习到全局的结构信息。

卷积神经网络则运用滑动窗口基于局部上下文来编码文本，例如核尺寸为 3 的卷积操作就是使用每一个词自身以及前一个和后一个词来生成该词的语义嵌入：
y_t = f(x_{t-1}, x_t, x_{t+1})
卷积神经网络能够并行计算，因此速度很快，但是由于是通过窗口来进行编码，所以更侧重于捕获局部信息（窗口内词元之间的交互），难以建模远距离词之间的依赖。

2017 年 Google 在《Attention is All You Need》中提供了第三个方案：直接使用注意力机制编码整个文本。相比循环神经网络要逐步递归才能获得全局信息（因此一般使用双向 RNN），而卷积神经网络只能获取局部信息，需要通过堆叠多层来增大感受野，注意力机制一步到位获取了全局信息：
y_t = f(x_t, A, B)
其中 A, B 是另外的词嵌入序列（矩阵），如果取 A = B = X 就称为自注意力（Self-Attention），即直接将 x_t 与自身序列中的每个词进行比较，最后算出y_t。

https://stats.stackexchange.com/questions/421935/what-exactly-are-keys-queries-and-values-in-attention-mechanisms

可视化：
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/queries-keys-values.html#visualization

By design, the attention mechanism provides a differentiable means of control by which a neural network can select elements from a set and to construct an associated weighted sum over representations.
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/queries-keys-values.html#summary

=== Attention Pooling by Similarity(基于相似度的注意力池化)
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/attention-pooling.html

Nadaraya–Watson kernel regression is an early precursor of the current attention mechanisms. It can be used directly with little to no training or tuning, either for classification or regression. The attention weight is assigned according to the similarity (or distance) between query and key, and according to how many similar observations are available.

从 NW 回归到现代 Attention 的演进：
深度学习通过引入可训练的线性变换（将原始的 x 和 y 投影为 Query、Key、Value 矩阵），解决了 NW 回归的灵活性不足（只能使用原始特征）和高维失效的问题。通过大规模并行计算（矩阵乘法）和模型设计（如位置编码、多头注意力），解决了 NW 回归的计算效率低下问题。通过端到端的训练，自动学习出最适合当前任务的“相似度度量”（即QKᵀ），替代了人工选择核函数和带宽的繁琐过程。

=== Attention Scoring Functions
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/attention-scoring-functions.html
https://zh.d2l.ai/chapter_attention-mechanisms/attention-scoring-functions.html

As it turns out, distance functions are slightly more expensive to compute than dot products. 怎么理解？
距离函数需要更多种类的运算（减法、平方、开平方），且不利于硬件的并行加速，而点积只需要乘法和加法，硬件支持最好、速度最快。

Q: 为什么是点积？
1. 性能：矩阵乘法
点积在计算机底层被实现为 GEMM（通用矩阵乘法）。这是所有 GPU 和 TPU 优化得最彻底的操作。
并行吞吐：相比于加法注意力需要逐元素操作或多层感知机（MLP），点积可以将整个序列的 Q 和 K 一次性打包计算，吞吐量极大。
2. 投影与相似度
方向 + 长度：点积不仅考虑了方向（余弦相似度），还保留了向量的模长信息。这意味着在特征空间中，模长更大的特征能获得更高的原始得分。
3. 与 softmax 的配合：产生尖锐分布
注意力需要将点积分数转化为概率（softmax）。
点积的结果范围可在 (−∞,+∞) 之间，通过 softmax 的指数函数能够放大差异，使模型更容易聚焦于少数位置（尖锐注意力分布）。
如果使用余弦相似度（值域 [−1,1]），softmax 输出会趋向均匀，难以产生强烈选择性。
4. 训练稳定性：简单的缩放控制
维度纠偏：由于点积的方差随维度 dₖ 线性增长（导致值域进入 Softmax 的梯度饱和区），只需要除以根号dₖ，就能把数值分布拉回平衡点，将方差拉回 1，让训练更稳定。这其实是点积的特殊性质——它的方差随维度线性增长，加性注意力则没有这个顾虑，但点积通过简单缩放就解决了问题。

Q1: 为什么 −1/2∥q∥² 可以省略？
Q2: 为什么 −1/2∥kᵢ∥² 可以省略？
核心原理：Softmax 的常数不变性，即对 Softmax 函数的输入同时加上或减去同一个常数，Softmax 的输出结果不变。
对于Q1, 对于同一个查询 q, 在计算它对所有键 k1,k2,... 的注意力分数时，−1/2∥q∥²这一项是固定不变的常数。
直观理解：查询自己的“长度”信息对比较不同键的相对重要性没有帮助，就像在比较两个人谁更高时，加上一个相同的基准身高（比如都减去1米）不影响比较结果。
对于Q2, 这个省略有条件：需要所有键 kᵢ 的范数 ∥kᵢ∥ 是恒定或有界的：
在 Transformer 中，键向量通常来自层归一化 (Layer Normalization) 的输出。层归一化会让向量的均值和方差稳定，导致所有键的范数非常接近一个常数（或者变化很小）。
如果 ∥kᵢ∥ 是常数，那么再次利用 Softmax 的常数不变性，这一项也可以完全省略。
即使范数不是绝对恒定，但只要变化相对于点积项 qᵀkᵢ 很小，影响也有限。实际上，现代 Transformer 的设计（加上层归一化）就是为了让这种省略成为合理的近似。
直观理解：如果所有键的“长度”都差不多，那么比较它们与查询的相关性时，主要就看它们的方向（即点积），长度的影响可以被忽略。

- 点积注意力(dot-product-attention)
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/attention-scoring-functions.html#dot-product-attention

- 缩放点积注意力(scaled-dot-product-attention)
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/attention-scoring-functions.html#scaled-dot-product-attention
Q: 为什么要缩放？
Q: 缩放因子是多少？
Q: 为什么使用维度的根号来缩放？
Q: 为什么在其他 softmax 的应用场景，不需要做 scaled?
参考：
https://www.zhihu.com/question/339723385
《Attention Is All You Need》

- 加性注意力(additive-attention)
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/attention-scoring-functions.html#additive-attention
不需要Q和K维度相同
适合多模态任务（文本+图像、文本+音频等）
适合处理不同来源的特征
通过tanh非线性激活函数
可学习的权重矩阵W_q、W_k和v
能捕获查询和键之间的复杂非线性关系
理论上更通用
缺点：效率低、参数量大、优化难，因此工业应用还是首选点积注意力（效率优先）

当查询和键是不同长度的矢量时，可以使用可加性注意力评分函数。
当它们的长度相同时，使用缩放的“点－积”注意力评分函数的计算效率更高。

Q: 点积操作要求查询和键具有相同的长度。如果查询(Q)和键(K)维度不相同，怎么办？

=== The Bahdanau Attention Mechanism(Bahdanau注意力)
Bahdanau 注意力是加性注意力（additive attention）评分函数在 RNN 编码器-解码器架构中的经典实现，最早用于神经机器翻译。
https://arxiv.org/abs/1409.0473 — Bahdanau, Cho, & Bengio, ICLR 2015

==== 解决的问题
传统的编码器-解码器模型在解码每个输出词元时，都使用相同的上下文变量（即编码器最后一个时间步的隐状态），无法区分输入序列中不同部分的重要性。
核心改进：在每个解码时间步，模型会动态地计算一个不同的上下文变量。它通过注意力机制，将当前解码状态与所有编码器隐状态进行对齐，从而选择性地关注输入序列中与当前预测最相关的部分。
Bahdanau注意力通过将上下文变量视为加性注意力池化的输出，实现了对输入序列的选择性关注。

==== 评分函数
ifndef::env-github[]
[stem]
++++
e_{ij} = \mathbf{v}^\top \tanh(\mathbf{W}_q \mathbf{s}_{i-1} + \mathbf{W}_k \mathbf{h}_j)
++++
endif::[]
ifdef::env-github[]
$$
e_{ij} = \mathbf{v}^\top \tanh(\mathbf{W}_q \mathbf{s}_{i-1} + \mathbf{W}_k \mathbf{h}_j)
$$
endif::[]

其中：
ifndef::env-github[]
- stem:[s_{i-1}] 是解码器上一时间步的隐状态
- stem:[h_j] 是编码器第 j 个隐状态（原始论文中为双向 RNN 的前向+后向拼接）
- stem:[\mathbf{W}_q, \mathbf{W}_k, \mathbf{v}] 是可训练参数
endif::[]
ifdef::env-github[]
- $s_{i-1}$ 是解码器上一时间步的隐状态
- $h_j$ 是编码器第 j 个隐状态（原始论文中为双向 RNN 的前向+后向拼接）
- $\mathbf{W}_q, \mathbf{W}_k, \mathbf{v}$ 是可训练参数
endif::[]

注意力权重：
ifndef::env-github[]
[stem]
++++
\alpha_{ij} = \frac{\exp(e_{ij})}{\sum_{k=1}^{T} \exp(e_{ik})}
++++
endif::[]
ifdef::env-github[]
$$
\alpha_{ij} = \frac{\exp(e_{ij})}{\sum_{k=1}^{T} \exp(e_{ik})}
$$
endif::[]

上下文向量（作为加性注意力池化的输出）：
ifndef::env-github[]
[stem]
++++
\mathbf{c}_i = \sum_{j=1}^{T} \alpha_{ij} \mathbf{h}_j
++++
endif::[]
ifdef::env-github[]
$$
\mathbf{c}_i = \sum_{j=1}^{T} \alpha_{ij} \mathbf{h}_j
$$
endif::[]

ifndef::env-github[]
然后将 stem:[\mathbf{c}_i] 与解码器当前状态 stem:[\mathbf{s}_i] 拼接后预测第 i 个目标词元。
endif::[]
ifdef::env-github[]
然后将 $\mathbf{c}_i$ 与解码器当前状态 $\mathbf{s}_i$ 拼接后预测第 $i$ 个目标词元。
endif::[]

==== 关键理解
本质上是编码器-解码器间的交叉注意力。解码器每一步都会查询编码器的所有隐藏状态。它并不对源句或目标句自身内部做注意力。
或者可以这么说，Bahdanau Attention 是 Transformer 架构中 Cross-Attention 这一模块的前身，而不包含 Self-Attention 的功能。

[cols="1,2,2", options="header"]
|===
| 任务    | RNN + Bahdanau Attention    | Transformer
| 理解源句内部关系(例如：指代消歧)  | RNN (循环)靠隐藏状态一步步传递信息。 | Self-Attention源句内部互相 Attention。
| 理解目标句内部关系(例如：语法连贯) | RNN (循环)靠解码器的隐藏状态传递。| Masked Self-Attention目标句内部互相 Attention。
| 连接源句与目标句(例如：翻译对齐) | Bahdanau Attention(仅负责这一步) | Cross-Attention(Encoder-Decoder Attention)
|===

==== 特性辨析
1. 它是 Cross-Attention 吗？
YES
机制：它的计算涉及两个不同的序列。Query 来自解码器（目标句），Key/Value 来自编码器（源句）。
目的：解决的是”翻译对齐”问题（Alignment），即”生成这个英文单词时，应该看哪一个中文单词”。

2. 解码器每一步都查询所有状态？
YES
全量扫描：Bahdanau 使用上一时间步的解码器隐状态
ifndef::env-github[]
stem:[s_{i-1}] 计算对齐分数（这与 Luong Attention 直接使用当前时间步 stem:[s_i] 不同），然后与编码器所有隐藏状态 stem:[(h_1, h_2, ..., h_T)] 计算分数。
endif::[]
ifdef::env-github[]
$s_{i-1}$ 计算对齐分数（这与 Luong Attention 直接使用当前时间步 $s_i$ 不同），然后与编码器所有隐藏状态 $(h_1, h_2, ..., h_T)$ 计算分数。
endif::[]
瓶颈：推理速度随源句子长度线性增加 O(T)。

3. 它不做源句内部的注意力？
YES
这是它与 Transformer 最根本的区别：
Bahdanau 模型中：源句子内部的语义理解（比如”猫”是主语，”吃”是谓语）完全依赖 RNN/LSTM 的循环结构。
信息通过时间步
ifndef::env-github[]
stem:[h_{t-1} \to h_t] 传递，而不是通过 Attention 互相关注。
endif::[]
ifdef::env-github[]
$h_{t-1} \to h_t$ 传递，而不是通过 Attention 互相关注。
endif::[]
Transformer 中：引入了 Self-Attention，让源句子的词可以”看”源句子的其他词，从而替代了 RNN 的职能。

==== 参考
https://arxiv.org/abs/1409.0473
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/bahdanau-attention.html
https://zh.d2l.ai/chapter_attention-mechanisms/bahdanau-attention.html

=== Multi-Head Attention(多头注意力)
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/multihead-attention.html
https://zh.d2l.ai/chapter_attention-mechanisms/multihead-attention.html

多头注意力（Multi-Head Attention, MHA）是 Transformer 的核心组件。
它通过并行运行多个独立的注意力机制，让模型能同时从不同的“角度”（子空间）捕捉输入信息之间的复杂联系。

instead of performing a single attention pooling, queries, keys, and values can be transformed with independently learned linear projections. Then these projected queries, keys, and values are fed into attention pooling in parallel. In the end, attention-pooling outputs are concatenated and transformed with another learned linear projection to produce the final output. This design is called multi-head attention, where each of the attention pooling outputs is a head. 

优点:
更丰富的表示：捕获不同类型的关系
训练更稳定：与单头注意力相比，降低了方差
并行处理：所有头可以同时计算
可解释性：不同的头通常能学习到可解释的模式

Multi-head attention combines knowledge of the same attention pooling via different representation subspaces of queries, keys, and values. To compute multiple heads of multi-head attention in parallel, proper tensor manipulation is needed.

在主流大模型（如 GPT-3、LLaMA、Qwen、DeepSeek）的 多头注意力 机制中：
每个头的 Query、Key、Value 向量的维度是相等的，且都用 dhead表示。
即：dq=dk=dv=dhead
早期 小模型	64
    BERT‑Base (768/12=64)
    GPT‑2 Small (768/12=64)
当代大模型	128
    GPT‑3 175B (12288/96=128)
    LLaMA 7B (4096/32=128)
    Qwen 72B (8192/64=128)
注: DeepSeek‑V3 等使用 MLA 的模型不适用此表——Q/K 经低秩压缩，每头维度的算法不同（且 7168/128=56 ≠ 128）。

为什么 dq=dk=dv ？
数学要求：注意力权重由 Q⋅Kᵀ 计算，要求 dq=dk 才能做内积。
工程简化：保持 dv 与之相同，可以统一矩阵形状，便于批量计算和硬件加速（GPU Tensor Core 对 64/128 等维度友好）。
事实标准：绝大多数开源实现（PyTorch、HuggingFace Transformers）均采用此设计。

=== Self-Attention and Positional Encoding(自注意力与位置编码)
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/self-attention-and-positional-encoding.html
https://zh.d2l.ai/chapter_attention-mechanisms/self-attention-and-positional-encoding.html

==== Self-Attention(自注意力)
自注意力（self-attention）：定义了数据来源，它指查询、键、值全部来源于同一个输入序列。
也被称为内部注意力（intra-attention）。

在自注意力机制中，查询、键和值都来自同一个来源。CNN 和自注意力机制都享有并行计算的优势，且自注意力具有最短的最大路径长度。然而，自注意力机制的计算复杂度与序列长度呈二次方关系，这使得它在处理非常长的序列时速度过慢。为了利用序列顺序信息，可以通过向输入表示中添加位置编码，来注入绝对或相对的位置信息。

自注意力之所以叫“自”，是因为：
没有外部参考：它只在自己的句子内部“反省”。
上下文重构：它把每个词从孤立的状态，变成了带有上下文语境的状态。

Q: 如何理解自注意力具有最短的最大路径长度？
最大路径长度指的是信息在模型中传递所需经过的“步数”或“距离”。这个距离越短，信息越不容易丢失或衰减，长距离依赖关系就越容易捕捉。
在RNN中：
数据是按时间步一个一个依次输入的。如果第一个词要和最后一个词建立联系，信息必须沿着时间轴一步一步传递，经过 n 个时间步。
最大路径长度=O(n)
这就像一条很长的流水线，头尾交流很困难，容易发生梯度消失或记忆衰减。
在自注意力中：
序列中的所有位置是两两直接相连的。无论两个词相隔多远，在计算注意力权重时，它们都可以直接“看到”对方，不需要经过中间任何词。
最大路径长度 = O(1)
这意味着自注意力在理论上没有长距离依赖问题，信息传递的路径长度是恒定的。

Q: 如何理解自注意力的并行计算的优势？
自注意力的核心计算是 Query、Key、Value 矩阵乘法。给定一个长度为 n 的序列，所有位置的 Query 可以一次性与所有位置的 Key 进行点积，形成注意力矩阵。这个过程不依赖上一个位置的结果，因此整个序列的计算可以同时进行。

在自注意力出现之前，主流序列模型往往只能占一头：
模型	最大路径长度	并行度
CNN    O(n/k) (需要堆叠多层)    高 (并行)
RNN    O(n) (长距离依赖难)      低 (串行)

以前在RNN、CNN模型中其实都出现过Position Embedding，但在那些模型中，Position Embedding是锦上添花的辅助手段，也就是“有它会更好、没它也就差一点点”，因为RNN、CNN本身就能捕捉到位置信息。

因果注意力（Causal Attention）：
也常被称为单向注意力（Unidirectional Attention）或掩码自注意力（Masked Self-Attention），是 Transformer 架构中用于自回归生成（如大语言模型文本生成）的核心机制——即在预测下一个 token 时，只能依赖已经生成的历史 token，而无法“看到”未来的 token。这一机制通过上三角掩码（将未来位置的注意力分数设为 −∞）来保证训练与推理的一致性，是当前主流大语言模型能够逐词生成连贯文本的关键设计。
因果注意力在 softmax 之前应用掩码：
M_ij = 0 (j ≤ i) 或 -∞ (j > i)
经过 softmax 后，每个位置 i 只能看到位置 ≤ i 的 token，保证训练与推理行为一致。

与CNN相比，卷积神经网络和自注意力都拥有并行计算的优势，而且自注意力的最大路径长度最短。但是因为其计算复杂度是关于序列长度的二次方，所以在很长的序列中计算会非常慢。

模型	         每层复杂度	    顺序操作数	    最大路径长度
Self-Attention	  O(n²·d)	      O(1)	          O(1)
RNN	              O(n·d²)	      O(n)	          O(n)
CNN	              O(k·n·d²)	      O(1)	          O(n/k)
n=序列长度, d=维度, k=卷积核大小

==== Positional Encoding(位置编码)
both CNNs and self-attention enjoy parallel computation and self-attention has the shortest maximum path length. However, the quadratic computational complexity with respect to the sequence length makes self-attention prohibitively slow for very long sequences.
在处理词元序列时，循环神经网络是逐个的重复地处理词元的， 而自注意力则因为并行计算而放弃了顺序操作。为了使用序列的顺序信息，通过在输入表示中添加位置编码（positional encoding）来注入绝对的或相对的位置信息。位置编码可以通过学习得到也可以直接固定得到。

===== 绝对位置编码 vs 相对位置编码
绝对位置编码：每个位置有固定或可学习的向量（如 Sinusoidal、BERT 的 Learned Embedding），不随内容变化。
相对位置编码：注意力计算时考虑"位置差"而非"绝对位置"，外推性更好（如 T5 偏置、Transformer-XL）。
旋转位置编码（RoPE）：将位置信息旋转到 Query 和 Key 的复数空间，兼具相对位置感知和良好的外推性，是当前 LLM 的事实标准。

===== RoPE（Rotary Position Embedding）
RoPE(Rotary Position Embedding，旋转位置编码)
核心思想：通过旋转矩阵对 Q 和 K 进行变换，使内积 Q·Kᵀ 自然编码 token 间的相对位置。
数学形式：将 d 维向量拆成 d/2 个二维平面，每个平面按不同角度旋转（角度随位置线性增长 θᵢ = base^{-2i/d}）。
优势：
- 相对位置编码：不会像绝对位置编码那样让长距离注意力消失
- 良好的外推性：可以扩展到训练时未见过的长度（如从 4K 外推到 32K）
- 无额外参数：不增加模型参数量

===== 位置编码的演进路线
Sinusoidal (Transformer, 2017) → Learned (BERT, 2018) → Relative (Transformer-XL, 2019) → RoPE (LLaMA/GPT-NeoX, 2021~2022) → ALiBi / NoPE (2022~2024)
- ALiBi：不学位置编码，给注意力分数加一个随距离线性增加的偏置
- NoPE：有研究发现无位置编码 + 因果掩码，模型能自发学到位置信息

===== 三种注意力对比
类型	                Q来源	    K,V来源	    掩码	    用途
Self-Attention	        自身	    自身	      无	    Encoder 双向上下文
Masked Self-Attention	自身	    自身	      因果掩码	Decoder 自回归生成
Cross-Attention	        Decoder	 Encoder	  无	    Encoder-Decoder 连接

=== 注意力的实现
参考：
https://zhuanlan.zhihu.com/p/366592542

=== The Transformer Architecture
==== 概览
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/transformer.html
https://zh.d2l.ai/chapter_attention-mechanisms/transformer.html

Q: 在实验中训练更深的Transformer将如何影响训练速度和翻译效果？
翻译质量(BLEU)	  显著提升	  深层 Encoder 对翻译质量的贡献通常大于 Decoder。
收敛速度	       变快	     在同等数据和计算资源下，深层模型往往能更快达到目标精度。
推理/解码延迟	   变慢	     除非使用模型压缩技术，否则推理时的计算量与层数呈线性相关。
训练稳定性	       降低	     需要额外的初始化策略或归一化手段来防止梯度问题。
https://arxiv.org/abs/2008.07772

Q: 在Transformer中使用加性注意力取代缩放点积注意力是不是个好办法？为什么？
在 Transformer 架构中，用加性注意力（Additive Attention）取代缩放点积注意力（Scaled Dot-Product Attention）通常不是个好选择。
在 Transformer 原论文《Attention Is All You Need》中，作者实际上做过对比实验：
ifndef::env-github[]
"We suspect that for larger values of stem:[d_k], the dot products grow large in magnitude, pushing the softmax function into regions where it has extremely small gradients. To counteract this effect, we scale the dot products by stem:[1/\sqrt{d_k}]."
endif::[]
ifdef::env-github[]
"We suspect that for larger values of $d_k$, the dot products grow large in magnitude, pushing the softmax function into regions where it has extremely small gradients. To counteract this effect, we scale the dot products by $1/\sqrt{d_k}$."
endif::[]
论文中明确提到，缩放点积注意力在
ifndef::env-github[]
stem:[d_k] 较小时表现与加性注意力相当，但 stem:[d_k] 较大时显著优于加性注意力，且速度更快。
endif::[]
ifdef::env-github[]
$d_k$ 较小时表现与加性注意力相当，但 $d_k$ 较大时显著优于加性注意力，且速度更快。
endif::[]
1. 计算效率的巨大差异（最关键原因）
缩放点积注意力：主要依靠矩阵乘法（MatMul）。现代硬件（GPU/TPU）针对矩阵乘法进行了极度的优化，可以高度并行化计算。
加性注意力：需要通过一个前馈神经网络（线性层 + 激活函数）来计算得分。这种方式无法像矩阵乘法那样在底层硬件上实现超大规模的并行，计算速度显著更慢。
2. 内存与复杂度
空间复杂度：加性注意力引入了额外的权重矩阵（用于变换 Q 和 K 再求和），这增加了模型的参数量。
维度扩展性：随着特征维度 d 的增加，点积注意力的复杂度增长比加性注意力更受控。
3. 训练稳定性
缩放因子（Scaling）：点积注意力虽然在维度很大时容易产生极大的数值（导致 Softmax 梯度消失），但 Transformer 通过除以根号下 dₖ 解决了这个问题。
成熟度：现有的深度学习框架和预训练模型都是围绕点积注意力优化的。改用加性注意力往往无法带来显著的精度提升，反而会拖累训练效率。

Q: 对于语言模型，应该使用Transformer的编码器还是解码器，或者两者都用？如何设计？
编码器-only 适合理解，解码器-only 适合生成，编码器-解码器适合转换。当前大模型趋势是向解码器-only 收敛，但具体选择仍需根据任务类型、数据规模和资源约束来决定。
1. Encoder-only（仅编码器）
核心机制：采用双向自注意力（Bidirectional Self-Attention）。模型在处理当前词时，可以同时看到“过去”和“未来”的上下文。
适用场景：自然语言理解 (NLU)。如文本分类、命名实体识别 (NER)、情感分析。
典型模型：BERT、RoBERTa。
局限性：由于预训练任务通常是“填空”（掩码语言模型 MLM），它不具备自回归生成长文本的能力。
2. Decoder-only（仅解码器）
核心机制：采用单向自注意力（Causal/Masked Self-Attention）。模型只能看到当前词左侧的信息，通过“预测下一个词”进行自回归生成。
适用场景：自然语言生成 (NLG)。如对话机器人、创意写作、代码补全。
典型模型：GPT 系列 (GPT-3, GPT-4)、Llama、Mistral。
优势：在海量数据下展现出极强的零样本迁移能力（Zero-shot）和涌现能力（Emergent Abilities）。
3. Encoder-Decoder（编码器-解码器）
核心机制：左边负责理解输入，右边负责根据输入生成输出。通过交叉注意力（Cross-Attention）连接两者。
适用场景：序列到序列 (Seq2Seq) 转换。如机器翻译、文本摘要、语法纠错。
典型模型：T5、BART。

Q: 为什么主流LLM都是Decoder-only的架构？
虽然理论上 Encoder-Decoder 在翻译等任务上更优雅，但目前主流 LLM 几乎全员转向 Decoder-only，主要原因如下：
训练效率与规模化：Decoder-only 架构在超大规模参数和数据上的表现更稳定，且由于省去了 Cross-Attention，计算效率在极大规模下更具优势。
零样本泛化：研究发现，统一预测下一个词（Next Token Prediction）这一简单的预训练目标，足以让模型通过指令微调（SFT）学会各种理解任务，从而取代了 Encoder-only 的地位。
理论视角：有研究指出，Encoder 的双向注意力可能存在“低秩”问题，削弱模型的表达能力；而在同等推理成本下，Decoder-only 往往是更优的选择。
https://kexue.fm/archives/9529

Q: Encoder的双向注意力会存在低秩问题，如何理解？
Encoder 的双向注意力矩阵由于 softmax 的饱和和全局对称交互，导致其行向量极易线性相关 → 低秩 → 表达瓶颈；而 Decoder 的因果 mask 强制产生下三角结构 → 满秩 → 表达能力更强，更适合生成任务。
1. 什么是“低秩问题”？
在 Transformer 中，自注意力机制计算出一个 注意力权重矩阵 A∈Rᴺˣᴺ，其中 N 是序列长度。
矩阵的 秩 可以理解为它所包含的独立线性信息的维度数量。
满秩矩阵：秩 = N，意味着矩阵的行/列之间线性无关，能表达最多样化的注意力模式。
低秩矩阵：秩远小于 N，意味着很多行（或列）可以由其他行线性组合得到，信息有冗余或压缩，表达能力受限。
低秩问题就是指：双向注意力的权重矩阵天然倾向于低秩，从而限制了模型对复杂依赖关系的建模能力。
2. 为什么双向注意力会产生低秩矩阵？
双向注意力允许每个 token 与序列中所有 token（包括前后的）计算相似度。经过 softmax 之后，注意力矩阵 A 具有如下特点：
每一行都是非负的，且和为 1（概率分布）。
矩阵由 QKᵀ经过 softmax 得到。
实验和理论（如 Dong et al., 2021; “On the Expressive Power of Transformers”）发现，这种结构会导致：
softmax 的饱和效应：当某些相似度分数远大于其他时，softmax 输出接近 one-hot 向量。不同的 one-hot 向量本身线性无关，但大量行的注意力分布集中到少数相同位置时会高度相似（近似重复），使矩阵的有效秩降低。
全局交互的冗余性：双向注意相当于对所有位置“一视同仁”地聚合信息。在长序列中，许多位置对信息聚合的贡献是相似的（例如句中的虚词），导致注意力权重的行之间存在线性相关性。
理论下界：有工作证明，在无需位置编码或常见初始化下，双向注意力的有效秩（effective rank）会随着层数增加而衰减，最终受限于每头的维度（head dimension）。
作为对比，Decoder 的因果注意力强制加入上三角 mask（未来 token 不可见），得到的矩阵是下三角且主对角线元素恒为正（每个位置至少看到自己）。下三角且对角元非零的矩阵必满秩（秩 = N），因此理论上低秩问题被自然消除。
3. 低秩的后果
表达能力下降：模型无法为每个 query 生成独特的注意力分布，很多 query 会得到相似的聚合结果 → 对复杂的位置关系、长距离依赖建模能力降低。
深度退化：堆叠多层双向注意力时，低秩效应累积，导致模型输出空间的秩迅速下降（称为“秩崩塌”），使模型表达能力趋近于线性模型。
对比验证：一些工作（如 “Attention is Not All You Need” (Dong et al., NeurIPS 2021)）实验证明，在相同参数量下，绕过低秩瓶颈（例如使用 softmax-free 注意力或分块因果结构）可以提升性能。
4. 为什么 Decoder-only 没有这个问题？
Decoder-only 使用了因果 mask：Aij=0 for j>i
同时保持自回归的自注意力（加上 softmax 按行归一化）。结果矩阵是单位下三角（近似），自然满秩。
满秩意味着每个位置可以学习到与前序历史完全不同的组合模式，不会出现“所有行的注意力分布都一样”的退化。

Q: Computational Scaling? Memory Bandwidth Requirements? 涉及多头注意力（Multi-Head Attention, MHA）呢？

Q: 如果输入序列很长，Transformer会面临什么挑战？为什么？
1. 计算量爆炸（算力挑战）
在自注意力机制中，序列中的每个词都需要与序列中所有其他词计算点积得分，也就是说，计算量与序列长度 N 的平方成正比。这将导致长文本的训练和推理变得极慢。
2. 显存瓶颈（内存挑战）
为了计算注意力分数，模型需要存储一个大小为 N x N 的 注意力矩阵，显存占用同样随 N 的平方增长。
3. 远程依赖捕获（效果挑战）
虽然理论上 Transformer 具有“全域视野”，但随着序列变长，位置编码的有效性会下降。模型可能难以准确关联相距极远的两个词，导致长文档的逻辑一致性变差，出现“顾头不顾尾”的现象。
小结：
Transformer处理长序列面临的核心挑战是自注意力的二次复杂度，导致计算和显存随序列长度平方增长；同时存在位置编码外推困难、长距离依赖捕获失效和KV缓存线性增长等衍生问题。这些问题根源于Transformer"每个token关注所有token"的全连接设计理念，需要通过稀疏化、线性化或架构创新来突破。

Q: 如何提高Transformer的计算速度和内存使用效率？
1. 优化注意力机制 (Attention Optimization)
FlashAttention：目前最标准的方法。它通过 IO 感知 (IO-Awareness) 减少了 GPU 显存与 SRAM 之间的读写次数，不仅速度极快，还显著降低了内存占用。
稀疏注意力 (Sparse Attention)：如 Longformer 或 BigBird，只计算部分点积（如滑动窗口注意力），将复杂度降为 O(N) 。
多查询注意力 (MQA) / 分组查询注意力 (GQA)：减少 KV 缓存（KV Cache）的大小。Llama 3 等大模型普遍使用 GQA，在保持效果的同时极大提升了推理吞吐量。
2. 算子融合与并行化 (Computation & Parallelism)
算子融合 (Operator Fusion)：将 LayerNorm、激活函数等多个小操作合并为一个 GPU 核函数执行，减少数据往返显存的开销。
张量并行 (Tensor Parallelism)：将大的矩阵乘法拆分到多张显卡上同时计算（如 Megatron-LM 方案）。
3. 内存节省技术 (Memory Efficiency)
混合精度训练 (Mixed Precision / FP16/BF16)：使用 16 位浮点数代替 32 位。不仅内存占用减半，还能利用 NVIDIA GPU 的 Tensor Cores 获得数倍加速。
梯度检查点 (Gradient Checkpointing)：用时间换空间。在反向传播时不存储中间激活值，而是重新计算，能显著降低训练长序列时的显存峰值。
量化 (Quantization)：推理时使用 INT8 甚至 INT4 格式，大幅压缩模型体积。
4. 数据处理与推理加速
KV Cache：在推理生成时，缓存之前计算过的 Key 和 Value 向量，避免重复计算。
连续批处理 (Continuous Batching)：动态处理不同长度的请求，提高 GPU 利用率。

Q: 如何理解transformer？
https://www.zhihu.com/question/596771388

==== model
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/transformer.html#model
https://zh.d2l.ai/chapter_attention-mechanisms/transformer.html#id3

==== Positionwise Feed-Forward Networks(基于位置的前馈网络)
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/transformer.html#positionwise-feed-forward-networks
https://zh.d2l.ai/chapter_attention-mechanisms/transformer.html#id5

- Q: Feed Forward的作用？
* 没有 FFN，Transformer 就相当于一个“记忆力很强但完全没有推理能力”的系统——能注意到该看哪里，却无法思考看到的信息意味着什么。主要损失表现为：
* 跨特征维度的交互：
    注意力在“位置（token）”维度做混合，FFN 在“特征通道”维度做混合。没有 FFN，模型就缺少了特征之间的交互能力。
* “记忆型”知识的存储：
    大量研究表明 FFN 的权重像一个键值存储器，存储了大量事实和语言模式（Geva et al., 2021, “Transformer Feed-Forward Layers Are Key-Value Memories”）。去掉 FFN，模型就失去了主要的静态知识存储库。
* 归纳偏置的平衡：
    注意力更适合建模“实例级别的交互”，FFN 更适合建模“特征级别的模式”，两个互补。
* https://arxiv.org/abs/2012.14913

- 论文《Transformer Feed-Forward Layers Are Key-Value Memories》
1. 第一层线性层（Key）：模式识别
FFN 的第一步是将向量从 512 维映射到 2048 维。
可以把它想象成： 2048 个“感应器”。
原理： 每一个神经元（Key）都代表了一种特定的模式。当输入向量（比如“Paris”和“Capital”的混合体）进入时，它会去匹配这 2048 个感应器。
结果： 比如第 1024 号神经元对“地理位置+职能”非常敏感，它就会被激活（点亮）。这就是“Key”的匹配过程，它在问：“当前的语境里，有没有我负责的那个知识点？”
2. 第二层线性层（Value）：知识注入
第二步是将 2048 维映射回 512 维。
可以把它想象成： 2048 条“知识补丁”。
原理： 每一个“感应器”被点亮后，都会释放出对应的“Value”。如果 1024 号神经元被激活了，它就会吐出一段代表“法国、中心城市、埃菲尔铁塔”这种含义的向量。
结果： 这个“知识补丁”会通过加法（残差连接）贴回到原来的向量上。
3. 为什么只有它能存“事实”？
Attention 也是矩阵乘法，为什么不能存？
Attention 的权重是“动态”生成的： 每次输入不同的句子，Attention 的权重矩阵（那个 A = Softmax(QKᵀ)）都会变。它更像是一个“临场反应”，根据当前句子临时决定看哪儿。
FFN 的权重是“静态”固定的： FFN 的 W₁ 和 W₂ 在模型训练好之后就刻在“骨子里”了，不管输入什么，它都在那里等着匹配。
结论： 这种固定的、不随输入改变的参数，才是存放“常识”（如：1+1=2，巴黎是法国首都）最理想的地方。
4. 论文中的直观证据
Mor Geva 在那篇经典的论文里通过实验展示了这一点：
他们发现 FFN 的底层（Lower Layers）通常存的是浅层模式（比如：如果看到“super-”，后面可能接“man”）。
而高层（Upper Layers）存的是语义事实（比如：看到“The capital of [X] is”，FFN 就会在隐藏层里激活对应的“首都神经元”并输出结果）。
总结：
没有 FFN，Transformer 就成了一个“没背过书的学霸”：
他逻辑分析能力极强（Attention 能完美理清句子里的代词指代、长难句结构），但他脑子里没有任何知识储备（FFN 被删了）。如果你问他“法国的首都是哪？”，他能听懂你在问地理，但由于“脑硬盘”空了，他给不出“巴黎”这个具体的答案。
这就是为什么说 FFN 是 “知识库”。

- 为什么transformer的FFN需要先升维再降维？
https://www.zhihu.com/question/665731716/answer/1891083475861370050
FFN(x)=max(0, xW₁+b₁)·W₂+b₂：W₁把维度从d升到4d，W₂再从4d降回d。先升维再降维的原因：
1. 让非线性真正起作用：若只有单个线性层，"线性→ReLU→线性"等价于一次线性变换，非线性被抹平，模型退化为线性模型。
必须"升→激活→降"三段式，激活函数夹在两个线性层之间，才构成真正的两层MLP，这是通用近似器的基础结构。
2. 高维空间更易线性分离(Cover定理)：把数据投影到更高维空间后，原本线性不可分的模式往往变得线性可分(类似核方法)。
升到4d给网络足够的算力空间做非线性切割，每个隐藏神经元可专门检测某种特征；降回d再把结果收拢回主表示空间。
3. 对齐残差连接与层堆叠：每层都要走x+Sublayer(x)，要求Sublayer的输入输出维度都是d_model。
所以FFN内部可以涨到4d去算，但末层必须降回d_model，才能与x相加并顺畅堆叠下一层。
4. 升维层像联想记忆：W₁的每一行像一个键/模式检测器(这个token是否匹配某种特征)，W₂的对应列像一个值(若匹配则贡献什么)。隐藏层宽度直接控制FFN的记忆容量。
5. 与注意力的分工：Attention做跨位置通信(线性地混合不同token)，FFN做逐位置计算(非线性地加工单个token)。二者交替出现，模型才能既"交流"又"加工"。升维再降维正是FFN承担"加工"职能的手段。
6. 为何偏偏是4倍
4x是容量与计算量的经验平衡点。
太小非线性表达力不足，太大参数和算力暴涨易过拟合，性价比下降。
MobileBERT等高效模型会调小该比例来省算力。

==== Residual Connection and Layer Normalization(残差连接和层规范化)
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/transformer.html#residual-connection-and-layer-normalization
https://zh.d2l.ai/chapter_attention-mechanisms/transformer.html#id6

特性	LayerNorm	BatchNorm
归一化方向	横向（跨特征）	纵向（跨样本）
计算单元	每个样本独立	每个特征独立
batch_size 依赖	不依赖	强依赖
适用场景	RNN、Transformer、小batch	CNN、大batch训练
训练/推理	行为一致	行为不同（推理用全局统计量）
LayerNorm：在“特征”维度上进行归一化。它计算每个样本（每一行）的均值和方差。
BatchNorm：在“批次”维度上进行归一化。它计算每个特征（每一列）在所有样本中的均值和方差。

- Q: 为什么 Transformer 倾向于使用 LayerNorm, 而非 BatchNorm?
* 变长序列问题 (Variable Sequence Length)
    Transformer 处理的是变长序列，每个样本的序列长度可能不同：
    batch中3个样本，序列长度分别为3, 5, 4
    sample1: [tok1, tok2, tok3]
    sample2: [tok1, tok2, tok3, tok4, tok5]  
    sample3: [tok1, tok2, tok3, tok4]
    BatchNorm 需要对同一位置的 token 计算统计量，但不同样本的序列长度不一，padding 位置会污染统计量
    LayerNorm 对每个 token 独立归一化，不受序列长度影响
    在 NLP 中，同一个 Batch 里的句子长度通常是不一样的。为了计算，一般会用 [PAD] 符号将短句子补齐。
    BatchNorm 的困境：BN 是纵向计算（跨样本计算均值）。如果一个 Batch 里句子的长度差异很大，BN 在计算序列后半部分的统计量时，会受到大量无效 [PAD] 符号的影响。这会导致计算出的均值和方差不稳定，无法代表真实的数据分布。
    LayerNorm 的优势：LN 是横向计算（在每个单词的特征向量内部计算）。它不关心序列有多长，每个位置的归一化都是相互独立的，因此完全不受句子长度变化的影响。
* 统计量的稳定性 (Statistical Stability)
    BatchNorm 依赖于整个 Batch 的统计特性。在训练时，如果 Batch Size 太小（比如显存受限时），BN 估算的均值和方差偏差会非常大，导致模型难以收敛。
    LayerNorm 的计算只依赖于当前这一个样本。即使 Batch Size 是 1，LN 的行为也完全一致，这种鲁棒性对于参数量巨大的 Transformer 模型至关重要。
* 推理阶段的一致性 (Inference Consistency)
    BatchNorm 在测试阶段需要使用训练时记录下来的“全局移动平均”均值和方差。如果测试数据的分布和训练数据有细微差别，BN 的效果会打折扣。
    LayerNorm 在训练和测试时的计算公式完全一样。这种一致性让 Transformer 在处理各种不同长度和内容的测试文本时表现得更加稳定。
* 总结：
    LayerNorm 的"样本内归一化"特性契合了 Transformer 处理变长序列、依赖样本内部关系、以及需要训练推理一致性的需求。这些优势使 LayerNorm 成为 Transformer 架构的标准配置。

kimi 注意力残差:
https://www.zhihu.com/question/2016993095078684011
https://arxiv.org/abs/2603.15031

==== encoder
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/transformer.html#encoder
https://zh.d2l.ai/chapter_attention-mechanisms/transformer.html#id7

Q: Transformer的编码器与解码器有何不同？

==== decoder
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/transformer.html#decoder
https://zh.d2l.ai/chapter_attention-mechanisms/transformer.html#id8

=== Transformers for Vision
Q: 如果不使用卷积神经网络，如何设计基于Transformer模型的图像分类任务？

Notably, Transformers show better scalability than CNNs: and when training larger models on larger datasets, vision Transformers outperform ResNets by a significant margin. Similar to the landscape of network architecture design in natural language processing, Transformers have also become a game-changer in computer vision.

https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/vision-transformer.html

=== Large-Scale Pretraining with Transformers
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/large-pretraining-transformers.html

- encoder-only
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/large-pretraining-transformers.html#encoder-only

- encoderdecoder
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/large-pretraining-transformers.html#encoderdecoder

- decoder-only
https://en.d2l.ai/chapter_attention-mechanisms-and-transformers/large-pretraining-transformers.html#decoder-only

=== 优化
在保持或提升效果的前提下，系统性降低注意力复杂性、显存占用和计算耗时，同时通过系统、算法、硬件的协同，让大模型训练得起、部署得快、上下文足够长。

==== 注意力机制优化 — 降低 O(n²) 复杂度
标准自注意力随序列长度平方增长，是主要瓶颈。

* 稀疏/局部注意力：只计算部分位置的注意力，如滑动窗口（Longformer）、局部+全局（BigBird）、块稀疏等。

* 线性注意力：将 softmax 核分解，使计算从 O(n²) 降到 O(n)，如 Performer、Linformer、Linear Transformer。

* 硬件感知的精确注意力：FlashAttention 系列，通过分块、重计算、IO优化在 GPU 上加速标准注意力，保持精确计算。

* 多尺度/金字塔注意力：在不同层使用不同分辨率的注意力，兼顾全局与局部。

==== 架构层面的改进
* 归一化层位置：Pre-LN 替代 Post-LN 提升训练稳定性；近年还有 Sandwich-LN、DeepNorm 等改进。

* 激活函数与FFN：SwiGLU、GeGLU 等门控激活替换 ReLU，常带来显著提升。

* 位置编码：RoPE（旋转位置编码）及其外推改进（NTK-aware、YaRN）、ALiBi 等，直接增强长度外推能力。

* 混合专家（MoE）：用多个 FFN 专家，通过路由只激活一部分，成倍提升模型容量而计算量增幅较小（如 Mixtral、DeepSeek-MoE）。

* 替代注意力模块：用卷积（如 Hyena）、长卷积或状态空间模型（Mamba、RetNet）替换注意力，在长序列上效率极高。

==== 训练效率优化
* 混合精度与低精度训练：FP16/BF16 混合训练，配合损失缩放，减少显存和计算。

* 并行策略：
    ** 数据并行（DP）、分布式数据并行（DDP/FSDP）；
    ** 模型并行：张量并行、流水线并行、序列并行；
    ** ZeRO（DeepSpeed）各阶段优化，将优化器状态、梯度、参数分片到多卡。

* 梯度检查点/重计算：用时间换空间，减少中间激活的显存占用量。

* 优化器改进：AdamW 成为标配；8-bit Adam、LION 等可节约显存并加速收敛。

* 训练稳定性：梯度裁剪、更好的权重初始化（如 Small Init）、标签平滑等正则化手段。

==== 推理部署优化
* KV Cache 压缩与共享：
    ** Multi-Query Attention (MQA)、Grouped-Query Attention (GQA) 大幅减少 KV 缓存尺寸；
    ** Multi-head Latent Attention (MLA)（如 DeepSeek-V2）将 KV 压缩到低维潜空间。
    ** KV 缓存量化、淘汰策略（PagedAttention 管理成页、StreamingLLM 保留局部+开头的缓存）、跨层共享 KV。

* 模型压缩：
    ** 量化：INT8/INT4（GPTQ、AWQ、SmoothQuant），权重量化、激活量化、KV 量化；
    ** 剪枝：非结构化/结构化剪枝去除冗余参数；
    ** 知识蒸馏：将大模型能力迁移给小模型。

* 投机解码：用小模型快速草稿生成，大模型并行校验，实现无精度损失加速。

* 算子与系统优化：算子融合、内核优化（如 FlashInfer）、连续批处理、动态批处理，提升吞吐。

* 参考
TriAttention面向长推理的 KV 缓存压缩: https://arxiv.org/abs/2604.04921

==== 长上下文与位置扩展
* 长度外推技术：RoPE 基础频率缩放（NTK-aware scaling）、YaRN、PoSE 等方法，让有限长度训练的模型推得更长。

* 记忆机制与检索增强：外挂记忆库（如 MemGPT）、分段编码+摘要缓存，或搭配向量检索（RAG）处理超长文本。

* 稀疏与压缩记忆：LongNet 的膨胀注意力、Recurrent Memory Transformer 等实现理论上无限长度。

==== 数据与训练目标优化
* 数据质量与配比：去重、质量过滤、知识分类加权重（如 DCLM），让小模型也能靠好数据胜出。

* 预训练目标创新：UL2 的混合去噪器、Fill-in-the-Middle 训练增强代码理解，甚至使用图文多模态对齐信号。

* 课程学习与阶段训练：先用短序列低价训练，再逐步放长序列和增加难度，降低总成本。

==== 自动化与硬件协同
* 神经架构搜索（NAS）：自动搜索最优层数、头数、宽度等配置。
* 硬件协同设计：针对特定芯片（GPU/TPU/NPU）定制算子、数据流，或定制 AI 芯片架构适配 Transformer。

* 参考
https://docs.nvidia.com/deeplearning/transformer-engine/user-guide/index.html

=== 参考
《Attention Is All You Need》 2017 https://arxiv.org/abs/1706.03762
https://www.zhihu.com/question/471328838

