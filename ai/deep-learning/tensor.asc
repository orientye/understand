:toc:
:toclevels: 6
:hardbreaks-option:

=== 张量(tensor)
==== 概念
n维数组，也称为张量(tensor)。
深度学习操作的主要对象是张量。
https://pytorch.org/docs/stable/tensors.html

==== 标量 → 向量 → 矩阵 → 张量
张量是标量、向量和矩阵的推广，描述具有任意数量轴的n维数组：

- **标量(scalar)**：零阶张量，单一数值。如：`3`, `-2.5`, `π`
- **向量(vector)**：一阶张量，一维数组。如：`[1, 2, 3]`
- **矩阵(matrix)**：二阶张量，二维数组。如：`[[1,2], [3,4]]`
- **张量(tensor)**：n阶张量，n维数组。如三维张量（三阶张量）、四维张量（四阶张量）

==== 张量 vs NumPy ndarray
无论使用哪个深度学习框架，其张量类（在PyTorch和TensorFlow中均为Tensor）都与NumPy的ndarray非常相似，但深度学习框架的张量多了一些重要功能：
- 首先，GPU很好地支持加速计算，而NumPy仅支持CPU计算；
- 其次，张量类支持自动微分（autograd），这是深度学习反向传播的基础；
- 此外，张量支持设备间迁移(CPU/GPU)。广播(broadcasting)与NumPy规则相同，并非框架独有。

==== 创建张量
张量的创建方式有多种：

| 方式 | 描述 | 示例 |
|------|------|------|
| 从数据 | `torch.tensor(list)` | `torch.tensor([1,2,3])` |
| 全零 | `torch.zeros(shape)` | `torch.zeros(3,4)` |
| 全一 | `torch.ones(shape)` | `torch.ones(2,3)` |
| 单位矩阵 | `torch.eye(n)` | `torch.eye(3)` |
| 均匀分布随机 | `torch.rand(shape)` | `torch.rand(3,3)` |
| 标准正态随机 | `torch.randn(shape)` | `torch.randn(3,3)` |
| 等差数列 | `torch.arange(start, end, step)` | `torch.arange(0,10,2)` |
| 等间隔 | `torch.linspace(start, end, steps)` | `torch.linspace(0,1,5)` |
| 全相同值 | `torch.full(shape, fill_value)` | `torch.full((2,2), 7)` |

常见用例对比：

| 函数 | `torch.rand()` | `torch.tensor()` |
|------|---------------|------------------|
| 主要功能 | 生成新随机数据 | 转换已有数据 |
| 输入参数 | 形状(Size/Shape) | 数据内容(List, Array等) |
| 数值范围 | 固定的[0,1) | 取决于提供的数据 |
| 典型场景 | 初始化权重、生成随机样本 | 加载标签、处理预定义常数 |

==== 张量的核心属性
设有一个张量 `x = torch.rand(3, 4, 5)`：

| 属性 | 含义 | 示例值 |
|------|------|--------|
| `x.shape` | 形状（各轴大小） | `torch.Size([3, 4, 5])` |
| `x.ndim` | 维度数（阶数/rank） | `3` |
| `x.numel()` | 元素总数 | `60` |
| `x.dtype` | 数据类型 | `torch.float32` |
| `x.device` | 所在设备 | `cpu` 或 `cuda:0` |
| `x.layout` | 内存布局 | `torch.strided` |

常用的数据类型（dtype）：
- `torch.float32` / `torch.float`：默认浮点类型
- `torch.float64` / `torch.double`：双精度浮点
- `torch.int64` / `torch.long`：默认整数类型（如标签）
- `torch.int32` / `torch.int`：32位整数
- `torch.bool`：布尔类型

维度示例（PyTorch 图像约定为通道在前）：N=批量大小，C=通道数（灰度 1，RGB 3），H=高，W=宽，T=帧数。
| 数据类型 | 典型维度数(Rank) | 常见形状结构 |
|---------|-----------------|--------------|
| 单张灰度图 | 2 | (H, W)；进网络时常为 (C, H, W)，C=1 |
| 单张彩色图 | 3 | (C, H, W) |
| 一批彩色图 | 4 | (N, C, H, W) |
| 一段视频 | 5 | (N, C, T, H, W) |

==== 索引与切片
张量的索引与切片操作与NumPy类似，从0开始：

```
x = torch.rand(4, 3)
x[0]         # 第一行
x[0, 1]      # 第一行第二列（标量）
x[:, 0]      # 所有行的第一列
x[1:3, :]    # 第二行到第三行，所有列
x[::2, :]    # 每隔一行取一行
x[x > 0.5]   # 条件索引（返回一维张量）
```

===== 与 Python 数组及 NumPy 的区别

**与 Python 原生 `list` 的区别：**
- `list` 不支持多维索引 `x[0, 1]`（运行时报 TypeError），必须写成 `x[0][1]`
- `list` 不支持切片中的逗号分隔多维写法 `x[1:3, :]`
- `list` 不支持条件索引 `x[x > 0.5]`
- `list` 切片返回**新列表**（浅拷贝），而非视图

**与 NumPy 的对比：**

PyTorch 的索引语法几乎完全继承自 NumPy，二者行为高度一致。但在语义层面有两点关键差异：

| 操作 | NumPy | PyTorch |
|------|-------|---------|
| **切片** | 返回视图 | 返回视图，且保留 `requires_grad` |
| **花式索引** `x[[0, 2]]` | 返回拷贝 | 返回拷贝，仍保留梯度 |
| **布尔索引** `x[x > 0.5]` | 返回一维数组 | 返回一维张量，仍保留梯度 |
| **`None` 增维** | `x[:, None, :]` | 同样支持，等同于 `unsqueeze(1)` |

**PyTorch 特有的索引操作：**

- `torch.gather(x, dim=0, index=idx)` — 按索引从指定维度收集值
- `x.scatter_(dim=0, index=idx, src=src)` — 按索引就地分散值（NumPy 无对应）
- `torch.index_select(x, dim=0, index=idx)` — 按一维索引选择指定维度上的切片
- `torch.masked_select(x, mask)` — 等价于 `x[mask]`

**小结：** PyTorch 的索引语法与 NumPy **几乎一致**。切片返回视图，花式索引和布尔索引返回拷贝，三者都保留梯度。

==== 张量的运算
===== 形状操作
- **reshape**：改变张量的形状，元素总数不变。可以指定某个维度为 -1，自动推断该维度大小。
  `X.reshape(-1, X.shape[-1])` 将张量X变为二维，第二维保持最后一个维度大小，第一维自动计算。
- **view**：与reshape类似，但要求张量在内存中是连续的(contiguous)。
- **transpose**：交换两个维度，返回的是原张量的视图(view)而非拷贝。
- **squeeze / unsqueeze**：移除/添加长度为1的维度。
- **expand**：将长度为1的维度扩大到指定大小（不分配新内存）。
- **cat**：沿已有维度拼接，阶数不变。**stack**：堆叠时新增一维。

===== 按元素运算
任何按元素的一元运算都不会改变其操作数的形状，同样，给定具有相同形状的任意两个张量，任何按元素二元运算的结果都将是相同形状的张量。

| 类别 | 操作 | 示例 |
|------|------|------|
| 算术 | +, -, *, /, ** | `x + y`, `x ** 2` |
| 比较 | ==, <, >, <=, >= | `x > 0.5` |
| 数学函数 | sin, cos, exp, log, sqrt, abs | `torch.sin(x)` |
| 裁剪 | clamp | `x.clamp(0, 1)` |

===== 归约
归约沿指定维度聚合，结果形状通常变小（可用 `keepdim=True` 保持维度）。

| 类别 | 操作 | 示例 |
|------|------|------|
| 求和/平均 | sum, mean | `x.sum()`, `x.mean(dim=0)` |
| 最大值/最小值 | max, min | `x.max()`, `x.max(dim=0)` |

**最大值与索引：** `torch.max()` 返回张量中的最大值，`torch.argmax()` 返回该值的索引。`x.max(dim=0)` 同时返回最大值和索引。

===== 矩阵乘法
| 操作 | 描述 | 示例 |
|------|------|------|
| `@` 或 `torch.matmul` | 矩阵乘法（支持广播） | `x @ y` |
| `*` | 按元素乘法(Hadamard积) | `x * y` |
| `torch.mm` | 仅二维矩阵乘法 | `torch.mm(A, B)` |
| `torch.bmm` | 批量矩阵乘法 | `torch.bmm(batch_A, batch_B)` |

===== 广播(Broadcasting)
广播是在算术运算中，系统自动将较小的张量”拉伸”以匹配较大张量的过程，且不占用额外内存。

规则：从右向左对齐维度。如果对应维度的长度相等，或其中一个为 1，则可以广播。

限制：如果两个维度都不为 1 且不相等，广播将失败。

Reshape 与广播的区别：
- Reshape 是显式改变张量的维度结构
- 广播是自动扩展形状以匹配运算

在进行广播前，通过 reshape 或 unsqueeze 增加长度为 1 的”新维度”，以满足广播的对齐规则。
示例：将形状为 (4,) 的向量 Reshape 为 (4, 1) 或 (1, 4)，以便与 2D 矩阵进行特定的广播运算。

==== 自动微分(Autograd)
张量支持自动微分，这是深度学习反向传播的基础：

- 创建张量时设置 `requires_grad=True`，框架会对该张量上的运算构建计算图。仅浮点和复数张量可以设置；`torch.tensor([1, 2, 3])` 默认是 int64，会报错
- 调用 `backward()` 后，梯度**累积**到叶子张量的 `.grad` 中。训练循环里每次 `backward()` 前需要 `optimizer.zero_grad()` 或 `x.grad.zero_()`，否则梯度会叠加
- 默认只有叶子（用户直接创建且 `requires_grad=True` 的张量，如 `nn.Parameter`）写入 `.grad`。中间结果的 `.grad` 是 `None`，要看得先 `y.retain_grad()`
- `backward()` 默认释放计算图，对同一张图第二次调用会报错；需要多次反向时传 `retain_graph=True`，求二阶导还要 `create_graph=True`
- 停止追踪梯度：
  ** `torch.no_grad()`：整段不建图，适合验证
  ** `torch.inference_mode()`：比 `no_grad()` 更严、更快，适合纯推理
  ** `.detach()`：只断开某一个张量，后续运算仍可建图（见下一节）

```
# 示例：自动微分
x = torch.tensor([1.0, 2.0, 3.0], requires_grad=True)
y = x ** 2 + 2 * x          # y = [3.0, 8.0, 15.0]
y.sum().backward()          # 先把 y 收成标量，才能省略 gradient 参数
x.grad                      # tensor([4., 6., 8.])  即 ∂yᵢ/∂xᵢ = 2xᵢ + 2
```
y 是向量 [3.0, 8.0, 15.0]，`y.sum()` 把它变成标量 26.0。
对标量调用 `.backward()` 时，PyTorch 默认传入隐式的 `gradient=torch.tensor(1.0)`，即假设 ∂L/∂(标量) = 1。
数学上，这等价于给原来的 y 传入全 1 的上游梯度 v = [1.0, 1.0, 1.0]。

不传 `gradient` 时，只有**标量**（只有一个元素的张量）才能调用 `.backward()`。对向量或矩阵直接 `y.backward()` 会报错：`RuntimeError: grad can be implicitly created only for scalar outputs`。
非标量仍可调用 `.backward()`，但必须传入与调用者**形状相同**的 `gradient` 参数，即上游梯度 ∂L/∂y。

- 为什么不传 gradient 时必须是标量？
隐式的 `gradient=1.0` 只对标量有意义：系统假定当前张量就是最终目标 L，于是 ∂L/∂y = 1。向量没有唯一的「对自身的导数 1」。

- 为什么深度学习中 Loss 通常是标量？
** 优化目标唯一：训练时需要一个最终得分来决定参数往哪个方向更新。多个目标要先合成一个标量（求和、加权平均等）。
** 参数更新：w ← w − η · ∂Loss/∂w 要求 ∂Loss/∂w 与 w 同形。向量 Loss 对应的是雅可比，形状是 (k, *w.shape)，必须再左乘一个组合向量 v，才能得到和 w 同形的梯度——这正是下面的向量-雅可比积。
  w 是所有可训练参数（权重和偏置）的统称，形状可以任意，但用于更新的梯度必须与它逐元素对齐。

- 为什么非标量必须传入 gradient？
若 y、x 都是向量，∂y/∂x 在数学上是雅可比矩阵，不是一条梯度。`backward()` 并不求完整雅可比，而是要求传入与 y 同形状的 v，作为各分量的权重，计算**向量-雅可比积**（vector-Jacobian product, VJP）：

    y = [y₁, y₂, ..., yₘ]
    x = [x₁, x₂, ..., xₙ]
    J 的第 i 行第 j 列是 ∂yᵢ/∂xⱼ，形状 m × n
    vᵀJ 的结果与 x 同形，等于 ∂(v·y)/∂x

- VJP 的实现
实现上对每个算子只做局部 VJP，从头到尾不会拼出整张 m × n 的 J。
标量 Loss 时 m=1，J 本身就是 1 × n 的梯度，没有 m × n 的存储问题。真正需要 VJP 的原因是：网络中间层输出也是高维向量，若每层都物化完整雅可比会撑爆内存。完整雅可比可用 `torch.autograd.functional.jacobian`，但是代价昂贵。


    # 示例：非标量 backward，传入上游梯度 v
    x = torch.tensor([1.0, 2.0], requires_grad=True)
    y = x ** 2                    # y = [1.0, 4.0]
    v = torch.tensor([0.5, 0.3])  # 上游梯度，形状与 y 相同
    y.backward(gradient=v)
    # 教学展开（实现并不会拼出 J）：
    # J = [[∂y₁/∂x₁, ∂y₁/∂x₂],     [[2x₁,  0 ],     [[2.0, 0.0],
    #      [∂y₂/∂x₁, ∂y₂/∂x₂]]  =   [ 0 , 2x₂]]  =   [0.0, 4.0]]
    # 此处 y = x² 是按元素运算，J 是对角阵，这是特例。
    # 一般公式：x.gradⱼ = Σᵢ vᵢ · ∂yᵢ/∂xⱼ
    #         = [0.5*2.0 + 0.3*0,  0.5*0 + 0.3*4.0]
    #         = [1.0, 1.2]
    print(x.grad)  # tensor([1.0000, 1.2000])

Q: `y.sum().backward()` 和 `y.backward(torch.ones_like(y))` 有什么区别？
对 `x.grad` 而言，在 reduction 是 sum 时结果相同：sum 的局部导数 ∂(Σ yᵢ)/∂yᵢ = 1，所以上游梯度是全 1 向量。这不是「把 1.0 广播成全 1」，也不是语法糖——前者在图上多一个 Sum 节点。
若换成 `y.mean().backward()`，等价的是 `y.backward(torch.ones_like(y) / y.numel())`，不是全 1。

Q: `backward()` 和 `torch.autograd.grad()` 有什么区别？
`backward()` 把梯度写入叶子的 `.grad`；`torch.autograd.grad(outputs, inputs)` 返回梯度张量，不碰 `.grad`，适合调试或只对部分输入求导。

Q: 为什么对需要梯度的叶子做原地操作会报错？
叶子是用户直接创建且 `requires_grad=True` 的张量。原地操作（`x.add_(1)`、`x += 1`）改的是这块内存本身。计算图记录的是「用了哪版数据」，叶子又是梯度的落点，原地改会让图里的版本和内存对不上，因此直接禁止：

```
x = torch.tensor([1.0, 2.0], requires_grad=True)
x += 1
# RuntimeError: a leaf Variable that requires grad is being used in an in-place operation.
```

应写成 `y = x + 1` 再用 `y`。`optimizer.step()` 是在 `no_grad` 里原地改权重，不走这条限制。中间结果的 inplace 有时能过，但若该内存已被后续节点使用，会触发 version counter 错误；需要梯度的路径上尽量不要 inplace。

Q: 前向用了广播，反向为什么要把广播维的梯度加回去？
`x.grad` 必须与 `x` 同形。广播在前向把小张量「看成」和大张量一样（不真复制），反向就要把这条虚影收成原形状——被拉伸的那些维，梯度加总。

```
x = torch.tensor([1.0, 2.0, 3.0], requires_grad=True)  # (3,)
w = torch.tensor([10.0], requires_grad=True)           # (1,) 广播成 (3,)
y = x + w
y.sum().backward()
# x.grad = [1, 1, 1]
# w.grad = [3.]   三个 1 加回去，而不是 [1, 1, 1]
```

偏置 `b` 形状 (D,) 加到 (N, D) 上同理：`b.grad` 仍是 (D,)，等于 N 行对应列的梯度之和。一条参数被用了多次，梯度就加多次。

==== 张量与NumPy的互转
- 张量 → NumPy：`.numpy()`，仅对CPU上的张量有效
- NumPy → 张量：`torch.from_numpy(array)`
- 若张量在GPU上，需先 `.cpu()`
- 若张量需要梯度（requires_grad=True），需先 `.detach()` 断开计算图

```
numpy_array = tensor.detach().cpu().numpy()
# .detach()：断开梯度  .cpu()：确保在CPU上  .numpy()：转换为NumPy数组
```

常见需要 detach() 的情况：
- 将需要梯度的张量转为NumPy数组时
- 需要从计算图中提取一个数值，但不希望它参与后续反向传播时
- 在强化学习、特征提取或模型评估中固定某个中间值，防止梯度意外回流

==== 底层实现：ATen
ATen (A Tensor Library) 是 PyTorch 的底层张量计算库：

```
aten/
├── src/
│   ├── ATen/
│   │   ├── native/          # 原生算子实现
│   │   ├── cpu/             # CPU 特定实现
│   │   ├── cuda/            # CUDA 特定实现
│   │   ├── core/            # 核心数据类型和接口
│   │   └── ops/             # 算子定义
│   └── torch/               # Python 绑定
```

张量的底层核心数据结构包含三个关键部分：
1. **Storage**：实际存储数据的连续内存块
2. **dtype**：数据类型（决定每个元素的字节数）
3. **步长(stride)**：每个维度中前进一个元素需跳过的步数，支持非连续内存的视图(view)操作

正是基于 stride 的存储方式使得 transpose、view 等操作可以是零拷贝的——只需创建新的张量元数据(meta-data)，共享同一块 Storage 即可。这与 NumPy 的 ndarray 底层设计思路一脉相承。

==== CPU 张量与 GPU 张量对比
| 维度 | CPU 张量 | GPU 张量 |
|------|---------|----------|
| 存储位置 | 主机内存 (RAM) | 设备显存 (VRAM) |
| 计算单元 | CPU 核心 | GPU 流处理器 |
| 后端 | ATen + MKL / oneDNN | CUDA / cuDNN、ROCm、MPS |
| 特点 | 通用计算，延迟较高但灵活性好 | 并行计算能力强，适合大规模矩阵运算 |

设备间迁移：
```
cpu_tensor = gpu_tensor.cpu()       # GPU → CPU
gpu_tensor = cpu_tensor.cuda()      # CPU → GPU
gpu_tensor = cpu_tensor.to('cuda')  # 统一API
```

当前使用的流处理器架构覆盖了从 CUDA(独占)到 ROCm、Intel oneAPI 再到 Apple Metal 的多元生态。

==== Q&A
- Q: reshape vs view？
  reshape 更灵活，不要求张量在内存中连续；view 要求连续，否则需先调用 .contiguous()。
  在 PyTorch 中，reshape 的底层逻辑是：优先返回数据的视图（View），只有在必要时（即内存不连续时）才会拷贝数据。
  https://docs.pytorch.org/docs/stable/generated/torch.Tensor.reshape.html
  https://docs.pytorch.org/docs/stable/generated/torch.Tensor.view.html

- Q: view vs transpose vs contiguous？
  transpose():
  绝大多数情况下转置会导致内存不连续，但有一个特例——如果原张量的某个维度大小为 1，或者它本身是一个标量，transpose后它依然可能是连续的。
  也就是说：transpose 返回共享内存的视图，但通常会改变 stride 导致内存不连续。
  contiguous():
  绝大多数情况下，contiguous() 会在内存不连续时拷贝数据，返回一个新张量。
  但如果原张量已经是连续的，contiguous() 返回的是原张量本身（不会拷贝，也不会创建新对象）。
  也就是说，contiguous() 返回一个内存连续的张量视图（可能是拷贝后的新张量，但也可能是原张量（如果原张量已经是连续的））。
  https://docs.pytorch.org/docs/stable/generated/torch.Tensor.transpose.html

- Q: reshape vs 广播(broadcasting)？
  Reshape 显式改变形状，广播是自动扩展。两者常配合使用：reshape 增删维度后满足广播条件。

- Q: 张量如何转化为NumPy数组？什么情况下需要detach()？（见上，张量与NumPy互转部分）

==== 参考
https://pytorch.org/tutorials/beginner/introyt/tensors_deeper_tutorial.html
https://docs.pytorch.org/docs/stable/tensors.html

