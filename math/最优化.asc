= 最优化(Optimization)
:hardbreaks-option:
:revnumber: 0.0.1
:author: orient
:toc:
:toclevels: 5
:sectnums:
:sectnumlevels: 5
:stem: latexmath
:homepage: http://orientye.com

<<<

== 概念
=== 最优化
https://en.wikipedia.org/wiki/Mathematical_optimization
最优化问题(也称优化问题)泛指定量决策问题，主要关心如何对有限资源进行有效分配和控制，并达到某种意义上的最优。它通常需要对需求进行定性和定量分析，建立恰当的数学模型来描述该问题，设计合适的计算方法来寻找问题的最优解，探索研究模型和算法的理论性质，考察算法的计算性能等。

=== 最优化问题的一般形式
* 数学形式
在决策变量 x ∈ ℝⁿ（或更一般的可行域 X）上，最优化问题可写为
ifndef::env-github[]
[stem]
++++
\begin{aligned}
\min_{\mathbf{x} \in \mathcal{X}} \quad & f(\mathbf{x}) \\
\text{s.t.} \quad & c_i(\mathbf{x}) = 0, \quad i = 1, \ldots, m \\
& c_j(\mathbf{x}) \le 0, \quad j = m+1, \ldots, p
\end{aligned}
++++
endif::[]
ifdef::env-github[]
```math
\begin{aligned}
\min_{\mathbf{x} \in \mathcal{X}} \quad & f(\mathbf{x}) \\
\text{s.t.} \quad & c_i(\mathbf{x}) = 0, \quad i = 1, \ldots, m \\
& c_j(\mathbf{x}) \le 0, \quad j = m+1, \ldots, p
\end{aligned}
```
endif::[]
其中 s.t. 表示 subject to（受限于）。最大化问题 max f(x) 等价于 min −f(x)，约束与可行域不变。

* 基本要素
** 决策变量 x：待确定的量，维数 n 由问题建模决定。
** 目标函数 f(x)：标量函数，刻画「优」的程度；求 min 时 f 越小越优。
** 约束函数 cᵢ(x)：等式约束 cᵢ(x)=0 与不等式约束 cⱼ(x)≤0 共同限定可行解。
** 可行域（可行集）Ω：同时满足所有约束且 x ∈ X 的点构成的集合；Ω 上的点称为可行解。
** 最优解 x* 与最优值 f*：若 x* ∈ Ω 且对任意 x ∈ Ω 有 f(x*) ≤ f(x)，则 x* 为全局最优解，f* = f(x*) 为最优值。

* 无约束与有约束
若 m = p = 0 且 X = ℝⁿ，则为无约束优化；只要存在等式或不等式约束，即为有约束优化。仅含等式约束时可用消元或拉格朗日乘子法处理；含不等式约束时常见 KKT 条件刻画一阶最优性。

* 向量不等式（推广）
多目标或矩阵变量问题中，约束可写为 g(x) ≤ 0，其中 ≤ 按分量或半正定序理解；本书后续按标量/向量实函数情形展开。

=== 最优化问题的类型与应用背景
同一套「目标 + 约束 + 变量」框架下，可按模型性质、变量类型与问题语境划分；分类不是互斥的，实际模型常同时带上多种标签（例如带整数变量的凸二次规划）。

* 按目标与约束的结构
** 线性规划（LP）：f 与所有 cᵢ 均为 x 的线性函数，可行域为多面体；单纯形法、内点法是经典算法（后文「线性规划」章）。
** 非线性规划（NLP）：目标或约束中至少有一项为非线性；无约束情形见「无约束优化问题」，等式/不等式约束见「有约束的非线性优化问题」。
** 凸优化：f 为凸函数、可行域为凸集（不等式约束 gⱼ(x)≤0 中 gⱼ 凸）；局部最优即全局最优，多项式时间可解类远多于一般 NLP（后文「凸优化」节）。
** 非凸优化：一般 NLP 或离散问题；可能存在多个局部极值，全局最优往往需分支定界、启发式或问题特化结构。
** 光滑与非光滑：f 或约束可微时可用梯度、牛顿、KKT；含绝对值、max、指示函数等时为非光滑，常用次梯度、 prox 或重构为光滑近似。

* 按决策变量与目标个数
** 连续优化：x ∈ ℝⁿ 或连续流形；本书主体默认连续变量。
** 离散与混合整数优化：部分或全部变量取整数（0-1 规划、整数规划）；可行域不再连通，计算复杂度通常显著上升（后文「整数规划」节）。
** 组合优化：有限集合上的极值（图、排程、背包）；常建模为整数规划或约束满足，算法侧与启发式搜索联系紧密。
** 单目标与多目标：前者只有一个 f(x)；后者向量目标 F(x)，最优性由 Pareto 支配关系定义（后文「多目标优化」节）。

* 按信息与时间结构
** 确定性优化：参数已知；模型中数据固定。
** 随机与鲁棒优化：参数含随机性（期望优化、机会约束）或仅知不确定集（最坏情形、分布鲁棒）；在数据、控制与机器学习中常见。
** 静态与动态：静态问题一次决策；动态/最优控制沿时间序列入状态 xₜ、控制 uₜ 与状态方程，化为有限维 NLP 或无穷维变分问题。

* 典型应用背景
|===
| 领域 | 典型问题 | 常见模型类型
| 运筹与物流 | 资源分配、运输、排程 | LP、整数规划、网络流
| 工程与设计 | 结构尺寸、控制参数、滤波器系数 | NLP、凸优化、最优控制
| 机器学习 | 参数估计、正则化训练、超参搜索 | 无约束/凸 NLP、随机优化
| 经济学与金融 | 投资组合、均衡、定价 | 凸优化、随机规划、变分
| 信号与通信 | 波束成形、压缩感知、功率分配 | 凸优化、半定规划
| 科学计算 | 反问题、数据同化、PDE 约束优化 | PDE 约束 NLP、Tikhonov 正则
|===

== 无约束优化问题
=== 约束和无约束优化问题的基础知识
=== 一维搜索方法
=== 梯度方法
=== 牛顿法(Newton's Method)
==== 概要
- 概念
又称牛顿-拉弗森方法(Newton-Raphson Method):
是一种用于求解非线性方程(组)的迭代优化算法。它通过利用函数的泰勒展开近似，快速收敛到方程的根或极值点。
牛顿法不仅可以用来求解方程的根，还可以用来求解函数的极值问题，二者在本质上是一个问题，因为求解函数极值的思路是寻找导数为0的点，本质上依然是求解方程。

- 基本思想
通过迭代逼近，利用函数的一阶导数(梯度)和二阶导数(Hessian矩阵)信息，构造局部线性或二次近似，从而找到方程的根或函数的极值。

- 应用场景
** 实数域和复数域上近似求解方程
** 优化问题: 寻找函数的极小值或极大值(此时需用二阶导数)

- 优缺点
    ** 优点:
        *** 收敛速度快: 若初始值合适，二阶收敛(误差平方级递减)。
        *** 高效: 尤其对于光滑凸函数。
    ** 缺点:
        *** 依赖初始值: 可能收敛到局部极值或发散。
        *** 需计算导数: 需显式给出梯度或Hessian矩阵，计算成本高。
        *** Hessian可能不正定: 优化问题中需修正(如拟牛顿法)。

==== 解非线性方程
===== 推导过程
Step1. 在xₙ处对f(x)作一阶泰勒展开：
ifndef::env-github[]
[stem]
++++
f(x) \approx f(x_n) + f'(x_n)(x - x_n)
++++
endif::[]
ifdef::env-github[]
```math
f(x) \approx f(x_n) + f'(x_n)(x - x_n)
```
endif::[]

Step2. 令近似方程为零求根:
ifndef::env-github[]
[stem]
++++
0 \approx f(x_n) + f'(x_n)(x - x_n) \implies x = x_n - \frac{f(x_n)}{f'(x_n)}
++++
endif::[]
ifdef::env-github[]
```math
0 \approx f(x_n) + f'(x_n)(x - x_n) \implies x = x_n - \frac{f(x_n)}{f'(x_n)}
```
endif::[]

Step3. 得到迭代公式:
ifndef::env-github[]
[stem]
++++
x_{n+1} = x_n - \frac{f(x_n)}{f'(x_n)}
++++
endif::[]
ifdef::env-github[]
```math
x_{n+1} = x_n - \frac{f(x_n)}{f'(x_n)}
```
endif::[]

牛顿法与泰勒展开的关系:
牛顿法本质上是利用泰勒展开的一阶线性近似来构造迭代公式。
如果使用二阶泰勒展开(包含f′′(x)项)，可以得到更复杂的迭代方法(如Halley方法)，但计算成本更高。
泰勒展开的高阶项在牛顿法中未被使用，因此牛顿法仅具有局部快速收敛性，而无法保证全局收敛。

===== 几何解释
相当于在每一步用切线逼近函数，并找到切线与x轴的交点作为新的近似解。
如果初始值x0接近真实解，且f'(x)不为0，牛顿法通常能快速收敛。

===== 示例
求 √2 的近似值（即 f(x)=x²−2 的正根）：

ifndef::env-github[]
[stem]
++++
\begin{aligned}
f(x) &= x^2 - 2, \quad f'(x) = 2x \\
x_{n+1} &= x_n - \frac{x_n^2 - 2}{2x_n} = \frac{x_n}{2} + \frac{1}{x_n}
\end{aligned}
++++
endif::[]
ifdef::env-github[]
```math
\begin{aligned}
f(x) &= x^2 - 2, \quad f'(x) = 2x \\
x_{n+1} &= x_n - \frac{x_n^2 - 2}{2x_n} = \frac{x_n}{2} + \frac{1}{x_n}
\end{aligned}
```
endif::[]

迭代过程:
|===
| 步骤 | x_n       | 计算
| 0    | 1.0             | 初始值
| 1    | 1.5             | 1/2 + 1/1 = 1.5
| 2    | 1.4167          | 1.5/2 + 1/1.5 ≈ 1.4167
| 3    | 1.4142          | 进一步逼近真值
|===

===== 初始值
牛顿法在理论上属于局部方法：只有在初值 x₀ 落在根 x* 附近足够小的邻域内，且迭代过程中 f'(xₙ) 不为零时，才通常表现出二次收敛。初值选得不好，会出现不收敛、收敛很慢、或收敛到别的根等情况。

* 基本要求
** 导数非零：迭代分母为 f'(xₙ)。若 x₀ 取在 f 的驻点（f'(x₀)=0），或根为重根导致 f'(x*)=0，牛顿步无定义或极不稳定。
** 靠近目标根：切线近似只在 xₙ 附近有效；x₀ 离 x* 过远时，切线与 x 轴的交点可能跳到函数性态很差的区域，甚至发散。

* 一维求根时的常用策略
** 粗定位：由图像、物理意义或量级估计，把 x₀ 放在真根附近（不必很准，但避免跨数量级的盲目取值）。
** 包围区间：若已知 [a,b] 内有根且 f(a)f(b)<0，可取中点或靠近 |f| 较小的一端作 x₀；或对区间做若干步二分缩小后再用牛顿法（兼顾稳健与速度）。
** 多根情形：f 有多个零点时，不同 x₀ 往往落入不同根的吸引域；要特定根时需多初值试算或结合问题约束。
** 延续初值：方程组、时间演化或外层迭代中，常用上一解作为 x₀，往往已在根的吸引域内。

* 与示例 f(x)=x²−2 的对照
正根 x*≈1.4142，负根 x*≈−1.4142。迭代式 x_{n+1}=xₙ/2+1/xₙ 的符号被 x₀ 决定：x₀>0 恒收敛到 +√2，x₀<0 恒收敛到 −√2，因此「符号与目标根不一致」会落到另一根。x₀=0 时 f'(0)=0，无法按牛顿公式起步；|x₀| 过大只是首步收敛较慢，并不会发散——「振荡、发散」是对一般函数才成立的现象，本例不出现。

* 初值不佳或迭代异常时的处理
** 阻尼牛顿：缩小步长，例如
ifndef::env-github[]
[stem]
++++
x_{n+1} = x_n - \alpha \frac{f(x_n)}{f'(x_n)}, \quad 0 < \alpha \le 1
++++
endif::[]
ifdef::env-github[]
```math
x_{n+1} = x_n - \alpha \frac{f(x_n)}{f'(x_n)}, \quad 0 < \alpha \le 1
```
endif::[]
先用较小 α 拉回可行区域，再令 α→1 恢复全牛顿步。
** 混合算法：先用二分法、弦截法等全局性较好的一维方法，再切换牛顿法做高精度收尾。
** 监控与重启：若 |f(xₙ)| 未下降或 |f'(xₙ)| 过小，应更换 x₀ 或改用不依赖 f' 为零点的方法。

===== 参考
https://www.bilibili.com/video/BV1Nt411T7HT/

==== 求极值
===== 推导过程
ifndef::env-github[]
Step1. 在xₖ处对f(x)作二阶泰勒展开: stem:[f(\mathbf{x}) \approx f(\mathbf{x}_k) + \nabla f(\mathbf{x}_k)^T \Delta \mathbf{x} + \frac{1}{2} \Delta \mathbf{x}^T \nabla^2 f(\mathbf{x}_k) \Delta \mathbf{x}]
其中 ∇f(xₖ) 是梯度，∇²f(xₖ) 是 Hessian 矩阵
endif::[]
ifdef::env-github[]
Step1. 在xₖ处对f(x)作二阶泰勒展开: $f(\mathbf{x}) \approx f(\mathbf{x}_k) + \nabla f(\mathbf{x}_k)^T \Delta \mathbf{x} + \frac{1}{2} \Delta \mathbf{x}^T \nabla^2 f(\mathbf{x}_k) \Delta \mathbf{x}$
endif::[]

ifndef::env-github[]
Step2. 为使近似函数极小化，对 Δx 求导并令其为零: stem:[\nabla f(\mathbf{x}_k) + \nabla^2 f(\mathbf{x}_k) \Delta \mathbf{x} = 0]
endif::[]
ifdef::env-github[]
Step2. 为使近似函数极小化，对 Δx 求导并令其为零: $\nabla f(\mathbf{x}_k) + \nabla^2 f(\mathbf{x}_k) \Delta \mathbf{x} = 0$
endif::[]

ifndef::env-github[]
Step3. 解得迭代公式: stem:[\mathbf{x}_{k+1} = \mathbf{x}_k - [\nabla^2 f(\mathbf{x}_k)]^{-1} \nabla f(\mathbf{x}_k)]
endif::[]
ifdef::env-github[]
Step3. 解得迭代公式: $\mathbf{x}_{k+1} = \mathbf{x}_k - [\nabla^2 f(\mathbf{x}_k)]^{-1} \nabla f(\mathbf{x}_k)$
endif::[]

===== 几何解释
牛顿法用二次曲面(二阶近似)拟合当前点附近的函数形状，并直接跳到该二次曲面的极小值点。若原函数是二次的，牛顿法一步收敛。

===== 示例
求下面函数的极小点。一维求极值时，牛顿法就是对 f′(x) = 0 做求根，迭代式为推导过程中的公式在 Hessian 退化为 f″(x) 时的情形：
ifndef::env-github[]
[stem]
++++
\begin{aligned}
f(x) &= \frac{1}{4}x^4 - \frac{1}{2}x^2 \\
f'(x) &= x^3 - x, \quad f''(x) = 3x^2 - 1 \\
x_{k+1} &= x_k - \frac{x_k^3 - x_k}{3x_k^2 - 1}
\end{aligned}
++++
endif::[]
ifdef::env-github[]
```math
\begin{aligned}
f(x) &= \frac{1}{4}x^4 - \frac{1}{2}x^2 \\
f'(x) &= x^3 - x, \quad f''(x) = 3x^2 - 1 \\
x_{k+1} &= x_k - \frac{x_k^3 - x_k}{3x_k^2 - 1}
\end{aligned}
```
endif::[]

f′(x) = x(x − 1)(x + 1)，驻点为 0 与 ±1。f″(±1) = 2 > 0，这两点是局部极小，极小值均为 −¼；f″(0) = −1 < 0，x = 0 是局部极大。

取 x₀ = 2。此时 f″(2) = 11 > 0，迭代进入极小点 x* = 1：

迭代过程:
|===
| 步骤 | x_k    | 计算
| 0    | 2      | 初始值
| 1    | 1.4545 | 2 − 6/11 ≈ 1.4545
| 2    | 1.1510 | 1.4545 − 1.6228/5.3471 ≈ 1.1510
| 3    | 1.0253 | 1.1510 − 0.3740/2.9747 ≈ 1.0253
| 4    | 1.0009 | 1.0253 − 0.0526/2.1539 ≈ 1.0009
|===

从第 2 步起误差大约按平方缩小，即概要中的二阶收敛。取 x₀ = −2 时，迭代收敛到另一个极小点 x* = −1。

目标函数为二次时一步到达极小点。对 f(x) = ½x² − x，有 f′(x) = x − 1、f″(x) = 1，因此任意 x₀ 都给出 x₁ = 1。

===== 注意事项
无约束极小化中的牛顿法同样是局部方法：在 x* 附近、且迭代点处 Hessian 对当前目标「足够像凸二次」时，才通常有二阶收敛；否则可能发散、步长过大，或收敛到鞍点、极大点而非期望的极小点。

* 与求根牛顿法的区别
** 求极值时对 ∇f(x)=0 迭代，每步除数为 Hessian ∇²f(xₖ)（一维即 f″(xₖ)），而不是 f′。
** 收敛到的是临界点（梯度为零）；是否为极小点还需二阶条件或问题结构（如全局凸）保证，不能仅凭 ∇f=0 断言已得极小。

* Hessian 与下降方向
** 极小化时理想情况是 ∇²f(xₖ) 正定，牛顿方向 dₖ = −[∇²f(xₖ)]⁻¹∇f(xₖ) 才是下降方向；正定等价于局部二次近似在 xₖ 处有极小。
** 若 Hessian 不定或负定，全牛顿步可能使 f 上升（朝极大方向跳）或沿鞍方向走远；一维上 f″(xₖ)<0 时步长符号与求极小意图相反。
** ∇²f(xₖ) 奇异或病态时，求逆不稳定，步长可能爆炸；一维示例中 f″(x)=3x²−1 在 x=±1/√3 处为零，迭代公式分母为零。

* 初值与示例 f(x)=¼x⁴−½x² 的对照
** 三个临界点：x=±1 为局部极小，x=0 为局部极大。x₀ 的吸引域一般划分到不同临界点；x₀=2 收敛到 +1，x₀=−2 收敛到 −1，与求根时「符号决定根」类似。
** 若 x₀ 恰使 ∇f(x₀)=0（如 x₀∈{0,±1}），标准牛顿步无进展或需靠数值扰动离开；x₀=0 且落在极大点附近时，不应期望牛顿法「自动找极小」。
** 非凸函数上初值稍偏就可能落入不同局部极小；全局最优需多初值、粗搜索或问题特化结构。

* 光滑性与建模
** 经典牛顿法假设 f 二阶可微且能算 Hessian；不可微点（|x|、max 等）需光滑化或改用次梯度类方法。
** 高维中显式形成并求逆 n×n Hessian 代价为 O(n³)，n 大时常用拟牛顿法（近似 Hessian 或其逆）或共轭梯度牛顿。

* 步长与 Hessian 修正
** 阻尼牛顿（线搜索）：沿牛顿方向 dₖ 做一维搜索，取
ifndef::env-github[]
[stem]
++++
\mathbf{x}_{k+1} = \mathbf{x}_k + \alpha_k \mathbf{d}_k, \quad \mathbf{d}_k = -[\nabla^2 f(\mathbf{x}_k)]^{-1} \nabla f(\mathbf{x}_k), \quad \alpha_k \in (0, 1]
++++
endif::[]
ifdef::env-github[]
```math
\mathbf{x}_{k+1} = \mathbf{x}_k + \alpha_k \mathbf{d}_k, \quad \mathbf{d}_k = -[\nabla^2 f(\mathbf{x}_k)]^{-1} \nabla f(\mathbf{x}_k), \quad \alpha_k \in (0, 1]
```
endif::[]
直至 f 充分下降，可缓解全步过大导致的发散。
** 修正 Cholesky、Levenberg–Marquardt 等对 Hessian 加正则，使线性系统可解且方向更稳；信赖域法则限制每步在二次模型可信的范围内（后文拟牛顿法、收敛性节会进一步展开）。
** 监控：若 f(xₖ) 未降、‖∇f(xₖ)‖ 停滞或 Hessian 出现负特征值，应减小 α、修正 Hessian，或退回梯度法等更稳健的一阶方法。

==== 收敛性

==== 参考
http://faculty.bicmr.pku.edu.cn/~wenzw/optbook/lect/11-lect-newton.pdf
https://zhuanlan.zhihu.com/p/37588590

=== 共轭方向法
=== 拟牛顿法
=== 求解线性方程组
=== 无约束优化问题和神经网络
=== 全局搜索算法

== 线性规划
=== 线性规划概述
=== 单纯形法
=== 对偶
=== 非单纯形法
=== 整数规划

== 有约束的非线性优化问题
=== 仅含等式约束的优化问题
=== 含不等式约束的优化问题

=== 凸优化(Convex Optimization)
==== 概念
凸优化是数学优化的一个子领域，它研究在凸集上最小化凸函数（或等价地，在凸集上最大化凹函数）的问题，许多凸优化问题都存在多项式时间算法，而数学优化通常是NP难问题。
https://en.wikipedia.org/wiki/Convex_optimization

=== 有约束优化问题的求解算法

=== 多目标优化

== 参考
《An Introduction to Optimization, 4th》 中:《最优化导论》第四版
《最优化：建模、算法与理论》
《最优化理论与方法》
《Optimization Model》
《Numerical Optimization》
《Linear and Nonlinear Programming, 4th》
《Introduction to Linear Optimization》
《Nonlinear Programming》
《Convex Optimization》 中:《凸优化》
《Lectures on Modern Convex Optimization》
《Convex Optimization in signal and communication》