:toc:
:toclevels: 5
:hardbreaks-option:

== 问题排查

=== 命令与工具
==== 尽量使用新的命令
- iproute2 vs net-tools
iproute2 是 Linux 系统上用于网络配置和管理的一套现代化工具集，取代了传统的 net-tools（如 ifconfig、route、arp 等）
[cols="1,2,2"]
|===
|功能            |iproute2 命令                |net-tools 命令
|查看 IP 地址     |ip addr                     |ifconfig
|启用/禁用网卡    |ip link set eth0 up/down	   |ifconfig eth0 up/down
|查看路由表       |ip route                    |route -n
|查看 ARP 表      |ip neigh                    |arp -a
|查看网络连接     |ss -tulnp                   |netstat -tulnp
|管理网桥         |bridge                      |brctl
|===

- 解读
net-tools比较古老
iproute2提供了更强大、更灵活的网络管理功能，支持 IPv4/IPv6、策略路由、VLAN、隧道等高级特性。
netstat比ss开销大: netstat通过读取/proc/net/下面的文件来解析网络连接信息; 而ss使用的是netlink方式，效率会高很多。

==== dstat
dstat 可以查看整体情况，实时显示系统资源的使用情况，包括 CPU、内存、磁盘、网络等。
它结合了 vmstat、iostat、netstat 等工具的功能，并以彩色、易读的方式输出。

==== sar
sar: System Activity Reporter（系统活动情况报告）。

sar工具将对系统当前的状态进行取样，然后通过计算数据和比例来表达系统的当前运行状态。它的特点是可以连续对系统取样，获得大量的取样数据；取样数据和分析的结果都可以存入文件，所需的负载很小。

sar是目前Linux上最为全面的系统性能分析工具之一，可以从各个方面对系统的活动进行报告，包括文件的读写情况、系统调用的使用情况、串口、CPU效率、内存使用状况、进程活动及IPC有关的活动等。

有两种用法:
(1) 追溯过去的统计数据（默认）
(2) 周期性查看当前数据

==== ping/hping3/traceroute/mtr
- hping3

    支持TCP, UDP, ICMP, and RAW-IP protocols

- traceroute

    Traceroute 的实现一共有三种方法, 分别是:
        TCP traceroute(使用tracetcp程序可以实现)
        UDP traceroute(Linux默认情况下使用)
        ICMP traceroute(MS Windows默认情况下使用)

- mtr

    mtr（My Traceroute）是一个结合了 ping 和 traceroute 功能的网络诊断工具，用于实时监测网络路径的连通性和延迟。
    它动态显示数据包经过的每一跳（路由器或节点）的丢包率、延迟和响应时间，比传统 traceroute 更直观。
    TCP/UDP 模式探测:
        默认使用 ICMP，但某些网络会屏蔽 ICMP，可改用 TCP/UDP:
            mtr --tcp -P 80 google.com    # 使用 TCP 端口 80
            mtr --udp -P 53 8.8.8.8       # 使用 UDP 端口 53（DNS）

==== ifconfig
现代 Linux 系统逐渐淘汰 ifconfig，优先使用 ip

==== netstat/nstat
- netstat

    现代 Linux 系统逐渐淘汰 netstat，优先使用 ss

- nstat

    nstat用于查看内核的网络统计信息（/proc/net/netstat 和 /proc/net/snmp）。
    它比传统的 netstat 更轻量级，适合监控网络协议栈的详细数据。
        nstat -z | grep -i drop
        watch -n 1 "nstat -z | grep -i retrans" #持续监控TCP重传(排查丢包)

==== ip
ip 命令属于 iproute2 软件套件，用于替代传统的 ifconfig、route、arp 等命令。
它提供了更现代、更统一的网络管理方式，支持 IPv4/IPv6、路由、邻居（ARP/NDP）、VLAN、隧道等高级功能。

==== ss
===== 命令

    ss -natp #可以查看每个tcp的连接状态
    ss -ltn  #监听端口
    ss -tn   #已连接端口
    ss -i

===== Recv-Q Send-Q
- 1. 对于 LISTEN 状态的套接字（服务端监听端口）
    ** Recv-Q
        表示当前已完成三次握手但尚未被应用程序调用 accept() 取走的连接数（即等待处理的已建立连接队列长度）。
        若该值持续增长，可能表明应用程序处理能力不足。
    ** Send-Q
        表示监听端口的最大连接队列长度（即 backlog 参数限制，可通过 somaxconn 系统调优）。
        例如，Send-Q 显示 128 表示最多允许 128 个连接在队列中等待 accept()。
- 2. 对于非 LISTEN 状态的套接字（已建立连接）
    ** Recv-Q
        表示内核已接收但应用程序尚未读取的数据量（字节）。
        若该值持续较大，可能表明应用程序读取速度过慢或存在阻塞。
    ** Send-Q
        表示内核已发送但未收到对方 ACK 确认的数据量（字节）。
        若该值持续较大，可能表明网络延迟高、丢包或对端接收缓慢。

==== iptables/firewalld

==== sysctl
===== 常用网络相关 sysctl 参数

    # 查看所有网络相关参数
    sysctl -a | grep net

    # 查看当前值
    sysctl net.core.rmem_max
    sysctl net.ipv4.tcp_congestion_control

    # 临时修改（立即生效，重启失效）
    sysctl -w net.core.rmem_max=16777216

    # 永久修改（写入 /etc/sysctl.conf 或 /etc/sysctl.d/）
    # echo "net.core.rmem_max = 16777216" >> /etc/sysctl.conf

===== 核心参数说明

NETWORK CORE::
    net.core.rmem_max:: 最大套接字接收缓冲区（字节）
    net.core.wmem_max:: 最大套接字发送缓冲区（字节）
    net.core.netdev_max_backlog:: 网卡队列最大长度，处理速度跟不上入包速度时溢出丢包
    net.core.somaxconn:: listen 队列最大长度，影响 SYN 半连接和 accept 全连接队列

TCP::
    net.ipv4.tcp_rmem:: TCP 接收缓冲区（min / default / max）
    net.ipv4.tcp_wmem:: TCP 发送缓冲区（min / default / max）
    net.ipv4.tcp_congestion_control:: 拥塞控制算法（推荐 bbr）
    net.ipv4.tcp_fin_timeout:: TIME_WAIT 状态超时时间（默认 60s）
    net.ipv4.tcp_tw_reuse:: 允许重用 TIME_WAIT 连接（配合 tcp_timestamps）
    net.ipv4.tcp_max_syn_backlog:: SYN 半连接队列最大长度
    net.ipv4.tcp_syn_retries:: SYN 重试次数（默认 6）
    net.ipv4.tcp_keepalive_time:: TCP keepalive 空闲时间（默认 7200s）

UDP::
    net.ipv4.udp_mem:: UDP 缓冲区（min / pressure / max）
    net.core.rmem_default:: 默认接收缓冲区大小（UDP 丢包常见原因之一）

==== ethtool

    ethtool -S 可以查看网卡统计信息(例如接收和发送的数据包总数，接收和发送的流量，丢弃的包数量，错误的数据包数量等)
    例如ethtool -S ens33

==== cat /proc/net/dev

    更高一层的网卡统计
    里显示的统计只是sysfs里的一个子集，适合作为一个常规的统计参考

==== tcpdump/wireshark/tshark
Q: tcpdump能否抓到被iptable封禁的包？
A: tcpdump工作在设备层，在ip层以前就能处理。netfilter工作在ip, arp等层，是在tcpdump后面工作的，因此iptable封禁规则不会影响到tcpdump。

==== charles/fiddler

==== tcpreplay/ipdecap/scapy
- tcpreplay
流量重放。可将 tcpdump 抓取的 pcap 文件按原样或修改后重新注入网络

    tcpreplay -i eth0 capture.pcap         # 原速重放
    tcpreplay -i eth0 -M 10 capture.pcap   # 10 倍速重放
    tcpreplay -i eth0 -l 0 capture.pcap    # 循环重放

- ipdecap
ipdecap 是一个解密IPsec流量的工具

- scapy
scapy 是一个强大的Python网络数据包操作工具，可用于构造、发送、捕获和分析数据包。

    # 简单的 ping 包
    send(IP(dst="8.8.8.8")/ICMP())

    # 构造 SYN 扫描包并等待响应
    ans = sr1(IP(dst="target")/TCP(dport=80, flags="S"), timeout=2)

    # 读取 pcap 文件
    pkts = rdpcap("capture.pcap")
    pkts.summary()

==== dropwatch
内核丢包监控工具，实时监控内核协议栈的丢包事件。

    # 启动 dropwatch
    dropwatch -l kas

    # 交互模式下输入 start 开始监控
    输出示例:
        drop at: tcp_v4_do_rcv+0x20/0x100
        表示 TCP 层发生了丢包，可以看到丢弃位置的内核函数

==== iperf3
网络性能测试工具，常用于测量网络带宽和质量。

    # 服务端
    iperf3 -s

    # 客户端（TCP 测试，默认 10 秒）
    iperf3 -c <server_ip>

    # 客户端（UDP 测试，指定带宽）
    iperf3 -c <server_ip> -u -b 100M

    # 双向测试
    iperf3 -c <server_ip> -d

    # 反向模式（服务端发，客户端收）
    iperf3 -c <server_ip> -R

    # 指定测试时长（秒）
    iperf3 -c <server_ip> -t 30

    # 指定并行流数
    iperf3 -c <server_ip> -P 4

==== perf
perf 是 Linux 性能分析工具，基于性能监控单元（PMU）和内核跟踪点，可用于分析网络栈热点。

==== systemtap
动态跟踪工具，可编写脚本监控内核网络栈行为。

    # 示例：监控 TCP 连接建立
    probe kernel.function("tcp_v4_connect") {
        printf("connect to %s\\n", daddr);
    }

==== TCP Tracepoints

    trace-cmd 是基于内核 ftrace 的轻量级追踪工具，通过 TCP 跟踪点捕获 TCP 状态变化、
    重传、丢包等事件，比 tcpdump 开销小得多，适合高并发场景。

    轻量级, linux内核4.16及以上

    用法示例:
        # 列出所有 tcp 相关 tracepoint
        trace-cmd list | grep tcp

        # 跟踪 TCP 丢包和重传
        trace-cmd record -e tcp:tcp_retransmit_skb -e tcp:tcp_drop

        # 查看结果
        trace-cmd report

    当系统中的网络连接数较多时，tcpdump抓包会对系统性能会产生较明显的影响;
    TCP Tracepoints则是比较轻量级的分析方案。

==== tc

    show/manipulate traffic control settings

    模拟网络异常:
        普通网络延迟: tc qdisc add dev eth0 root netem delay 100ms
        抖动延迟:
            可模拟较真实的差网络条件，延迟的同时伴随丢包
            该命令将eth0网卡的传输设置为100ms, 同时大约有30%的包会延迟±10ms发送
            tc qdisc add dev eth0 root netem delay 100ms 10ms 30%
        丢包1%:
            tc qdisc add dev eth0 root netem loss 1%
        重复包:
            tc qdisc add dev eth0 root netem duplicate 1%

    模拟数据包乱序:
        该命令将eth0网卡的传输设置为:有25%的数据包(50%相关)会被立即发送, 其他的延迟10毫秒
        tc qdisc change dev eth0 root netem delay 10ms reorder 25% 50%

    查看已经配置的网络条件:
        tc qdisc show dev eth0

    删除已经配置的网络条件:
        将以上命令中的add改为del

==== nc(netcat)
https://zh.wikipedia.org/zh-cn/Netcat

==== Nmap
用于网络发现和安全审计的网络安全
https://zh.wikipedia.org/zh-cn/Nmap

=== 丢包
==== 网卡
RingBuffer溢出丢包
网卡端口协商丢包
网卡流控丢包
报文mac地址丢包
其它网卡异常
    网卡firmware版本bug
    网卡插线网口规格等
    报文长度丢包

==== 驱动
网络接口丢包
netdev_max_backlog溢出丢包
单核负载高丢包

==== 协议栈
neighbor系统丢包
ip层丢包
tcp层丢包
udp层丢包
socket配置

==== 丢包排查通用方法

===== 网卡层面 — 查看硬件/驱动级丢包

    # 查看网卡详细统计（含丢弃、错误等）
    ethtool -S <interface> | grep -iE "drop|error|miss"

    # 查看接口汇总
    ip -s link show <interface>

===== 系统级统计

    # 各接口收发统计
    cat /proc/net/dev

    # softnet backlog 溢出（第 2 列 = dropped）
    cat /proc/net/softnet_stat

    # 协议层丢包
    cat /proc/net/netstat | grep -iE "drop|overflow"
    nstat -z | grep -iE "drop|retrans"

===== 实时监控

    watch -n 1 "nstat -z | grep -iE 'drop|retrans'"
    watch -n 1 "ethtool -S <interface> | grep -iE 'drop|error'"

=== 延迟

==== 延迟分类
传播延迟:: 信号在物理介质上传播的时间（由距离决定）
传输延迟:: 将数据包发送到链路上的时间（= 包大小 / 带宽）
处理延迟:: 路由器/交换机处理数据包的时间
队列延迟:: 数据包在设备队列中等待转发的时间

==== 排查方法

    # 逐跳延迟与丢率（推荐 mtr）
    mtr <target>
    mtr --tcp -P 80 <target>     # TCP 模式（穿透 ICMP 防火墙）
    mtr --udp -P 53 <target>     # UDP 模式

    # 查看当前 TCP 连接的 RTT
    ss -i
    # 输出示例：rtt:10.204/0.5 ms 表示平均 RTT 和偏差
    ss -ti | grep -E "rtt|cwnd"

    # 基础延迟测试
    ping -c 10 <target>          # 连续 10 次，看 min/avg/max/stddev

    # 特定协议延迟
    hping3 -S -p 80 <target>     # SYN 到 80 端口

    # tcpdump 分析握手延迟
    tcpdump -i any host <target> and tcp port 80 -tttt

==== 延迟优化思路
- 确认瓶颈：传播延迟（物理距离）VS 处理/队列延迟
- 传播延迟：更换 CDN、选择更近的机房
- 队列延迟：增大带宽、调整 qdisc 策略（如 fq_codel、BBR）
- 处理延迟：优化应用程序、增大缓冲区

=== TCP 连接状态分析

==== 查看各状态连接数分布

    ss -tan | awk '{print $1}' | sort | uniq -c

==== TIME_WAIT 过多

    ss -tan state time-wait | wc -l

    调优:
        sysctl -w net.ipv4.tcp_tw_reuse=1     # 启用 TIME_WAIT 复用（需配合 tcp_timestamps）
        sysctl -w net.ipv4.tcp_fin_timeout=30 # 缩短 FIN 超时（默认 60s）
    // tcp_tw_recycle 在 Linux 4.12+ 已从内核移除，不推荐

==== CLOSE_WAIT 过多

    ss -tan state close-wait | head -20

    常见原因：应用程序未正确关闭连接（未调用 close()）
    排查对应进程：ss -tanp state close-wait

==== SYN 半连接队列溢出

    查看:
        netstat -s | grep -E "SYN|overflow|drop"

    调优:
        sysctl -w net.ipv4.tcp_max_syn_backlog=4096
        sysctl -w net.core.somaxconn=4096
        sysctl -w net.ipv4.tcp_syncookies=1   # 启用 SYN Cookie（队列满时自动生效）
