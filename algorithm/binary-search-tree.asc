:toc:
:toclevels: 5
:hardbreaks-option:

== binary-search-tree

=== 概念
二叉搜索树(BST, binary search tree)，也称二叉查找树、有序二叉树。

注意: 二叉索引树(BIT / Fenwick Tree / 树状数组)是另一回事，用数组模拟树形结构，服务于前缀和/区间和。

对任意节点:
- 左子树所有节点的值 < 该节点的值
- 右子树所有节点的值 > 该节点的值
- 左右子树也分别是 BST

有的实现允许相等，相等节点统一放左或统一放右。

=== 性质
- 中序遍历得到严格递增序列。很多 BST 题本质是在中序过程中做「有序数组」能做的事。
- 查找 / 插入 / 删除都是 O(h)。平衡时 h = O(log n)，退化成链时 h = O(n)。
- C++ 的 std::map / std::set 底层一般是红黑树(一种自平衡 BST)。

=== 查找
一直向左或向右走，不必遍历整棵树。

[source, cpp]
.https://leetcode.com/problems/search-in-a-binary-search-tree/
----
TreeNode* searchBST(TreeNode* root, int val) {
    while (root && root->val != val) {
        root = val < root->val ? root->left : root->right;
    }
    return root;
}
----

=== 插入
空位即插入点。新节点总是作为叶子挂上去(不考虑自平衡)。

[source, cpp]
.https://leetcode.com/problems/insert-into-a-binary-search-tree/
----
TreeNode* insertIntoBST(TreeNode* root, int val) {
    if (!root) return new TreeNode(val);
    if (val < root->val) root->left = insertIntoBST(root->left, val);
    else root->right = insertIntoBST(root->right, val);
    return root;
}
----

=== 删除
三种情况:
1. 叶子: 直接删
2. 只有一个孩子: 用孩子顶替
3. 两个孩子: 用中序后继(右子树最小值)或中序前驱(左子树最大值)顶替，再删掉那个后继/前驱

[source, cpp]
.https://leetcode.com/problems/delete-node-in-a-bst/
----
TreeNode* deleteNode(TreeNode* root, int key) {
    if (!root) return nullptr;
    if (key < root->val) {
        root->left = deleteNode(root->left, key);
    } else if (key > root->val) {
        root->right = deleteNode(root->right, key);
    } else {
        if (!root->left) return root->right;
        if (!root->right) return root->left;
        TreeNode* succ = root->right;
        while (succ->left) succ = succ->left;
        root->val = succ->val;
        root->right = deleteNode(root->right, succ->val);
    }
    return root;
}
----

=== 常见思路
中序遍历:
验证合法性、众数、相邻最小差值、第 k 小，都可在中序过程中一次做完，不必先抽出整个数组。

利用有序性剪枝:
查找、LCA、修剪(trim)都可以根据目标值与当前节点的大小关系，直接丢掉一整侧子树。

验证 BST 的坑:
只比较父子不够。反例: 根 5，左孩子 1，右孩子 6，6 的左孩子 4，4 < 6 但 4 不该出现在 5 的右子树。
正确做法是带上下界递归，或中序检查是否严格递增。

[source, cpp]
.https://leetcode.com/problems/validate-binary-search-tree/
----
bool isValidBST(TreeNode* root) {
    return check(root, LONG_MIN, LONG_MAX);
}
bool check(TreeNode* node, long lo, long hi) {
    if (!node) return true;
    if (node->val <= lo || node->val >= hi) return false;
    return check(node->left, lo, node->val)
        && check(node->right, node->val, hi);
}
----

BST 的 LCA:
p、q 都在左侧则向左，都在右侧则向右，否则当前节点就是分岔点。普通二叉树的 LCA 见 tree.asc 的 lc236。

[source, cpp]
.https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-search-tree/
----
TreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) {
    while (root) {
        if (p->val < root->val && q->val < root->val)
            root = root->left;
        else if (p->val > root->val && q->val > root->val)
            root = root->right;
        else
            return root;
    }
    return nullptr;
}
----

构造:
有序数组/链表取中点当根，左右递归，得到高度平衡的 BST。

=== 例题
https://leetcode.com/tag/binary-search-tree/

查找 / 验证:
lc700e: https://leetcode.com/problems/search-in-a-binary-search-tree/
lc98m: https://leetcode.com/problems/validate-binary-search-tree/
lc700 的思路可直接套到「是否存在」。

中序:
lc530e: https://leetcode.com/problems/minimum-absolute-difference-in-bst/
lc783e: https://leetcode.com/problems/minimum-distance-between-bst-nodes/ (与 530 同题)
lc501e: https://leetcode.com/problems/find-mode-in-binary-search-tree/
lc230m: https://leetcode.com/problems/kth-smallest-element-in-a-bst/
lc173m: https://leetcode.com/problems/binary-search-tree-iterator/
lc653e: https://leetcode.com/problems/two-sum-iv-input-is-a-bst/

增删改:
lc701m: https://leetcode.com/problems/insert-into-a-binary-search-tree/
lc450m: https://leetcode.com/problems/delete-node-in-a-bst/
lc669m: https://leetcode.com/problems/trim-a-binary-search-tree/
lc99m: https://leetcode.com/problems/recover-binary-search-tree/

祖先 / 转换:
lc235m: https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-search-tree/ (最近公共祖先)
lc108e: https://leetcode.com/problems/convert-sorted-array-to-binary-search-tree/
lc109m: https://leetcode.com/problems/convert-sorted-list-to-binary-search-tree/
lc538m: https://leetcode.com/problems/convert-bst-to-greater-tree/
lc1038m: https://leetcode.com/problems/binary-search-tree-to-greater-sum-tree/

计数(卡特兰数，见 dp.asc):
lc96m: https://leetcode.com/problems/unique-binary-search-trees/
lc95m: https://leetcode.com/problems/unique-binary-search-trees-ii/

=== 参考
https://github.com/youngyangyang04/leetcode-master#%E4%BA%8C%E5%8F%89%E6%90%9C%E7%B4%A2%E6%A0%91
https://labuladong.github.io/algo/di-yi-zhan-da78c/shou-ba-sh-66994/diao-you-y-8dd33/
