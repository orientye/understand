:toc:
:toclevels: 5
:hardbreaks-option:

== bit-manipulation

=== 与运算&
判断是否为2次幂

    NO: if (n & (n - 1))

2次幂的性质: % -> &

    ringbuffer(如kfifo): in % size 可以转化为 in & (size – 1), 其中size为2次幂

=== 异或运算^
x ^ x = 0
x ^ 0 = x

=== 布隆过滤器(bloom filter)
布隆过滤器的核心是一个概率型数据结构，用于回答一个问题：“某个元素是否绝对不在一个集合中？”

特性：
如果它说“某个元素不在集合中”，那么这个结论是 100% 准确的。
如果它说“某个元素在集合中”，那么这个结论可能是错误的。

优点：
空间效率和查询时间都远远超过一般的算法

缺点：
- 有一定的假阳性率(false positive)：元素从未插入过，查询却可能说「可能在」。多个元素共用同一段 bit，哈希撞到已置位的位置就会误判。反过来，若某位为 0，一定没插入过——所以「不在」确定，「在」才可能错。
- 删除困难：插入只是把若干 bit 置 1，这些 bit 可能同时服务其它元素。若直接清 0，可能抹掉别的元素的证据，造成假阴性。经典布隆过滤器因此不提供安全删除；真要删可用 Counting Bloom Filter 等变体(每位改计数)，但空间更大。

参考:
https://en.wikipedia.org/wiki/Bloom_filter

=== 例题
https://leetcode.com/problem-list/bit-manipulation/

=== 参考
