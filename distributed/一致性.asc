:toc:
:toclevels: 5
:sectnums:
:sectnumlevels: 5
:hardbreaks-option:

== 一致性

=== 一致性模型
==== 强一致性模型
===== 线性一致性(Linearizable Consistency)
也称严格一致性(Strict Consistency)/原子一致性(Atomic Consistency)

    条件:
        1. 任何一次读都能读取到某个数据最近的一次写的数据。
        2. 所有进程看到的操作顺序都跟全局时钟下的顺序一致。

    应用场景:
        分布性锁、Leader 选举、唯一性约束等

* 对于写操作来说:
    ** 任意两个写操作 x1 和 x2：如果写 x1 操作和写 x2 操作有重叠，那么可能 x1 覆盖 x2，也可能 x2 覆盖 x1；
    ** 如果写 x1 操作在写 x2 开始前完成，那么 x2 一定覆盖 x1。
* 对于读操作来说:
    ** 写操作完成后，所有的客户端都能立即观察到
    ** 对于多个客户端来说，必须读取到一样的顺序。

线性一致性保证了所有的读取都可以读到最新写入的值，即一旦新的值被写入或读取，所有后续的读都会看到写入的值，直到它被再次覆盖。在线性一致性模型中不论是数据的覆盖顺序还是读取顺序，都是按时间线从旧值向新值移动，而不会出现旧值反转的情况。

有一种说法是“在分布式系统中不可能实现”，通常源于对 “全局完全一致的物理时钟” 的假设。确实，由于网络延迟的不确定性和狭义相对论效应，无法获得完美同步的、无误差的全局物理时钟。

然而，线性一致性并不要求系统拥有完美的物理时钟！它要求的是外部观察者（客户端）感知到的执行顺序，必须与一个虚拟的、单调递增的时间线一致，这个时间线尊重真实时间的先后关系（Real-time Order）。实现线性一致性时，不需要在所有服务器上同步物理时钟，而是通过算法和协议来构建这个“虚拟时间线”。

===== 顺序一致性(Sequential Consistency)

    条件:
        1. 任何一次读写操作都是按照某种特定的顺序。
        2. 所有进程看到的读写操作顺序都保持一致。

Leslie Lamport
1979 How to Make a Multiprocessor Computer That Correctly Executes Multiprocess Program

* 对于写操作来说:
    ** 任意两个写操作 x1 和 x2：如果写 x1 操作和写 x2 操作有重叠，那么可能 x1 覆盖 x2，也可能 x2 覆盖 x1；
    ** 当写 x1 操作在写 x2 开始前完成，如果两个写操作没有因果关系，当写 x1 操作在写 x2 开始前完成，那么有可能 x1 覆盖 x2，也有可能 x2 覆盖 x1；如果两个写操作有因果关系，即同一台机器节点先写 x1，或者先看到 x1 然后再写 x2，则所有节点必须用 x2 覆盖 x1。
* 对于读操作来说:
    ** 如果写操作 x2 覆盖 x1 完成，那么如果一个客户端到 x2 后，它就无法读取到 x1 了，但是这个时候，其他的客户端还可以观察到 x1；
    ** 对于多个客户端来说，必须观察到一样的顺序。

* 与线性一致性相比，顺序一致性在一致性方面有两点放松:
    ** 对于写操作，对没有因果关系的非并发写入操作，不要求严格按时间排序；
    ** 对于读操作，只要求所有的客户端观察到的顺序一致性，不要求写入后，所有的客户端都必须读取新值。

==== 弱一致性模型
===== 因果一致性(Causal Consistency)

    条件:
        1. 所有进程必须以相同的顺序看到具有因果关系的读写操作。
        2. 不同进程可以以不同的顺序看到并发的读写操作。
    因果关系: 如果事件B是由事件A引起的或者受事件A的影响，那么这两个事件就具有因果关系。

Mustaque Ahamad, Gil Neiger, James E. Burns, Prince Kohli, Phillip W. Hutto
1991 Causal memory: definitions, implementation, and programming

* 对于写操作来说:
    ** 任意两个写操作 x1 和 x2：如果两个写操作没有因果关系，那么写 x1 操作在写 x2 开始前完成，有的节点是 x1 覆盖 x2，有的节点则 x2 可能覆盖 x1；
    ** 如果两个写操作有因果关系，即同一台机器节点先写 x1，或者先看到 x1 然后再写 x2，则所有节点必须用 x2 覆盖 x1。

* 对于读操作来说:
    ** 如果写操作 x2 覆盖 x1 完成，那么如果一个客户端到 x2 后，它就无法读取到 x1 了，但是这个时候，其他的客户端还可以观察到 x1。

* 相对于顺序一致性来说，因果一致性在一致性方面有两点放松:
    ** 对于写操作，对没有因果关系的非并发写入操作，不仅不要求按时间排序，还不再要求节点之间的写入顺序一致了；
    ** 对于读操作，由于对非并发写入顺序不再要求一致性，所以自然也无法要求多个客户端必须观察到一样的顺序。

Q: 为什么说因果一致性是一种弱化的顺序一致性模型?
A: 顺序一致性虽然不保证事件发生的顺序与实际发生的保持一致，但是能保证所有进程看到的读写操作顺序是一样的。
而因果一致性更进一步弱化了顺序一致性中对读写操作顺序的约束，仅保证有因果关系的读写操作有序，没有因果关系的读写操作(并发事件)则不做保证。也就是说如果是无因果关系的数据操作不同进程看到的值是有可能是不一样，而有因果关系的数据操作不同进程看到的值保证是一样的。

===== 最终一致性(Eventual Consistency)
是更加弱化的一致性模型
因果一致性起码还保证了有因果关系的数据不同进程读取到的值保证是一样的，而最终一致性只保证所有副本的数据最终在某个时刻会保持一致。

1. "最终"到底是多久？通常来说，实际运行的系统需要能够保证提供一个有下限的时间范围。
2. 多副本之间对数据更新采用什么样的策略？一段时间内可能数据可能多次更新，到底以哪个数据为准？一个常用的策略以时间戳最新的数据为准。

由于最终一致性对数据一致性的要求比较低，在对性能要求高的场景中是经常使用的一致性模型。

Amazon 的 CTO Werner Vogels
2009 Eventual Consistency，它是 Amazon 基于 Dynamo 等系统的实战经验所总结的一种很务实的实现。

简单地理解:
对于同一台机器的两个写操作 x1 和 x2 来说: 如果写 x1 操作在写 x2 开始前完成，那么所有节点在最终某时间点后，都会用 x2 覆盖 x1。
对于读操作来说: 在数据达到最终一致性的过程中，客户端的多次观察可以看到的结果是 x1 和 x2 中的任意值；在数据达到最终一致性的过程后，所有客户端都将只能观察到 x2。

===== 以客户端为中心的一致性(Client-centric Consistency)
以客户端为中心的一致性是指分布式系统向单个客户端保证其访问数据时的一致性体验，而不要求在任意时刻、所有客户端看到的数据都完全一致（即强一致性）。

前面讨论的一致性模型都是针对数据存储的多副本之间如何做到一致性，考虑这么一种场景: 在最终一致性的模型中，如果客户端在数据不同步的时间窗口内访问不同的副本的同一个数据，会出现读取同一个数据却得到不同的值的情况。为了解决这个问题，提出了以客户端为中心的一致性模型。

以客户端为中心的一致性为单一客户端提供一致性保证，保证该客户端对数据存储的访问的一致性，但是它不为不同客户端的并发访问提供任何一致性保证。

举个例子: 客户端A在副本M上读取x的最新值为1，假设副本M挂了，客户端A连接到副本N上，此时副本N上面的x值为旧版本的0，那么一致性模型会保证客户端A读取到的x的值为1，而不是旧版本的0。一种可行的方案就是给数据x加版本标记，同时客户端A会缓存x的值，通过比较版本来识别数据的新旧，保证客户端不会读取到旧的值。

以客户端为中心的一致性包含了四种子模型:

    1. 单调读一致性(Monotonic-read Consistency)

        如果一个进程读取数据项x的值，那么该进程对于x后续的所有读操作要么读取到第一次读取的值要么读取到更新的值。
        即保证客户端不会读取到旧值。

    2. 单调写一致性(Monotonic-write Consistency)

        一个进程对数据项x的写操作必须在该进程对x执行任何后续写操作之前完成。
        即保证客户端的写操作是串行的。

    3. 读写一致性(Read-your-writes Consistency)

        一个进程对数据项x执行一次写操作的结果总是会被该进程对x执行的后续读操作看见。
        即保证客户端能读到自己最新写入的值。

    4. 写读一致性(Writes-follow-reads Consistency)

        同一个进程对数据项x执行的读操作之后的写操作，保证发生在与x读取值相同或比之更新的值上。
        即保证客户端对一个数据项的写操作是基于该客户端最新读取的值。

==== 小结
- 首先，一致性级别最强的是线性一致性，它是指所有进程看到的事件历史一致有序，并符合时间先后顺序，单个进程遵守 program order，并且有 total order。
- 其次，是顺序一致性，它是指所有进程看到的事件历史一致有序，无实时约束，单个进程遵守 program order，也有 total order。
- 再次，是因果一致性，它是指所有进程看到的因果事件历史一致有序，单个进程遵守 program order，不对没有因果关系的并发排序。
- 最后，是最终一致性，它是指所有进程看到的最终一致。

==== 参考
https://en.wikipedia.org/wiki/Consistency_model
极客时间 深入浅出分布式技术原理 - 一致性与共识

=== 一致性与共识

    一致性(Consistency)通常指指分布式系统中多个副本对外呈现的数据的状态。
    共识(Consensus)则描述了分布式系统中多个节点之间，彼此对某个状态达成一致结果的过程。

    一致性描述的是结果状态，共识则是一种手段。
    达成某种共识并不意味着就保障了(强)一致性。只能说共识机制，能够实现某种程度上的一致性。

    一致性的实现，核心过程往往需要通过共识算法来达成。
    但整个系统的一致性不仅仅取决于共识算法，例如可能还依赖客户端策略。

    共识的应用:
        逻辑时间的共识，用于决定时间发生的顺序
        互斥性的共识，用于决定谁正拥有访问的资源
        协调者的共识，用于决定谁是当前的leader

=== 共识算法
==== 拜占庭容错 vs 非拜占庭容错
根据是否具备处理"作恶节点"的能力，共识算法可分为两大类:

拜占庭容错算法(Byzantine Fault Tolerance, BFT)::
  能容忍节点存在任意恶意行为（包括篡改数据、伪造消息、合谋攻击等），
  即使部分节点作恶，系统仍能达成正确共识。
  代表算法: PoW、PBFT、HotStuff 等。
  适用: 相对开放、不可信的场景，如公链、联盟链。

非拜占庭容错算法(Non-Byzantine Fault Tolerance)::
  只能容忍节点"无响应/宕机"这种非作恶的故障（Crash Fault），
  一旦节点作恶，整个系统的一致性就会被破坏。
  代表算法: 2PC、3PC、Raft、Paxos、ZAB 等。
  适用: 封闭、绝对可信的场景，如私链、数据中心内部集群。

.BFT vs 非 BFT
[cols="2,3,3",options="header"]
|===
| 维度 | 拜占庭容错 (BFT) | 非拜占庭容错 (非 BFT)
| 容错模型 | 任意行为（宕机 + 作恶） | 仅宕机（Crash Fault）
| 典型阈值 | 容错 `≤ (n-1)/3` 个拜占庭节点 | 容错 `≤ (n-1)/2` 个宕机节点
| 性能代价 | 高（加密验证、多轮通信） | 低
| 代表算法 | PoW、PBFT、HotStuff、Tendermint | Raft、Paxos、ZAB、2PC
| 适用场景 | 公链、联盟链 | 私链、数据中心、分布式数据库
|===

==== 2PC(Two-Phase Commit)
===== 概念
准备阶段（Prepare Phase）：协调者询问所有参与者是否可以提交
提交阶段（Commit Phase）：根据参与者的响应决定提交或中止

协调者（Coordinator）：事务的发起者和决策者，通常就是发起事务的应用或单独的事务管理器
参与者（Participant）：实际执行事务分支的节点（如数据库、服务），也称为资源管理器（RM）

===== 准备阶段(Prepare)
- 1、协调者向所有参与者发送 Prepare（预提交） 请求，并开始等待各参与者的响应。

- 2、参与者执行事务操作：
    * 写 undo/redo 日志到本地磁盘（持久化）
    * 锁定相关资源

- 3、参与者根据本地事务执行结果回复协调者：
    * 若成功：返回 "Yes"（同意提交）
    * 若失败：返回 "No"（不同意提交）

===== 提交阶段(Commit)
- 协调者根据所有参与者的投票结果决定最终操作：

- 情况一：所有参与者都返回 "Yes"
    * 协调者发送 Commit 命令给所有参与者
    * 参与者收到 Commit 后：
        ** 正式提交本地事务
        ** 释放锁定的资源
        ** 向协调者发送 "Ack"（确认）
    * 协调者收到所有 Ack 后，事务完成

- 情况二：任一参与者返回 "No" 或超时
    * 协调者发送 Rollback（回滚） 命令给所有参与者
    * 参与者收到 Rollback 后：
        ** 利用 undo 日志回滚本地事务
        ** 释放锁定的资源
        ** 向协调者发送 "Ack"
    * 协调者收到所有 Ack 后，事务回滚完成

===== 特点
- 优点: 原理简单，实现方便
- 缺点: 阻塞，单点，数据不一致
- 应用: 数据库，JTA(Java Transaction API)等，Google Percolator

Q: 如何理解阻塞?
A: 参与者需要等待两个阶段都结束了，才会释放资源锁，这也是2PC性能较差的原因。

Q: 如何理解单点?
A: 事务管理器(协调者)存在单点。

Q: 如何理解数据不一致?
A: 协调者向参与者发送commit请求之后，发生了局部网络异常或者在发送commit请求过程中协调者发生了故障，导致只有一部分参与者接受到了commit请求执行commit操作。但是其他部分未接到commit请求的机器则无法执行事务提交。于是整个系统便出现了数据不一致性的现象。

===== 解决方案
====== XA
2PC的传统方案是在数据库层面实现的，MySQL，Oracle等都支持2PC协议。
为了统一标准，国际开放标准组织Open Group定义了分布式事务处理模型DTP(Distributed Transaction Processing Reference Model)。
DTP模型定义TM(事务管理器)和RM(资源管理器，即参与者)之间的接口规范叫XA，可以简单的理解为数据库提供的2PC接口协议。
基于数据库的XA协议来实现2PC称为XA方案。

推荐使用的场景：
事务参与者少：2-3 个数据库，且网络稳定
强一致性要求：业务上不允许中间状态（如金融核心交易）
短事务：事务执行时间短
传统架构：已有基于应用服务器（如 WebLogic）的遗产系统
技术栈匹配：所有资源都支持 XA，且有成熟运维经验

数据库层 XA 事务的核心价值在于提供标准化的、跨数据库的强一致性保证。它是企业级传统架构中的成熟解决方案，但在云原生、微服务时代逐渐被更灵活的最终一致性方案替代。
如果是金融核心交易，且技术栈都是支持 XA 的传统数据库，那么 XA 仍是可靠选择；
如果是互联网高并发业务，建议优先考虑 Saga、TCC、消息最终一致性等方案。

====== MySQL 2PC
http://mysql.taobao.org/monthly/2020/05/07/
https://dev.mysql.com/doc/refman/8.0/en/xa.html

====== Google Percolator
http://mysql.taobao.org/monthly/2018/11/02/

====== TiDB与Percolator
https://tidb.net/blog/f537be2c

====== Seata
http://seata.io/zh-cn/docs/dev/mode/xa-mode.html

===== 参考
https://icyfenix.cn/architect-perspective/general-architecture/transaction/global.html

==== 3PC(Three-Phase Commit)
===== 概念
优点: 降低了阻塞问题
缺点: 同样存在很多问题

===== 参考
https://icyfenix.cn/architect-perspective/general-architecture/transaction/global.html

==== PBFT
===== 概念
Practical Byzantine Fault Tolerance，即实用拜占庭容错算法。
分布式系统中有两种Fault: Crash Fault与Byzantine Fault

- 核心目标
在分布式系统中，多个节点需要就某个值或某个状态达成一致。但系统中可能存在一些“恶意节点”（即“拜占庭节点”），它们会任意行事：可能不发送消息、发送错误消息、对不同节点发送不同消息，企图破坏系统的一致性。
PBFT 的目标就是: 在存在不超过一定数量的拜占庭节点时，系统依然能够达成共识，保证所有诚实节点行动一致。

- 关键假设与前提条件
PBFT 不是万能的，它在一个特定的模型下工作：
** 节点类型: 系统中的节点分为两类：
    *** 诚实节点: 严格遵守协议规则的节点。
    *** 拜占庭节点: 行为任意的恶意节点，数量为 f。
** 容错能力: PBFT 可以容忍最多 f 个拜占庭节点。要保证系统正常运作，总的节点数 N 必须满足: N ≥ 3f + 1。
    *** 为什么是 3f+1？
        **** 要达成一致，诚实节点的数量必须超过总节点数的一半（N - f > f），否则恶意节点可以联合起来形成多数派，欺骗诚实节点。这推导出 N > 2f。
        **** 同时，在投票过程中，即使有 f 个节点无响应，系统仍需收到足够多的响应来继续前进。综合起来，最少的节点数就是 3f + 1。
** 网络环境: 假设是部分同步网络。消息最终会被送达，但可能会有延迟。
** 密码学安全: 使用非对称加密技术（如数字签名）进行身份认证，确保消息不会被篡改和伪造。

- 优缺点
** 优点:
    *** 高性能: 与传统的工作量证明（PoW）等算法相比，它不需要消耗大量算力，交易确认速度快，吞吐量高。
    *** 最终性: 一旦达成共识，结果就是最终的，不会像 PoW 那样存在回滚的可能。
    *** 理论完备: 在满足 N ≥ 3f + 1 的条件下，能严格保证安全性和活性。
** 缺点:
    *** 扩展性差: 由于需要所有节点之间进行多轮广播通信，通信复杂度为 O(N²)。当节点数量（N）非常大时（例如成千上万个），网络开销会变得巨大，性能急剧下降。这限制了它不适合完全开放、节点数量巨大的公链系统。
    *** 复杂度高: 协议本身比较复杂，实现难度大，尤其是视图更换和日志垃圾回收等细节。

===== 核心流程
PBFT 的共识过程分为三个阶段：**Pre-Prepare**（预准备）、**Prepare**（准备）、**Commit**（提交）。主节点（Primary）负责提议，备份节点（Backup）负责验证和投票。

视图（View）变更周期: 每个视图有一个主节点，主节点按 `p = v mod N` 轮流担任（`v` 是视图编号，`N` 是节点总数）。如果主节点发生故障（超时无响应），则触发 View-Change（视图更换）协议。

** N ≥ 3f + 1 为什么是这个数字?
`N - f > 2f ⇒ N > 3f ⇒ N ≥ 3f + 1`

** 正常流程（无拜占庭故障的情况）:

客户端 C 向主节点 0 发送请求 `REQUEST`，主节点 0 将请求编号并广播给所有备份节点。

步骤:

1. **Pre-Prepare（预准备）**:
   * 主节点（Primary）为客户端请求分配一个序列号 `n`，构造 `⟨⟨PRE-PREPARE, v, n, d⟩, σₚ⟩` 消息，广播给所有 Backup 节点。
   * 其中 `v` 是当前视图编号，`d` 是请求消息的摘要（哈希值），`σₚ` 是主节点的签名。
   * Backup 节点收到后验证：签名是否正确？视图 `v` 是否正确？是否在该视图内收到过相同 `n` 的其他 Pre-Prepare？如果验证通过则接受。

2. **Prepare（准备）**:
   * Backup 节点接受 Pre-Prepare 后，构造 `⟨⟨PREPARE, v, n, d, i⟩, σᵢ⟩` 消息（`i` 为自身节点编号），广播给所有其他节点（包括主节点）。
   * 每个节点收集 Prepare 消息。当节点收到 **2f** 个来自不同 Backup 节点的 Prepare 消息（加上自己共 **2f+1** 个，即法定人数）并与之前收到的 Pre-Prepare 匹配时，则进入 **Prepared** 状态。
   * 这一步确保所有诚实节点对请求 `m` 在视图 `v` 内的序号 `n` 达成一致（*If a correct node prepares at sequence number n , then any other correct node that also prepares at n must prepare with the same digest d and same view v.*）。

3. **Commit（提交）**:
   * 节点进入 Prepared 状态后，构造 `⟨⟨COMMIT, v, n, d, i⟩, σᵢ⟩` 消息，广播给所有其他节点。
   * 每个节点收集 Commit 消息。当节点收到 **2f+1** 个匹配的 Commit 消息（包含自己的）时，请求 `m` 即被 **Committed**。
   * 此时节点执行请求 `m` 中的操作，并将结果 `r` 返回给客户端 C。
   * 这一步确保即使 Prepare 阶段的消息被延迟或丢失，足够多的节点也能确保跨视图的全局一致性。

** 流程图示意:

```
Client   Primary(0)   Backup(1)   Backup(2)   Backup(3)
  |          |           |           |           |
  | REQUEST  |           |           |           |
  |--------->|           |           |           |
  |          | Pre-Prepare(v,n,d)    |           |
  |          |---------->|---------->|---------->|
  |          |           |           |           |
  |          | Prepare(v,n,d,i)      |           |
  |          |<--------->|<--------->|<--------->|
  |          |           |           |           |
  |          | Commit(v,n,d,i)       |           |
  |          |<--------->|<--------->|<--------->|
  |          |           |           |           |
  |<---------|           |           |           |
  |   reply  |           |           |           |
```

> 注: 图中假设 N=4、f=1，即 N = 3f + 1 的最小配置。Prepare 阶段收集 2f+1=3 个匹配（含自己）即达成；Commit 阶段同理。一次共识需要 3 轮全广播，通信复杂度为 O(N²)。

** 关键特性:

* **三阶段提交**: Pre-Prepare → Prepare → Commit。Prepare 保证同一视图内的序号唯一性; Commit 保证跨视图的全局一致性（即使发生视图更换）。
* **消息认证**: 每条消息都带有发送者的数字签名 `σ`，防止伪造和篡改。
* **垃圾回收（Checkpoint / Garbage Collection）**: 节点每执行一定数量的请求后，创建一个 Checkpoint（检查点），广播 Checkpoint 消息。当收到 2f+1 个相同 seq 的 Checkpoint 消息后，该 Checkpoint 成为 Stable Checkpoint（稳定检查点），此时可以安全地删除该序号之前的日志，防止日志无限增长。
* **视图更换（View-Change）**: 当 Backup 节点检测到主节点无响应（超时）时，发起 View-Change 协议，选举新节点为主节点，并确保已经 Commit 的请求不会丢失。

===== PBFT-vs-Raft

[cols="2,3,3",options="header"]
|===
| 对比维度 | PBFT | Raft
| **容错类型** | 拜占庭容错（容忍恶意节点） | 非拜占庭容错（仅容忍 Crash Fault）
| **最小节点数** | `N ≥ 3f + 1`（至少 4 节点容 1 故障） | `N ≥ 2f + 1`（至少 3 节点容 1 故障）
| **多数派定义** | `2f + 1`（超 2/3） | `(N/2) + 1`（过半）
| **阶段数** | 三阶段：Pre-Prepare → Prepare → Commit | 两阶段：Leader 选举 → 日志复制
| **Leader 角色** | 主节点（Primary）按视图轮流，`p = v mod N` | 强 Leader，所有写入必须经过 Leader
| **通信模型** | 全节点间多轮全广播 | 日志单向从 Leader 流向 Follower
| **通信复杂度** | `O(N²)` | `O(N)`
| **日志排序** | Pre-Prepare 序列号 `n` 全局排序 | Leader 的 term + index 全局排序
| **日志冲突处理** | Prepare 阶段 `2f` 验证确保同一视图内序号唯一 | Follower 日志与 Leader 不一致时强制覆盖
| **日志压缩** | Checkpoint + Stable Checkpoint | Snapshot + Log Compaction
| **视图变更 / 选举** | View-Change 需收集 `2f+1` 票，复杂度高 | Candidate 发起投票，获得多数即当选，流程简单
| **变更期间可用性** | 视图变更期间系统暂停处理新请求 | 选举期间系统暂停，当选后立即恢复
| **客户端请求提交** | 向主节点发送请求 | 向 Leader 发送请求
| **结果确认** | 需等待 `f+1` 个副本返回相同结果 | 等待 Leader 确认即可
| **重试机制** | 超时后向所有节点广播请求，触发视图变更 | 超时后重试或寻找新 Leader
| **消息认证** | 数字签名 `σ` 防伪造 | 依赖可信环境，无防伪造机制
| **分裂防护** | `N ≥ 3f+1` 条件下不可能 split-brain | 网络分区时旧 Leader 被隔离后不会产生分裂
| **恶意节点破坏力** | 最多容忍 `f` 个拜占庭节点联合攻击 | 单个恶意节点即可破坏整个系统
| **延迟** | 3 轮全广播，延迟较高 | 1 轮 RPC + 心跳维持，延迟低
| **吞吐量瓶颈** | 消息量随 `N²` 增长 | 主要受 Leader 单节点处理能力限制
| **最佳节点规模** | ≤ 20 节点 | 数十到上百节点均可行
| **复杂度** | 协议复杂，尤其视图更换和垃圾回收 | 相对简单，易理解和实现
| **典型系统** | Hyperledger Fabric（早期版本）、Zilliqa | etcd、Consul、Kafka（基于 Raft 的共识层）
|===

===== 参考
https://www.geeksforgeeks.org/practical-byzantine-fault-tolerancepbft/

==== Gossip
Gossip protocol也叫Epidemic Protocol(流行病协议)

Gossip过程是由种子节点发起，当一个种子节点有状态需要更新到网络中的其他节点时，它会随机的选择周围几个节点散播消息，收到消息的节点也会重复该过程，直至最终网络中所有的节点都收到了消息。这个过程可能需要一定的时间，由于不能保证某个时刻所有节点都收到消息，但是理论上最终所有节点都会收到消息，因此它是一个最终一致性协议。

使用Gossip的典型系统:
bitcoin(bitcoin除了gossip还依赖pow共识)

- 优点:

    扩展性性好
        网络可以允许节点的任意增加和减少，新增加的节点的状态最终会与其他节点一致。
    容错性强
        网络中任何节点的宕机和重启都不会影响Gossip消息的传播，Gossip协议具有天然的分布式系统容错特性。
    去中心化
        Gossip协议不要求任何中心节点，所有节点都可以是对等的
        任何一个节点无需知道整个网络状况，只要网络是连通的，任意一个节点就可以把消息散播到全网。
    一致性收敛
        Gossip协议中的消息会以一传十、十传百一样的指数级速度在网络中快速传播
        因此系统状态的不一致可以在很快的时间内收敛到一致。消息传播速度达到了logN
    简单
        实现Gossip十分简单

- 缺点:

    消息延迟
        节点随机向少数几个节点发送消息，消息最终是通过多个轮次的散播而到达全网，不可避免的造成消息延迟。
    消息冗余
        节点定期随机选择周围节点发送消息，而收到消息的节点也会重复该步骤
        因此不可避免地引起同一节点多次接收同一消息，增加消息处理的压力。
        一次通信会对网路带宽、CUP资源造成很大的负载，而这些负载又受限于通信频率，该频率又影响着算法收敛的速度。
    拜占庭问题
        如果有一个恶意传播消息的节点，Gossip协议的分布式系统就会出问题。

参考: https://zhuanlan.zhihu.com/p/41228196

==== Paxos
Leslie Lamport 于 1989 年提出该算法。论文《The Part-Time Parliament》1990 年投稿，因议会隐喻过于晦涩被拒，1998 年才正式发表；人们真正读懂它主要靠 2001 年的《Paxos Made Simple》。

Paxos 是崩溃容错（非拜占庭）、异步/部分同步模型下最早被广泛引用、并深刻影响后来工业实现的共识算法之一，但并非「第一个被严格证明正确的共识算法」：Viewstamped Replication（Oki & Liskov, 1988）更早发表，Dwork–Lynch–Stockmeyer（1988）在部分同步模型下也有带证明的共识算法。

Paxos 定义了三种逻辑角色（同一节点可兼任）：**Proposer**（提议者）、**Acceptor**（批准者）、**Learner**（学习者）。Basic Paxos 通过 **Prepare → Accept** 两阶段、多数派 quorum，对**单个值**达成共识。故障模型为崩溃容错，节点数满足 `N ≥ 2f + 1`。

Paxos 的核心贡献是用**多数派交叠**保证 Safety，并把 Safety 与 Liveness 分开——而不是把共识拆成多个独立子问题（那是 Raft 的设计叙事）：

* **Safety 始终成立**：一旦某个值被选定（Chosen），之后任何再被选定的值必须与之相同。即使网络异步、节点崩溃，也不会选出两个不同的值。
* **Liveness 不保证**：符合 FLP 不可能原理。多个 Proposer 竞争时可能永远无法完成 Accept（活锁）。活性依赖部分同步：足够长时间内只有一个活跃 Proposer 才能推进。

Raft、ZAB 与 Multi-Paxos 在安全性上等价，同属「多数派 + 稳定 Leader」的崩溃容错共识家族，但不宜说成「Paxos 的简化版」：Raft 强调独立设计与更约束的结构；ZAB 更接近 primary-backup / 原子广播。Paxos 里的 Leader（distinguished proposer）只是**活性与性能优化**——没有 Leader 仍然安全，只是可能活锁；Raft 的 Leader 则是协议的必要组成部分。

===== Basic Paxos
Basic Paxos 只决定一个值。每个提案带一个全局唯一且全序的编号 n（ballot / proposal number）。两阶段规则如下：

* **Phase 1 Prepare(n)**：Proposer 向 Acceptor 广播 Prepare(n)。Acceptor 若 n 大于自己见过的最大编号，则回复 Promise：承诺不再接受编号小于 n 的提案，并返回自己已 Accepted 的最高编号提案（若有）。
* **Phase 2 Accept(n, v)**：Proposer 收到多数 Promise 后确定值 v——若 Promise 中带有已接受的值，**必须沿用其中编号最高的那个**；否则使用自己提议的值。然后广播 Accept(n, v)。Acceptor 若尚未对更高编号做过 Promise，则 Accepted。
* **Chosen**：某个 `(n, v)` 被多数 Acceptor Accepted 后，值 v 被选定。Learner 还需被通知（由 Acceptor 广播，或向多数派查询），否则「选定了」但没有节点知道。

**活锁**：提案编号本就要求全局唯一，编号「冲突」本身不是问题。真正的问题是 dueling proposers——两个 Proposer 交替用更高编号打断对方的 Accept，Phase 2 永远完不成。解决办法是选出 distinguished proposer，或随机退避。

Paxos 的两阶段**不是**两阶段提交（2PC）。2PC 在协调者故障时会阻塞；Paxos 提出更高编号的 ballot 即可更换 Proposer 继续推进。这是两者最关键的区别。

===== Multi-Paxos
* Multi-Paxos 是 Basic Paxos 面向状态机复制的用法：把 Paxos 应用到日志槽位（log slot）序列上，每个 slot 独立运行一次 Paxos，从而就一条命令日志达成共识。
* **Basic Paxos 的问题**：每一个值都要完整的两阶段（每阶段一次多数派往返）。多个 Proposer 竞争时会出现 dueling proposers 活锁，性能低下。
* **优化**：选出稳定 Leader 后，Prepare 按 **ballot（提案编号）** 做一次——可覆盖一批甚至全部未来 slot——同一 ballot 下后续 slot 只需 Accept。前提是该 ballot 仍是最高的；一旦出现更高编号的 Prepare，必须重新 Phase 1。Phase 1 若发现某些 slot 已有被接受的值，Leader **必须沿用编号最高的那个值**，不能换成自己的。
* **空洞**：各 slot 独立，允许乱序选定，因此日志可以有空洞。这与 Raft 的严格连续日志不同。但状态机执行前通常仍要用 no-op 填洞——「协议允许空洞」不等于「状态机可以跳着执行」。
* **角色**：Proposer 通常只由 Leader 担当，Acceptor 由所有节点承担，Learner 通过读取多数派或 Leader 推送来学习已提交的值。
* **实现差异**：原始论文对 Leader 选举、日志压缩、成员变更等工程细节着墨不多，议会隐喻也加重了理解成本。Google Chubby 的《Paxos Made Live》（2007）是工程缺口的经典出处。Lamport 后续另有 Cheap Paxos、Fast Paxos、Vertical Paxos、Stoppable Paxos 及 TLA⁺ 规范。Flexible Paxos 进一步指出：两阶段不必都用多数派，只要 Phase 1 与 Phase 2 的 quorum 相交即可。
* **典型系统**：Chubby、Spanner 的 Paxos group。

参考:
https://lamport.azurewebsites.net/pubs/paxos-simple.pdf
https://www.usenix.org/legacy/events/nsdi07/tech/chandra.html
https://www.zhihu.com/question/52337912

==== Raft
===== 概念
Diego Ongaro 与 John Ousterhout 提出（论文《In Search of an Understandable Consensus Algorithm》，USENIX ATC 2014；更早的技术报告约 2013 年），以**可理解性**为首要设计目标。故障模型与 Paxos 相同：崩溃容错（非拜占庭），`N ≥ 2f + 1`。Raft 比 Paxos 更容易理解的几个关键原因：

1. **强 Leader**：Paxos 中 Leader 仅为活性与性能优化，任何节点随时可发起提议；Raft 则强制所有写入必须经过 Leader，日志单向流动（Leader → Follower），大幅降低了需要考虑的状态空间。
2. **角色分离为明确状态机**：Paxos 的 Proposer/Acceptor 是逻辑角色，一个节点在不同阶段扮演不同角色；Raft 将节点划分为 **Leader、Follower、Candidate** 三种互斥状态，在任何时刻每个节点只处于一种状态，状态转换清晰。
3. **选举纳入共识**：Paxos 的 Leader 选举和 Basic Paxos 共识是两套正交协议；Raft 把 Leader 选举当作达成共识的第一步，与日志复制组成统一流程（选举 → 复制，这里的「两阶段」不是 2PC，也不是 Paxos 的 Prepare/Accept）。
4. **日志连续性假设**：Paxos 允许日志有空洞（各 log slot 独立选定），需要额外的机制补齐；Raft 要求日志严格连续递增，日志匹配特性（Log Matching Property）大幅简化了日志一致性检查。
5. **模块化分解**：Raft 将共识问题拆解为 Leader 选举、日志复制、安全性、成员变更等独立子问题，每个子问题可单独理解和实现。

像 Raft 一样，Viewstamped Replication（VR）和 ZooKeeper（ZAB）也是基于领导人的。但 Raft 比它们更少机制，因为它尽可能削减了非领导人的功能：日志只从 Leader 发给其他人（AppendEntries 单向向外）。VR 在选举过程中领导人可以接收日志，流动是双向的，因此更复杂。根据 ZooKeeper 公开资料，ZAB 的日志也可以双向传输，但其实现更接近 Raft。

Raft 动画演示：
https://thesecretlivesofdata.com/raft/

===== 实现
Raft 的实现可以分解为三个核心子问题：**Leader 选举**、**日志复制**、**安全性**。下面分别阐述其核心机制和实现要点。

====== Leader 选举
Raft 使用**选举超时**（election timeout）触发选举，不要和心跳搞混：心跳是 Leader 发出的空 AppendEntries，选举超时才是 Follower 发起选举的计时器。节点启动时处于 **Follower**，持续等待心跳。若随机超时时间内未收到（论文建议约 150–300 ms），则转变为 **Candidate** 并发起选举：

1. **任期递增**：Candidate 将 currentTerm +1，先给自己投票，再向其他节点广播 RequestVote。
2. **投票规则**：每个节点在每个 term 内最多投一票，先到先得（投给第一个合法请求）。另有日志限制：若 Candidate 的日志比自己旧，则拒绝。新旧比较先看最后一条日志的 term，term 相同再看 index。这不是「在多个 Candidate 里挑更新的那个」，而是「拒绝日志更旧的 Candidate」。
3. **多数派与退位**：Candidate 获得多数选票后成为 Leader。若收到更高 term 的 RPC，或发现已有同 term 的 Leader 心跳，则立即退回 Follower。
4. **随机超时**：选举超时在固定区间内随机取值，降低同时选举导致的选票分裂（split vote）。若分裂，本轮无人当选，超时后进入新 term 再选。
5. **心跳维持**：Leader 当选后定期发送 AppendEntries（空条目作心跳）以维持权威。

时序关系（论文）：broadcastTime ≪ heartbeatInterval ≪ electionTimeout。心跳间隔必须远小于选举超时，否则 Follower 会误以为 Leader 挂了。

====== 日志复制
Leader 负责处理所有客户端请求，流程如下：

1. **追加到本地日志**：Leader 将命令作为新日志条目追加到本地日志，term 为当前任期，index 连续递增。
2. **并行 AppendEntries**：Leader 并行向 Follower 发送 AppendEntries，携带新条目以及前一条的 prevLogIndex / prevLogTerm，用于一致性检查。
3. **一致性检查与冲突覆盖**：Follower 检查 prevLogIndex/prevLogTerm 是否匹配；不匹配则拒绝。基本算法中 Leader 将对应 nextIndex 减 1 后重试（论文另有 conflictTerm/conflictIndex 优化，一次跳过一整段冲突 term）。匹配后，Follower **删除冲突的后续条目**，再追加 Leader 发来的新条目。这是日志单向覆盖的核心：Follower 向 Leader 看齐，而不是双向合并。
4. **提交规则（易错）**：Leader **只能直接提交当前 term 的条目**——该条目复制到多数节点后标为 committed，并**顺带间接提交**它之前的所有日志（含更早 term）。**不能**仅因「旧 term 的条目已经出现在多数节点上」就提交它（论文 Figure 8）：否则新 Leader 可能还没复制该条目就被覆盖，破坏 Leader Completeness。新 Leader 当选后通常立即追加一条当前 term 的 no-op，以便尽快提交前任留下的未提交日志。
5. **应用到状态机**：Leader 更新 commitIndex，在后续 AppendEntries 中带上。各节点按 index **顺序**把已提交日志应用到状态机，不能跳号。
6. **分区愈合**：网络分区期间，少数派 Leader 凑不齐多数，无法提交。愈合后旧 Leader 见到更高 term 退位，其上未提交的条目会被新 Leader 的日志覆盖。

====== 安全性
Raft 定义了五个关键安全性约束：

1. **选举安全（Election Safety）**：在任何任期内，最多只有一个 Leader。由多数派投票保证——两个多数派必然重叠。
2. **Leader 只追加（Leader Append-Only）**：Leader 从不覆盖或删除自己的日志，只追加。冲突条目只发生在 Follower 上，由 Leader 覆盖。
3. **日志匹配（Log Matching）**：若两个节点在同一 index 有相同 term 的条目，则（a）这两条命令相同，且（b）该 index 之前的全部日志也相同。这由 AppendEntries 的一致性检查，以及 Leader 只追加、Follower 冲突即删除共同保证；选举时的日志比较并不直接推出这条性质。
4. **Leader 完全性（Leader Completeness）**：已提交的日志在后续任期的 Leader 中一定存在。选举时 Follower 拒绝日志比自己旧的 Candidate，因此当选者的日志至少与多数节点一样新（先比 lastLogTerm，再比 lastLogIndex）。
5. **状态机安全（State Machine Safety）**：若某节点已把 index i 应用到状态机，其他节点不会在同一 index 应用不同命令。这由 Leader Completeness、Log Matching，以及按序应用共同保证——不只是「全局唯一 Leader」。

====== 成员变更
原始论文使用 **联合共识（Joint Consensus）**：

1. 先把共同配置 **C(old,new)** 作为日志条目提交。在此阶段，一次决策必须**同时**获得旧配置的多数派以及新配置的多数派（不是「两个多数派的交集」这么简单）。
2. 再提交 C(new)，过渡到新配置。
3. 若当前 Leader 不在 C(new) 中，它在提交 C(new) 后下台。

联合共识实现复杂。Ongaro 的学位论文和 etcd 等实现更常用 **单节点变更**（一次只加或只减一个节点）：新旧多数派必然重叠，可避免两阶段联合配置。上一次变更必须提交完成后才能开始下一次。

====== 日志压缩
为防止日志无限增长，Raft 使用 **快照（Snapshot）**：

1. 任意节点都可以对自己已应用的状态机做快照（不必等 Leader）。
2. 快照包含状态机状态，以及所覆盖的最后一条日志的 lastIncludedIndex / lastIncludedTerm；该 index 之前的日志可以丢弃。
3. 若 Follower 落后太多，nextIndex 已落在 Leader 快照之前，则用 InstallSnapshot RPC 追赶，而不是回放整段旧日志。

====== 关键实现细节
- **RPC 调用**：Raft 定义 `RequestVote`（Candidate 发出）和 `AppendEntries`（Leader 发出，兼心跳/复制/提交通知），外加可选的 `InstallSnapshot`。RPC 应做成幂等；Follower 只回复、不主动发这三类 RPC。
- **持久化时机**：currentTerm、votedFor、log[] 必须在**回复 RPC 之前**持久化。commitIndex 和 lastApplied 是易失的，重启后从 0 起，靠 Leader 的 AppendEntries 和重放日志恢复。
- **nextIndex / matchIndex**：Leader 为每个 Follower 维护 nextIndex（下一个要发送的 index）和 matchIndex（已确认匹配的最高 index），用 matchIndex 判断当前 term 条目是否已复制到多数。

====== 工程实践
- **PreVote / CheckQuorum**（etcd 等广泛采用，非论文必选项）：分区出去的节点先预投票，得不到多数则不增加 term，避免回来后用更大的 term 打断集群；Leader 若失去与多数派的联系则主动下台。
- **线性一致读**：只读 Leader 本地状态不够——该节点可能已在分区里失去领导权。常见做法是 ReadIndex（向多数派确认自己仍是 Leader）或基于租约的读。读 Follower 可能读到过时数据。
- **客户端**：请求带唯一序列号，以便重试幂等；非 Leader 把请求重定向到当前 Leader。
- **典型系统**：etcd、Consul、TiKV、CockroachDB、Kafka KRaft、MongoDB（副本集后期）。

===== 正确性
Raft 的正确性通过论文中的安全性证明和 TLA⁺ 模型检验得到验证。以下从不同层面分析。

====== 安全性（Safety）证明思路
五项约束里最关键的是 **Leader Completeness**：

- 这里的「已提交」必须按前述提交规则理解：当前 term 的条目复制到多数后才**直接**提交；旧 term 的条目随一条当前 term 条目**间接**提交。若旧 Leader 仅因「旧 term 条目已在多数节点上」就宣布提交，论文 Figure 8 的反例会打破 Leader Completeness。
- 设 index i、term t 的条目已提交。后续选举中 Candidate 必须获得多数选票。提交时的多数派与投票多数派必然重叠，重叠节点拥有该条目（或拥有按 Log Matching 可推出包含该条目的前缀）。
- RequestVote 携带 lastLogTerm / lastLogIndex。Follower 仅在 Candidate 的日志不比自己旧时投票，因此当选 Leader 的日志至少与多数节点一样新。
- 故已提交条目一定保留在新 Leader 的日志中。这个论证与 Paxos 的多数派交叠本质等价，Raft 用连续日志 + 比较最后一条把它变得更直观。

====== 活性（Liveness）保证
Raft 不能保证在纯异步网络中一定能达成共识，这是 FLP 不可能原理下任何确定性共识算法的固有局限。活性依赖：

1. **多数派可通信**：少数派分区的 Leader 无法提交；多数派选出新 Leader。分区恢复后旧 Leader 见到更高 term 退位。
2. **重传而非「消息必达」**：允许丢包、乱序、重复，靠重试推进，不假设恰好一次传递。
3. **时序可稳定一段时间**：broadcastTime ≪ heartbeatInterval ≪ electionTimeout，使 Leader 能维持权威，又避免频繁选举。

**选举活锁**：多个节点同时超时会导致 split vote，但随机超时让反复分裂的概率随轮次迅速下降，几乎不会无限持续。

====== 与 FLP 不可能原理的关系
FLP 指出：纯异步模型中，即使只有一个节点可能崩溃，也不存在确定性算法能保证在有限时间内达成共识。Raft 的出路主要是引入超时，从而落到**部分同步**（Partial Synchrony）：在某个未知时刻之后，消息延迟变得有界。随机化选举超时是为了减少 split vote，不是绕开 FLP 的主手段。这与 Dwork、Lynch、Stockmeyer 的结论一致——部分同步下共识可解。

====== 崩溃容错
Raft 容忍 `f ≤ ⌊(N − 1) / 2⌋` 个非拜占庭节点崩溃（多数派可用即可）。3 节点容 1 个故障，5 节点容 2 个。假设故障是停故障（Fail-Stop），不处理拜占庭故障。

====== Raft 与其他共识算法的正确性对比
[cols="2,3,3,3",options="header"]
|===
| 维度 | Raft | Multi-Paxos | ZAB
| **形式化** | 论文证明 + TLA⁺ 模型检验 | 1998 论文有证明；亦有 TLA⁺ | 有形式化规范
| **提交条件** | 当前 term 条目复制到多数，并间接提交更早条目；广播 commitIndex | Prepare/Accept，或 Leader 稳定后一轮 Accept | Leader 提交并广播 COMMIT
| **主流程** | RequestVote + AppendEntries（+ 可选 InstallSnapshot） | 两阶段 Prepare/Accept | 类似的两阶段广播
| **空洞** | 不允许，日志严格连续 | 允许，需填洞后才能应用到状态机 | 不允许，日志必须连续
| **Leader** | 协议必需 | 仅为活性/性能优化 | 协议必需
| **理解门槛** | 低（设计目标就是可理解） | 高 | 中
|===

Raft 的正确性不亚于 Paxos，优势也不在「数学上更严谨」，而在论证更容易被人理解，从而减少实现错误导致的不一致。

===== 参考
https://raft.github.io/raft.pdf
https://thesecretlivesofdata.com/raft/
https://github.com/ongardie/dissertation
https://github.com/maemual/raft-zh_cn/blob/master/raft-zh_cn.md

==== Quorum NWR
===== NWR含义
三个参数：N, W, R
协议由三个可配置的参数定义：

    N (Number of Replicas) - 副本总数
    含义：一份数据在分布式集群中总共保存了多少个副本。
    目的：决定了系统的耐久性。N越大，数据能容忍的节点故障就越多。
    示例：N=3 表示每份数据都有3个副本存储在不同的节点上。

    W (Write Quorum Size) - 写法定票数
    含义：一次写操作必须成功完成的最少副本数，才算写入成功。
    目的：决定了写操作的一致性强度。W越大，写操作越可靠，但延迟也可能越高。

    R (Read Quorum Size) - 读法定票数
    含义：一次读操作必须成功查询的最少副本数，才能确定最终返回的数据。
    目的：决定了读操作的一致性强度。R越大，读到的数据越可能是最新的，但延迟也可能越高。

NWR:

    N: 副本数，也称复制因子(Replication Factor)
        表示集群中同一份数据有多少个副本
    W: 一致性级别(Write Consistency Level)
        表示成功完成W个副本更新，才算完成写操作
    R: 读一致性级别(Read Consistency Level)
        表示读取一个数据对象时需要读R个副本，返回R个副本中最新的那份数据

===== Quorum 机制
Quorum: 法定人数

NWR可以组合N、W、R参数，提供了按需选择一致性级别的灵活度:

    当 W + R > N 时:
        根据抽屉原理，在进行读操作时R个副本返回的结果中一定包含最新的数据。
        然后再利用时间戳、版本号等手段即可确定出最新的数据。
        换言之在满足该条件的参数组合下，可以实现数据的强一致性。

        因为写入的副本集合（大小为W）和读取的副本集合（大小为R）必定至少有一个重叠的副本。
        这个重叠的副本包含了最后一次成功写入的最新数据，因此读操作通过比较时间戳或版本号，总能从这个重叠副本中获取到最新的值。

    当 W + R <= N 时:
        无法实现强一致性，只能保障最终一致性。即系统可能会获取旧数据。

    一些特殊情况:
        W = N且R = 1，即WARO(Write All Read One)
        W = N: 读性能比较好
        R = N: 写性能比较好
        W = (N + 1) / 2、R = (N + 1) / 2，容错能力比较好，能容忍少数节点(即(N - 1) / 2)的故障。

===== vs-Raft
Quorum NWR 和 Raft 是不同层面的概念。Quorum NWR 是一种**读写法定票数策略**，它本身不是一个完整的共识协议；而 Raft 是一个**完整的共识算法**（包含 Leader 选举、日志复制、安全保证等）。两者的定位和适用场景不同，以下是核心对比：

.Quorum NWR vs Raft
[cols="2,3,3",options="header"]
|===
| 维度 | Quorum NWR | Raft
| **定位** | 读写一致性策略（参数化配置） | 完整的共识算法
| **机制** | 通过调整副本数 N、写法定票数 W、读法定票数 R 的组合，在一致性与性能之间做 trade-off | 通过 Leader 选举 + 日志复制实现线性一致性（线性一致读还需 ReadIndex 或租约）
| **一致性级别** | 可配置：`W+R>N` 可实现强一致性，`W+R≤N` 为最终一致性 | 强一致性（线性一致性）
| **Leader 角色** | 无 Leader，所有副本对等 | 有 Leader，所有写入必须经过 Leader
| **写入方式** | 并发写入 W 个副本即可，无需全局排序 | 写入首先到达 Leader，Leader 将日志条目复制到多数节点后提交
| **读取方式** | 读取 R 个副本，取最新版本 | 读取可走 Leader（保证强一致），也可走 Follower（可能读到旧数据）
| **容错能力** | `N - W` 个写节点故障，或 `N - R` 个读节点故障；配置灵活 | 容忍 `(N - 1) / 2` 个节点故障（多数派可用即可）
| **数据耐久性** | N 越大耐久性越好，但与一致性无关 | 日志复制到多数节点即认为已提交
| **冲突解决** | 依赖时间戳/版本号（Last-Write-Win） | 通过 Leader 的全局有序日志，保证所有节点执行顺序一致
| **复杂性** | 简单，无协调者，无选举流程 | 相对复杂，需要 Leader 选举、心跳、日志压缩等
| **适用场景** | Cassandra、DynamoDB 等 NoSQL 数据库；对最终一致性容忍度高、追求可用性和扩展性的场景 | etcd、Consul、Kafka (基于 Raft 的共识层) 等；需要强一致性和全局有序的场景
|===

==== ZAB
ZAB: zookeeper atomic broadcast(zookeeper原子广播协议)

==== POW
Proof-of-Work 工作量证明
比特币(bitcoin)就使用了工作量证明的分布式一致性算法。

工作量证明的关键: 分布式系统中的节点必须解决一个不易解决但容易验证的问题。

==== 正确性
https://lamport.azurewebsites.net/tla/tla.html